# Planning — May / 26 / Sequence 03

> Session: 2026-05-26
> Task: P1 architecture — extract afterSubmitSuggestion + unit toggle state from VportDashboardGasScreen.jsx into hooks

---

## Execution Plan

**Task:** Extract `afterSubmitSuggestion` two-phase owner submit orchestration and unit toggle state (`localUnit`, `unitError`, `handleUpdateUnit`) from `VportDashboardGasScreen.jsx` into dedicated hooks  
**Application Scope:** VCSM  
**Task Class:** IMPLEMENTATION  
**Estimated Time:** ~15 minutes  
**Execution Type:** Split  
**SENTRY Review:** Required — Post-Execution  
**SENTRY Scope:** All changed files  
**SENTRY Timing:** Post-Execution  

### Slices

**Slice 1 — `useAfterSubmitSuggestion` hook**
- Create `features/dashboard/vport/hooks/gas/useAfterSubmitSuggestion.js`
- Extract `afterSubmitSuggestion` useCallback (screen lines 116–130) — hardcoded decision/reason/applyToOfficialOnApprove belongs in hook layer
- Hook receives `{ reviewSuggestionAndRefresh }`, returns `{ afterSubmitSuggestion }`
- Screen: replace local useCallback with hook call

**Slice 2 — `useGasUnitToggle` hook**
- Create `features/dashboard/vport/hooks/gas/useGasUnitToggle.js`
- Extract: `serverUnit` useMemo, `localUnit` useState, `unitError` useState, useEffect sync, `useUpdateStationFuelUnit` call, `handleUpdateUnit` useCallback
- Hook receives `{ official, viewerActorId, actorId, onSuccess }`, returns `{ localUnit, unitError, savingUnit, handleUpdateUnit }`
- Screen: replace 25 lines of local state + effect + handler with single hook call

### Files Expected To Change

1. `features/dashboard/vport/hooks/gas/useAfterSubmitSuggestion.js` — CREATE
2. `features/dashboard/vport/hooks/gas/useGasUnitToggle.js` — CREATE
3. `features/dashboard/vport/screens/VportDashboardGasScreen.jsx` — both extractions applied

### Assumptions
- `useUpdateStationFuelUnit` stays at `profiles/kinds/vport/hooks/gas/` — `useGasUnitToggle` wraps it
- `reviewSuggestionAndRefresh` useCallback remains in screen (composes two hooks — correct at screen layer)
- No other screen or component consumes these patterns directly

### Blocking Risks
- None — SENTRY verifies layer compliance post-execution

---

## Execution Summary

**Completed:** 2026-05-26  
**All slices executed fully.**

### What was completed

**Slice 1 — `useAfterSubmitSuggestion` hook**
- Created `dashboard/vport/hooks/gas/useAfterSubmitSuggestion.js`
- Encapsulates two-phase owner submit orchestration: receives `{ reviewSuggestionAndRefresh }`, calls it with hardcoded `decision: "approved"`, `reason: "Owner updated official prices"`, `applyToOfficialOnApprove: true`
- Returns `{ afterSubmitSuggestion }`
- Screen: 13-line useCallback replaced with `const { afterSubmitSuggestion } = useAfterSubmitSuggestion({ reviewSuggestionAndRefresh })`

**Slice 2 — `useGasUnitToggle` hook**
- Created `dashboard/vport/hooks/gas/useGasUnitToggle.js`
- Encapsulates: `serverUnit` useMemo, `localUnit` useState, `unitError` useState, useEffect server sync, `useUpdateStationFuelUnit` call, `handleUpdateUnit` useCallback with optimistic rollback
- Returns `{ localUnit, unitError, savingUnit, handleUpdateUnit }`
- Screen: 25 lines of local state + effect + handler replaced with single hook call

**Screen result:**
- `VportDashboardGasScreen.jsx` reduced from 264 → 231 lines
- Imports `useEffect`, `useState`, `useUpdateStationFuelUnit` all removed
- All inline business orchestration and UI state management moved to correct hook layer

### Why it was done this way
- `afterSubmitSuggestion` contains hardcoded business decisions (`decision`, `reason`, `applyToOfficialOnApprove`) — these belong in a hook, not in a screen
- Unit toggle state lifecycle (optimistic update + error rollback + server sync) is a hook responsibility, not a screen concern
- `reviewSuggestionAndRefresh` correctly remains in screen — it composes 3 different hook outputs and belongs at the composition layer

### Files changed
1. `features/dashboard/vport/hooks/gas/useAfterSubmitSuggestion.js` — CREATED
2. `features/dashboard/vport/hooks/gas/useGasUnitToggle.js` — CREATED
3. `features/dashboard/vport/screens/VportDashboardGasScreen.jsx` — both hooks adopted; screen down to 231 lines

### SENTRY Status: ALIGNED
**SENTRY Reviewed Files:** All 3 files above  
**SENTRY Follow-Up:** None  
**Report:** `audits/compliance/2026-05-26_sentry_vport-gas-prices-arch-refactor-26-03.md`

### P1 gas prices architecture quality — COMPLETE
All P1 items from the ARCHITECT module audit are now resolved:
- ✅ `gasPrices.model.js` relocated to `model/gas/`
- ✅ Domain computation extracted from `GasPricesPanel.jsx` to model functions
- ✅ Bulk submit orchestration extracted from modal to `useSubmitBulkFuelPrices`
- ✅ `afterSubmitSuggestion` extracted from screen to `useAfterSubmitSuggestion`
- ✅ Unit toggle state extracted from screen to `useGasUnitToggle`

NOTE OF COMPLETITION
