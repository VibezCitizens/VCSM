# Planning — April / 12 / Sequence 12

## Task

Fix bootstrap duplication — platform/identity/badge reads repeating multiple times in same trace on /dev/performance.

### Source

Promoted from `.tp-ready.md`.

## Root Cause

`loadDefaultIdentityForUser()` calls `resolveAuthenticatedContext()` (5+ platform reads). AuthProvider fires `onAuthStateChange` multiple times → concurrent identity resolution → duplicate reads.

## Fix

Added in-flight promise dedup (`_identityInflight` Map) to `loadDefaultIdentityForUser()`. Concurrent calls with same userId return same promise.

## Files Changed
- apps/VCSM/src/state/identity/identity.controller.js

## Build: PASSES

NOTE OF COMPLETITION
