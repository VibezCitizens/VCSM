# Planning — April / 09 / Sequence 12

run summary session first 

then 

# Single-Source Actor Architecture

## Goal

Eliminate actor-switch desync by making **one runtime actor source of truth** and forcing every feature to derive from it, never copy it.

## Core Rule

At runtime, the **only authoritative active actor state** is:

```
identityContext.identity
```

Everything else must be **derived**, never independently stored.

That means these must **not** own actor state:

- feed viewer store
- screen-local useState(actorId)
- memoized actor snapshots that survive switches
- route-scoped viewer models
- local cache objects used as current truth
- debug stores treated as live state

## Required Architecture

1. Identity Provider is the only writer
2. Derived consumers must never persist actor identity
3. Re-key actor-scoped providers
4. All actor-dependent queries must include actorId
5. Actor switch must be version-guarded
6. Derived debug stores must subscribe, not own
7. No feature-specific actor reconstruction
8. Force actor-bound cleanup on switch
9. Invariant enforcement
10. Permanent architectural rule: No feature may store current actor independently of IdentityProvider

## Execution Plan

PLAN PROPOSAL
Task: Save Single-Source Actor Architecture rules as permanent contract
Application Scope: VCSM (documentation only)
Estimated Time: ~2 minutes
Execution Type: Direct
Files Expected To Change:
- zcontract/SINGLE_SOURCE_ACTOR_ARCHITECTURE.md (new)
- planning/april/09/09-12.md (execution summary)

## Execution Summary

### What was completed

1. **Session summary** — already written earlier to `session-summaries/2026-04-09_01-58_booking-review-identity-audit.md`

2. **Single-Source Actor Architecture contract** — saved to `zcontract/SINGLE_SOURCE_ACTOR_ARCHITECTURE.md` as a permanent locked contract with all 10 architecture rules, enforcement criteria, and review checks.

### Files created
- zcontract/SINGLE_SOURCE_ACTOR_ARCHITECTURE.md

NOTE OF COMPLETITION
