# Planning — May / 27 / Sequence 07

> Task: P1 fixes — VPORT Exchange Rate Dashboard (ARCHITECT report)

## Execution Plan

Task Class: IMPLEMENTATION
Scope: VCSM
Estimated Time: ~6 minutes
Execution Type: Direct (3 targeted edits, no slices needed)

Files Expected To Change:
- `apps/VCSM/src/features/profiles/kinds/vport/controller/rates/upsertVportRate.controller.js`
- `apps/VCSM/src/features/dashboard/vport/screens/VportDashboardExchangeScreen.jsx`

Files NOT changing (findings noted):
- `apps/VCSM/src/app/routes/protected/app.routes.jsx` — P1 Fix #4 already implemented
- `apps/VCSM/src/app/routes/protected/appRoutes.redirects.jsx` — Redirect already correct

Specialist Commands Required: SENTRY (Post-Execution)
Relevant Logan Docs: None (no existing logan doc for this module — LOGAN handoff deferred)
Documentation Mode: None required this task
SENTRY Review: Required
SENTRY Scope: upsertVportRate.controller.js, VportDashboardExchangeScreen.jsx
SENTRY Timing: Post-Execution

Assumptions:
- vport.rates table does not have an actor_id column — actorId must be supplied from screen context on persisted swap
- toNumOrNull, normalizeCurrencyCode, toRatePairKey remain inline in screen (presentation utilities, not domain transforms)
- P1 Fix #4 (legacy route redirect) is already correctly implemented via VportToActorDashboardExchangeRedirect component

Blocking Risks: None — targeted changes, no schema changes required

---

## Execution Summary

### What was completed

**Fix 1 — Cache invalidation wired (upsertVportRate.controller.js)**
- Added `import { invalidateRatesCache }` from `readVportRatesByActor.dal.js`
- Stored upsertVportRateDal return in `result`
- Called `invalidateRatesCache()` after successful write, before returning
- On failure (exception), cache is NOT invalidated — correct: data hasn't changed

**Fix 2 — Removed duplicate mapRawRateRowToDomain (VportDashboardExchangeScreen.jsx)**
- Deleted 15-line `mapRawRateRowToDomain` function from screen
- Added `import { mapVportRateRow }` from canonical model layer

**Fix 3 — actorId:null bug fixed (VportDashboardExchangeScreen.jsx:121)**
- Was: `const mapped = mapRawRateRowToDomain(saved) ?? optimisticRate;`
  — `mapRawRateRowToDomain` mapped `row.actor_id` but upsert select has no `actor_id` column → null
- Now: `const mapped = saved ? { ...mapVportRateRow(saved), actorId } : optimisticRate;`
  — uses canonical model mapper + injects correct `actorId` from screen context

**Fix 4 (P1) — Legacy route redirect confirmed pre-existing**
- `/vport/:actorId/dashboard/exchange` already uses `VportToActorDashboardExchangeRedirect`
  which issues `<Navigate to={/actor/${actorId}/dashboard/exchange} replace />`
- No change needed — already correct

### Build result
✓ 7261 modules transformed — zero errors — 5.60s

### Test result
✓ 35/35 tests passing (5 files) — no regressions

### Files changed

| File | Change |
|---|---|
| `apps/VCSM/src/features/profiles/kinds/vport/controller/rates/upsertVportRate.controller.js` | Added `invalidateRatesCache` import + call post-write |
| `apps/VCSM/src/features/dashboard/vport/screens/VportDashboardExchangeScreen.jsx` | Removed duplicate model fn; added `mapVportRateRow` import; fixed actorId:null swap |

### SENTRY Status: MINOR DRIFT

SENTRY Reviewed Files:
  - `apps/VCSM/src/features/profiles/kinds/vport/controller/rates/upsertVportRate.controller.js` — ALIGNED
  - `apps/VCSM/src/features/dashboard/vport/screens/VportDashboardExchangeScreen.jsx` — MINOR DRIFT

SENTRY Follow-Up (P3, non-blocking):
  - SF-001: Create `features/profiles/adapters/kinds/vport/model/rates/vportRates.model.adapter.js` and update screen import
  - SF-002: Create `features/profiles/adapters/kinds/vport/hooks/exchange/usePublishExchangeRatePost.adapter.js` and update screen import (pre-existing gap)

SENTRY Report: `zNOTFORPRODUCTION/CURRENT/features/dashboard/evidence/2026-05-27_02-50_sentry_vport-exchange-rate-p1-fixes.md`

### Change Intent Registry
- Entry written: NO
- Files logged: N/A — no deletions, moves, or renames occurred (edits only)

---

NOTE OF COMPLETITION
