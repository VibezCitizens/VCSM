# Planning — May / 03 / Sequence 09

> Execution complete.

## Execution Plan

Task: Optimistic + warm-cache notification cards
Application Scope: VCSM
Task Class: IMPLEMENTATION
Estimated Time: ~25 min
Execution Type: Split / 3 Slices

Slices:
  Slice 1 — useNotificationInbox: keepPreviousData + DEV marks (cache-hit/miss/render-cached/server-reconciled)
  Slice 2 — useMarkNotificationsRead: optimistic onMutate + rollback onError + DEV marks
  Slice 3 — bootstrap.invalidate + identityContext: purgeNotificationCache on actor switch/logout

Files Expected To Change:
  - apps/VCSM/src/features/notifications/inbox/hooks/useNotificationInbox.js
  - apps/VCSM/src/features/notifications/inbox/hooks/useMarkNotificationsRead.js
  - apps/VCSM/src/bootstrap/bootstrap.invalidate.js
  - apps/VCSM/src/state/identity/identityContext.jsx

Constraints: No DB changes. No RLS changes. No visual redesign. Reuse existing query keys.

---

## Execution Summary

### Slice 1 — useNotificationInbox (warm-cache + DEV marks)
- Added `keepPreviousData` from `@tanstack/react-query` to query config — prevents skeleton flash on background refetch.
- `loading` changed to `query.isPending && !query.isPlaceholderData` — skeleton only on true cold open.
- Added `isRefreshing: query.isFetching && !query.isPending` to return value.
- Added `noti:optimistic:replace` window event handler — allows external callers to patch the inbox list without a round-trip.
- DEV marks added: `[noti:cards:cache-hit/miss]` on actorId mount, `[noti:cards:render-cached]` on initial render, `[noti:cards:server-reconciled]` after each successful fetch.

Files changed:
- `apps/VCSM/src/features/notifications/inbox/hooks/useNotificationInbox.js`

### Slice 2 — useMarkNotificationsRead (optimistic onMutate + rollback)
- `useMarkNotificationRead`: added `onMutate` — cancels in-flight inbox query, snapshots prev, sets `isRead: true, isSeen: true` on matching card by `recipientId`. `onError` rolls back to snapshot.
- `useMarkAllNotificationsRead`: added `onMutate` — cancels both inbox + badge queries, snapshots both, marks all cards read, zeros badge count. `onError` rolls back both.
- DEV marks: `[noti:cards:optimistic-update]` on mutate, `[noti:cards:rollback]` on error.
- `onSuccess` still invalidates both queries to reconcile with server state.

Files changed:
- `apps/VCSM/src/features/notifications/inbox/hooks/useMarkNotificationsRead.js`

### Slice 3 — purgeNotificationCache on actor switch/logout
- Added `purgeNotificationCache()` to `bootstrap.invalidate.js` — hard evict via `removeQueries({ queryKey: ['notifications'] })`.
- Wired into `identityContext.jsx` `switchActor()` success branch alongside `purgeChatMessageCache()`.
- Wired into `identityContext.jsx` `useEffect([user?.id])` alongside `purgeChatMessageCache()`.

Files changed:
- `apps/VCSM/src/bootstrap/bootstrap.invalidate.js`
- `apps/VCSM/src/state/identity/identityContext.jsx`

### Logan Docs Updated
- `vcsm.notifications.pipeline.md` Section 7 corrected: realtime is disabled (no-op), not active on `notification.inbox_items` + `notification.recipients`. Updated to reflect polling-only model.
- `vcsm.notifications.pipeline.md` Section 11 updated: realtime subscription line corrected.

Documentation Drift Status: RESOLVED

NOTE OF COMPLETITION
