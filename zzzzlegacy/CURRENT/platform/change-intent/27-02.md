# Planning — May / 27 / Sequence 02

## Execution Plan

**Task:** Fix npm dependency vulnerabilities (Dependabot — 88 alerts on default branch)
**Application Scope:** TRAFFIC
**Task Class:** IMPLEMENTATION
**Approved:** YES

### Findings

| App | Pre-fix | Post-fix |
|---|---|---|
| VCSM | 0 vulns (current branch already updated) | 0 |
| Wentrex | 0 vulns | 0 |
| Traffic | 2 moderate — postcss 8.4.31 < 8.5.10 (GHSA-qx2v-qp2m-jg93) | 0 |

**Note:** The 88 Dependabot alerts on main are VCSM-branch artifacts from the old lockfile. The current branch (`vport-booking-feed-security-updates`) already carries updated VCSM deps (0 vuln). Those will resolve on merge. No additional code action needed for VCSM.

### Files Changed

- `apps/Traffic/package.json` — added `"overrides": { "postcss": "^8.5.10" }`
- `apps/Traffic/package-lock.json` — regenerated; postcss 8.4.31 → 8.5.15

---

## Execution Summary

**What was completed:**
- Audited all three apps + monorepo root for npm vulnerabilities
- VCSM: 0 vulns — branch already has updated deps; 88 Dependabot alerts on main will resolve on merge
- Wentrex: 0 vulns — clean
- Traffic: postcss 8.4.31 (< 8.5.10) fixed via `overrides` in `package.json`; lockfile regenerated; build verified clean

**Why this way:**
- npm's suggested fix (downgrade Next.js to 9.3.3) was nonsensical — overrides is the correct approach for transitive dependency pinning
- postcss 8.5.x is a minor version bump from 8.4.x — semver-compatible; no breaking change

**Blockers / follow-up:**
- None. All three apps are now at 0 npm audit vulnerabilities on the current branch.
- Merge `vport-booking-feed-security-updates` → main will clear the Dependabot count on the default branch.

**Files changed:**
- `apps/Traffic/package.json`
- `apps/Traffic/package-lock.json`

Change Intent Registry:
- Entry written: NO
- Files logged: N/A — no deletions, moves, or renames occurred

NOTE OF COMPLETITION
