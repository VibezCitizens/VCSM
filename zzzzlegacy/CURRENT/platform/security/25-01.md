# Planning — May / 25 / Sequence 01

> Session: 2026-05-25
> Task: VENOM P0 fixes — gas prices module security hardening

---

## Execution Plan

**Task:** Execute P0/P1 VENOM findings — vport gas prices module  
**Application Scope:** VCSM  
**Task Class:** IMPLEMENTATION  
**SENTRY Review:** Required — Post-Execution  
**SENTRY Scope:** All 5 changed files  
**SENTRY Timing:** Post-Execution  

### Slices

**Slice 1 — F-009:** Move `resolveActorIdFromProfileId` from write DAL to read DAL  
**Slice 2 — F-001/F-003/F-004:** Add ownership + access gate to `reviewFuelPriceSuggestion.controller.js`  
**Slice 3 — F-006a:** Replace string compare with `checkVportOwnershipController` in `submitFuelPriceSuggestion.controller.js`  
**Slice 4 — F-006b:** Replace string compare with `checkVportOwnershipController` in `updateStationFuelUnit.controller.js`  

### Files Expected To Change

1. `apps/VCSM/src/features/profiles/kinds/vport/dal/gas/vportFuelPrices.read.dal.js`
2. `apps/VCSM/src/features/profiles/kinds/vport/dal/gas/vportFuelPrices.write.dal.js`
3. `apps/VCSM/src/features/profiles/kinds/vport/controller/gas/reviewFuelPriceSuggestion.controller.js`
4. `apps/VCSM/src/features/profiles/kinds/vport/controller/gas/submitFuelPriceSuggestion.controller.js`
5. `apps/VCSM/src/features/profiles/kinds/vport/controller/gas/updateStationFuelUnit.controller.js`

---

## Execution Summary

**Completed:** 2026-05-25

### What was done

| Slice | VENOM Finding | File | Change |
|---|---|---|---|
| 1 | F-009 | `vportFuelPrices.read.dal.js` | Added `resolveActorIdFromProfileId` export |
| 1 | F-009 | `vportFuelPrices.write.dal.js` | Removed `resolveActorIdFromProfileId` export |
| 2 | F-001 / F-003 / F-004 | `reviewFuelPriceSuggestion.controller.js` | Added `checkVportOwnershipController` gate before any write; fixed import to read DAL; removed redundant resolve in approval block |
| 3 | F-006a | `submitFuelPriceSuggestion.controller.js` | Replaced string compare with `checkVportOwnershipController` |
| 4 | F-006b | `updateStationFuelUnit.controller.js` | Replaced string compare with `checkVportOwnershipController` |

### Why this way
- Ownership check placed in controller layer (correct) — not hook, not UI
- `checkVportOwnershipController` uses `actor_owners` table — canonical DB-backed ownership check
- `resolveActorIdFromProfileId` moved to read DAL — file scope now accurately reflects responsibility
- `targetActorId` resolved once at the ownership gate and reused in the approval block — no double DB call

### SENTRY Review
- **SENTRY Status: ALIGNED**
- **SENTRY Reviewed Files:**
  - `dal/gas/vportFuelPrices.read.dal.js`
  - `dal/gas/vportFuelPrices.write.dal.js`
  - `controller/gas/reviewFuelPriceSuggestion.controller.js`
  - `controller/gas/submitFuelPriceSuggestion.controller.js`
  - `controller/gas/updateStationFuelUnit.controller.js`
- **SENTRY Follow-Up (S-001 — LOW, non-blocking):**
  - Create `profiles/adapters/kinds/vport/ownership.adapter.js` re-exporting `checkVportOwnershipController`
  - Update 3 gas controllers to import via adapter (P3)

### Blockers / Follow-up
- VENOM F-002 (`createSystemPost` actor_id) — P1, separate task
- VENOM F-005 (anon GRANT on fuel_price_submissions) — P1, Carnage/DB task
- SENTRY S-001 adapter boundary — P3, non-blocking

NOTE OF COMPLETITION
