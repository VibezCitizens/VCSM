# Batman Planning — May / 03 / Sequence 02

Generated by: Batman
Scope: VCSM
Task Class: MIXED (DB_REVIEW + IMPLEMENTATION)

## Task

Fix VPORT Dashboard Leads permission error (business_card_leads)

## Root Cause Analysis

- Table: vport.business_card_leads (vport schema confirmed from full_schema.sql)
- RLS audit: grep of all_rls_policies.csv for "business_card_leads" → zero rows
  Meaning: no SELECT policy exists for authenticated role
  RLS is enabled on the table (permission denied = RLS blocking, not missing GRANT)
- DAL: vportLeads.read.dal.js uses `vport` Supabase client, queries from("business_card_leads")
  filtered by vport_profile_id. No schema prefix used — relies on vport client's search_path.
- INSERT path: SECURITY DEFINER RPC submit_business_card_lead — bypasses RLS, not affected.
- Controller resolves profileId from actorId via readVportProfileByActorIdDAL, then
  filters leads by vport_profile_id. Ownership path:
  auth.uid() → vc.actor_owners.user_id → vc.actor_owners.actor_id → vc.actors.vport_id
  → vport.profiles.id = business_card_leads.vport_profile_id

## Execution Plan

BATMAN PLAN
Task: Fix VPORT Dashboard Leads permission error
Scope: VCSM
Task Class: MIXED (DB_REVIEW + IMPLEMENTATION)
Execution Type: Direct
Slices: N/A

Files Expected To Change (code):
  - apps/VCSM/src/features/dashboard/vport/screens/VportDashboardLeadsScreen.jsx
    [friendly error display — raw {error} string removed]

DB Changes (text proposal only — NOT executed):
  - GRANT SELECT on vport.business_card_leads TO authenticated
  - CREATE POLICY "vport owners read own leads" on vport.business_card_leads FOR SELECT
  - Verify existing DELETE/UPDATE policies for mark-contacted and delete operations

Specialist Commands Required: DB (read-only review applied)
Batman Docs Expected: None
Assumptions:
  - vc.actors.vport_id links actor to vport.profiles.id
  - vc.actor_owners.user_id = auth.uid() is the correct ownership check
  - The write DALs (mark-contacted, delete) may also need policies — must check write.dal
  - vport client search_path includes vport schema so from("business_card_leads") resolves correctly
Blocking Risks:
  - write policies (DELETE/UPDATE) may also be missing — must confirm before proposing SQL
    so the full migration covers SELECT + any missing write policies together

## Execution Summary

Root cause confirmed:
  All 5 RLS policies were already live on vport.business_card_leads:
    - business_card_leads_owner_select (SELECT / authenticated)
    - business_card_leads_owner_update (UPDATE / authenticated)
    - business_card_leads_owner_delete (DELETE / authenticated)
    - business_card_leads_insert_anon (INSERT / anon)
    - business_card_leads_insert_authenticated (INSERT / authenticated)
  The snapshot in all_rls_policies.csv was stale — policies existed in the live DB.
  The actual missing piece: GRANT SELECT/UPDATE/DELETE on the table to authenticated.
  The table was created 2026-04-25, after the bulk grants migration (2026-04-19),
  so it was never included in that migration.

DB changes applied (user confirmed):
  GRANT SELECT ON vport.business_card_leads TO authenticated;
  GRANT UPDATE ON vport.business_card_leads TO authenticated;
  GRANT DELETE ON vport.business_card_leads TO authenticated;

Code changes applied (pre-approved with Wolverine batch):
  - VportDashboardLeadsScreen.jsx: raw error string → friendly production message

Files Changed:
  - apps/VCSM/src/features/dashboard/vport/screens/VportDashboardLeadsScreen.jsx
  DB: GRANT SELECT/UPDATE/DELETE on vport.business_card_leads TO authenticated

Generated by: Batman

BATMAN TASK COMPLETE
