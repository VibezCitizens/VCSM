# Planning — May / 03 / Sequence 02

## Task
Command Path Audit — Phase 1 (promoted from .tp-active.md, prior session)

Scan every command file in `.claude/commands/`, extract all hardcoded/implied output paths, classify each path against the proposed `zNOTFORPRODUCTION/` folder reorganization (`_ACTIVE/`, `_CANONICAL/`, `_HISTORY/`, `_BACKUPS/`), and produce two audit reports. Read-only. Do not implement.

## Execution Plan

**Task:** Command Path Audit — Phase 1
**Application Scope:** FULL REPO (.claude/commands — workspace-level, outside all protected app roots)
**Task Class:** DOCUMENTATION
**Estimated Time:** ~3 minutes
**Execution Type:** Direct

**Files Expected To Change:** None (read-only)
**Files To Read:**
- .claude/commands/*.md (19 files)
- .claude/commands/listofcomand

**Output:** Reports printed in chat (zNOTFORPRODUCTION/_ACTIVE/audits/ does not exist)

**Specialist Commands Required:** None
**Relevant Logan Docs:** None
**Documentation Mode:** Audit output only
**SENTRY Review:** Not Needed
**SENTRY Timing:** None

**Assumptions:**
- `zNOTFORPRODUCTION/_ACTIVE/audits/` folder does not exist — reports output in chat
- Phase 1 only — no moves, no file creation, no implementation

**Blocking Risks:** None (read-only)

---

## Execution Summary

**Completed:** 2026-05-03

**What was completed:**
Command Path Audit Phase 1 — read-only scan of all 19 command files in `.claude/commands/` plus `listofcomand.v2.md`. Extracted every `zNOTFORPRODUCTION/` path referenced in each file, classified each against the proposed `_ACTIVE/` / `_CANONICAL/` / `_HISTORY/` / `_BACKUPS/` reorganization, identified pre-existing issues, and produced two reports in chat.

**Key findings:**
- 13 of 19 command files have hardcoded `zNOTFORPRODUCTION/` paths and require updates before any folder move
- 6 commands (DB, Loki, Kraven, Venom, Carnage, Ironman) have zero such paths — unaffected
- CLAUDE.md (outside commands/) also references `zcontract/ARCHITECTURE.md` — must be updated too
- `zNOTFORPRODUCTION/_ACTIVE/audits/` folder does not exist — both reports were output in chat
- **Critical pre-existing bug found:** AvengersAssemble.md reads from `zNOTFORPRODUCTION/architecture/` — a folder that does NOT exist on disk. ARCHITECT writes to `zNOTFORPRODUCTION/logan/marvel/architect/` instead. AvengersAssemble would fail its ARCHITECT pass on any run regardless of reorganization.
- 3 folders have no command references: `backups/`, `phase6-screen-splits-backup/`, `supabase/`, `supabase_migrations_archive/` — safe to move anytime
- `zNOTFORPRODUCTION/apps/` found on disk but not referenced anywhere — requires investigation before any move

**Why done this way:**
Read-only pass as specified. Reports output in chat per task spec (target folder missing). No files were created or modified.

**Blockers / Follow-up:**
- Phase 2 (actual command file updates) requires explicit approval before any changes
- AvengersAssemble dead path bug should be fixed regardless of whether reorganization proceeds
- `native/` classification (→ `_ACTIVE/` vs `_CANONICAL/`) needs a decision before Robin.md can be updated
- `zNOTFORPRODUCTION/apps/` needs investigation — purpose unclear
- `debuggers/` and `db_snapshot/` need classification decision before command updates

**Files changed:** None (read-only audit)

NOTE OF COMPLETITION
