# Planning — May / 27 / Sequence 19

Deadpool — Gas prices not visible after write fix

## Symptom
Gas tab shows "—" / "None yet" for all fuel keys after manual DB INSERT of 4 price rows.
User reports app broken post DAL signature changes.

## Investigation

Write DAL callers audit: only 2 controllers call the changed DALs — both fixed.
Read path traced: fetchVportFuelPricesDAL → resolveVportProfileId → vport.profiles → fuel_prices.
Break point suspected: resolveVportProfileId may return null (RLS / domain restriction on localhost)
OR profile_id mismatch between INSERT and what resolveVportProfileId returns for this actor.

## Debug Probes Built

BUILDDED BUGBUNNY ON FILE apps/VCSM/src/features/profiles/kinds/vport/dal/gas/vportFuelPrices.read.dal.js
BUILDDED BUGBUNNY ON FILE apps/VCSM/src/features/profiles/kinds/vport/dal/services/resolveVportProfileId.dal.js

Probes log to browser console under "🐰 DEADPOOL" group.
Gated: import.meta.env.DEV — cannot run in production build.
Remove after root cause confirmed.

## Awaiting
Console output from user after hard reload on /profile/gas-1ca2 gas tab.

---

## BUGBUNNY Probe — vport schema JS client error investigation

**Problem:** resolveVportProfileId returns null for actor 21aa839b despite:
- Row exists in vport.profiles ✅
- is_active=true, is_deleted=false, owner_user_id correct ✅
- All SELECT RLS policies should pass ✅
- Affects ALL vport card modules (systematic)

**Root cause hypothesis:** vportSchema singleton is created before the supabase proxy
installs (async dynamic import). All vport schema query errors are silently dropped —
resolveVportProfileId destructures only `data`, not `error`.

**Probe added:**

BUILDDED BUGBUNNY ON FILE apps/VCSM/src/features/profiles/kinds/vport/dal/services/resolveVportProfileId.dal.js
BUILDDED BUGBUNNY ON FILE apps/VCSM/src/features/dashboard/vport/screens/VportDashboardGasScreen.jsx

VportDalProbePanel renders in DEV above the unit toggle bar.
window.__vportDalProbe__ captures every resolveVportProfileId call: { actorId, data, error, at }

**Status:** Probe deployed. Awaiting error capture from browser.

