# Planning — May / 27 / Sequence 13

## Task
Implement Subscribers Module Security Fixes — V-SUB-001, V-SUB-002, V-SUB-003, V-SUB-005

> Approved and executed 2026-05-27.

---

## Execution Summary

**What was completed:**

Slice 1 — Controller fixes (3 files):
- `follow.controller.js`: added `assertingActorId` ownership gate (V-SUB-001). Added check `assertingActorId !== followerActorId → throw 'session actor does not match follower'` after self-follow guard, before block check. Changed notification `linkPath` from `/profile/${followerActorId}` (raw UUID) to `/feed` (V-SUB-005).
- `unsubscribe.controller.js`: same ownership gate pattern (V-SUB-002). Privacy-critical: gate fires before `invalidateFeedFollowCache`, preventing attacker from busting victim's private post visibility.
- `followRequests.controller.js`: added `assertingActorId` gate to `ctrlListIncomingRequests` (V-SUB-003). Changed notification `linkPath` to `/feed` in `ctrlSendFollowRequest` and `ctrlAcceptFollowRequest` for V-SUB-005 consistency.

Slice 2 — Hook + notification caller updates (5 files):
- `useUnsubscribeAction.js`: returned function now accepts and forwards `assertingActorId`.
- `useSubscribeAction.js` (request/hooks): both `ctrlSubscribe` and `unsubscribe` calls now pass `assertingActorId: actionActorId`.
- `useFollowActorToggle.js`: `ctrlSubscribe` and `ctrlUnsubscribe` calls pass `assertingActorId: followerActorId`.
- `useIncomingFollowRequests.js`: added `useIdentity()` import, derives `sessionActorId` from session, passes `assertingActorId: sessionActorId` to `ctrlListIncomingRequests`. Strongest guarantee — session-derived, not prop-derived.
- `Notifications.controller.js`: passes `assertingActorId: targetActorId` to `listIncomingRequests` (targetActorId already session-derived from `resolveInboxActor(identity)`).

Test result (follow.controller.test.js blocked guard): One test needed `assertingActorId: FOLLOWER` added to reach the block check after the ownership gate. Intent unchanged — still verifies blocked actors are rejected. Updated in the test file.

**Final test count:** 257 total / 256 passing / 1 intentionally failing (getSubscribersController missing gate — IRONMAN decision pending)
**Previously failing (V-SUB security stubs):** 16 → now passing ✓
**Previously passing:** 81 → all still passing ✓

**Files changed:**
  apps/VCSM/src/features/social/friend/subscribe/controllers/follow.controller.js
  apps/VCSM/src/features/social/friend/subscribe/controllers/unsubscribe.controller.js
  apps/VCSM/src/features/social/friend/request/controllers/followRequests.controller.js
  apps/VCSM/src/features/social/friend/subscribe/hooks/useUnsubscribeAction.js
  apps/VCSM/src/features/social/friend/request/hooks/useSubscribeAction.js
  apps/VCSM/src/features/social/friend/subscribe/hooks/useFollowActorToggle.js
  apps/VCSM/src/features/social/friend/request/hooks/useIncomingFollowRequests.js
  apps/VCSM/src/features/notifications/inbox/controller/Notifications.controller.js
  apps/VCSM/src/features/social/friend/subscribe/controllers/__tests__/follow.controller.test.js (blocked-guard assertingActorId update)

SENTRY Status: ALIGNED
SENTRY Reviewed Files:
  - All 8 implementation files above
SENTRY Follow-Up: None required

Change Intent Registry:
- Entry written: NO
- Files logged: N/A — no deletions, moves, or renames occurred
- Registry path: zNOTFORPRODUCTION/_ACTIVE/change-intent/CHANGE_INTENT.md

NOTE OF COMPLETITION
