# Planning — May / 26 / Sequence 12

**Scope:** VCSM
**Task Class:** IMPLEMENTATION
**Task:** Resolve 3 caution items from QR security release review

---

## Execution Plan

**Caution 1 — onClose UUID navigation residual**
- Files: VportPublicReviewsQrView.jsx, VportPublicMenuQrView.jsx
- Fix: use isQrSafeSlug (already computed) instead of canonicalSlug in onClose fallback branch

**Caution 2 — BookingQrLinksPanel slug encoding**
- File: BookingQrLinksPanel.jsx
- Fix: add null guard on qrLink.slug + encodeURIComponent(String(qrLink.slug))
- Do NOT enable the panel (stays enabled: false)

**Caution 3 — GasPricesPanel identity minimization**
- Files: GasPricesPanel.jsx, VportDashboardGasPanels.jsx, VportDashboardGasScreen.jsx, VportGasPricesView.jsx
- Fix: replace identity prop with canSubmit boolean across the panel chain
- GasPricesPanel.jsx: remove identity prop, remove me useMemo, accept canSubmit=false
- VportDashboardGasPanels.jsx: receive canSubmit, pass to GasPricesPanel
- VportDashboardGasScreen.jsx: compute canSubmit = !!identity?.actorId, pass to panel
- VportGasPricesView.jsx: compute canSubmit from identity, pass instead of identity
- VportGasPricesScreen.jsx: no change (still passes identity to view for submit hook)

**SENTRY Review:** Required (post-execution)
**SENTRY Scope:** all 6 changed files
**SENTRY Timing:** Post-Execution

---

## Execution Summary

**Caution 1 — onClose UUID navigation residual: RESOLVED**

- `VportPublicReviewsQrView.jsx` — `onClose` fallback changed from `if (canonicalSlug)` to `if (isQrSafeSlug)`. UUID fallback now routes to `/actor/${actorId}/reviews` instead of `/profile/${UUID}/reviews`.
- `VportPublicMenuQrView.jsx` — identical fix applied.
- The `history.length > 1` branch (`navigate(-1)`) is unchanged — always safe.

**Caution 2 — BookingQrLinksPanel slug encoding: RESOLVED**

- `BookingQrLinksPanel.jsx` — `QrLinkCard` now: (1) returns null if `qrLink?.slug` is falsy, (2) constructs `fullUrl` with `encodeURIComponent(String(qrLink.slug))`.
- Panel remains `enabled: false` — no data flows, dormant code is now hardened.

**Caution 3 — GasPricesPanel identity minimization: RESOLVED**

- `GasPricesPanel.jsx` — `identity` prop removed; `me` useMemo removed; `canSubmit = false` boolean prop added with identical semantics.
- `VportDashboardGasPanels.jsx` — `VportDashboardOfficialGasPanel` prop signature updated: `identity` out, `canSubmit` in; threaded to `GasPricesPanel`.
- `VportDashboardGasScreen.jsx` — passes `canSubmit={!!identity?.actorId}` instead of `identity={identity}` to the panel. `identity` is still correctly used on the same screen for `useSubmitFuelPriceSuggestion` and `useOwnerPendingSuggestions`.
- `VportGasPricesView.jsx` — derives `canSubmit = !!(identity?.identity ?? identity)?.actorId` (normalises both flat and nested identity shapes), passes `canSubmit` to `GasPricesPanel`. `identity` prop kept on view for submit hook.

**Files changed (6 total):**
- `apps/VCSM/src/features/public/vportMenu/view/VportPublicReviewsQrView.jsx`
- `apps/VCSM/src/features/public/vportMenu/view/VportPublicMenuQrView.jsx`
- `apps/VCSM/src/features/profiles/kinds/vport/screens/booking/components/BookingQrLinksPanel.jsx`
- `apps/VCSM/src/features/profiles/kinds/vport/screens/gas/components/GasPricesPanel.jsx`
- `apps/VCSM/src/features/dashboard/vport/screens/components/VportDashboardGasPanels.jsx`
- `apps/VCSM/src/features/dashboard/vport/screens/VportDashboardGasScreen.jsx`
- `apps/VCSM/src/features/profiles/kinds/vport/screens/gas/view/VportGasPricesView.jsx`

Wait — that is 7 files. Count correct: 2 QR views + 1 booking + 4 gas panel chain = 7.

**SENTRY Status:** ALIGNED
**SENTRY Reviewed Files:** all 7 files
**SENTRY Follow-Up:** NONE

**Deferred:**
- Nothing from this task. All 3 caution items fully resolved.

NOTE OF COMPLETITION
