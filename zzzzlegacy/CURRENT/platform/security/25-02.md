# Planning — May / 25 / Sequence 02

> Session: 2026-05-25
> Task: VENOM F-002 + SENTRY S-001 — gas prices security hardening continuation

---

## Execution Plan

**Task:** F-002 ownership gate on publishFuelPriceUpdateAsPost.controller.js + S-001 ownership adapter boundary  
**Application Scope:** VCSM  
**Task Class:** IMPLEMENTATION  
**Estimated Time:** ~3 minutes  
**Execution Type:** Direct  
**SENTRY Review:** Required — Post-Execution  
**SENTRY Scope:** All 5 changed files  
**SENTRY Timing:** Post-Execution  

### Slices

**Slice 1 — S-001:** Create `ownership.adapter.js` + update 3 gas controller imports  
**Slice 2 — F-002:** Add `checkVportOwnershipController` gate to `publishFuelPriceUpdateAsPost.controller.js`  

### Files Expected To Change

1. `apps/VCSM/src/features/profiles/adapters/kinds/vport/ownership.adapter.js` — CREATE
2. `apps/VCSM/src/features/profiles/kinds/vport/controller/gas/reviewFuelPriceSuggestion.controller.js` — import update
3. `apps/VCSM/src/features/profiles/kinds/vport/controller/gas/submitFuelPriceSuggestion.controller.js` — import update
4. `apps/VCSM/src/features/profiles/kinds/vport/controller/gas/updateStationFuelUnit.controller.js` — import update
5. `apps/VCSM/src/features/profiles/kinds/vport/controller/gas/publishFuelPriceUpdateAsPost.controller.js` — add ownership check

---

## Execution Summary

**Completed:** 2026-05-25

### What was done

| Slice | Finding | File | Change |
|---|---|---|---|
| 1 | S-001 | `profiles/adapters/kinds/vport/ownership.adapter.js` | CREATED — re-exports `checkVportOwnershipController` |
| 1 | S-001 | `controller/gas/reviewFuelPriceSuggestion.controller.js` | Import updated to adapter path |
| 1 | S-001 | `controller/gas/submitFuelPriceSuggestion.controller.js` | Import updated to adapter path |
| 1 | S-001 | `controller/gas/updateStationFuelUnit.controller.js` | Import updated to adapter path |
| 2 | F-002 | `controller/gas/publishFuelPriceUpdateAsPost.controller.js` | Added `checkVportOwnershipController` gate before throttle check; import via adapter |

### Why this way
- Adapter is a thin re-export — no business logic — correct adapter contract
- All 4 controllers now import through the adapter boundary — structural fragility from 25-01 eliminated
- F-002 gate uses self-check (`callerActorId === actorId, targetActorId === actorId`) — `checkVportOwnershipController` treats this as "verify this actorId is a valid non-void VPORT owner in actor_owners"
- Gate placed after input/realm guards, before throttle check — security-first, no unnecessary DB calls for invalid callers
- Comment on gate explains the gap in `createSystemPost` so future readers understand why it exists

### SENTRY Review
- **SENTRY Status: ALIGNED**
- **SENTRY Report:** `audits/compliance/2026-05-25_sentry_vport-gas-prices-f002-s001.md`
- **SENTRY Reviewed Files:**
  - `profiles/adapters/kinds/vport/ownership.adapter.js`
  - `controller/gas/reviewFuelPriceSuggestion.controller.js`
  - `controller/gas/submitFuelPriceSuggestion.controller.js`
  - `controller/gas/updateStationFuelUnit.controller.js`
  - `controller/gas/publishFuelPriceUpdateAsPost.controller.js`
- **SENTRY Follow-Up:** NONE — all findings from 25-01 session resolved

### Blockers / Follow-up
- VENOM F-005 (anon GRANT on fuel_price_submissions) — P1, Carnage/DB task
- VENOM F-007 (PUBLIC_REALM_ID hardcoded) — P3
- VENOM F-008 (dual Supabase client rationale doc) — P3
- VENOM F-010 (fuelKey enum validation + price sanity in post builder) — P2

NOTE OF COMPLETITION
