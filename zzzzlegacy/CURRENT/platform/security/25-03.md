# Planning — May / 25 / Sequence 03

> Session: 2026-05-25
> Task: VENOM F-005 Carnage pass (fuel_price_submissions anon grant + RLS) + F-010 input validation

---

## Execution Plan

**Task:** F-005 — Produce migration plan for fuel_price_submissions RLS + anon grant fix; F-010 — Add fuelKey enum validation and price sanity to publishFuelPriceUpdateAsPost.controller.js  
**Application Scope:** VCSM  
**Task Class:** MIXED (DB_REVIEW + IMPLEMENTATION)  
**Estimated Time:** ~6 minutes  
**Execution Type:** Split  
**SENTRY Review:** Required — Post-Execution  
**SENTRY Scope:** `publishFuelPriceUpdateAsPost.controller.js` (F-010 only — no controller change in F-005)  
**SENTRY Timing:** Post-Execution  

### Slices

**Slice 1 — F-005 Carnage:** Audit the anon SELECT grant; revise the staged migration to remove it; produce + write new official migration with proper RLS policies (owner-select, submitter-select, citizen-insert, owner-update)  
**Slice 2 — F-010 Implementation:** Add `fuelKey` enum validation + price sanity (`finite`, `>= 0`) to `publishFuelPriceUpdateAsPost.controller.js` — filter invalid entries before `buildPostText`

### Files Expected To Change

1. `zNOTFORPRODUCTION/_ACTIVE/migrations/2026-05-10_fix_fuel_price_submissions_grants.sql` — remove anon SELECT; update comment
2. `apps/VCSM/supabase/migrations/20260525010000_fuel_price_submissions_rls.sql` — CREATE (RLS policies for fuel_price_submissions)
3. `apps/VCSM/src/features/profiles/kinds/vport/controller/gas/publishFuelPriceUpdateAsPost.controller.js` — fuelKey enum + price sanity

### Assumptions
- Staged migration has not yet been promoted to official migrations
- No RLS policy currently exists in any migration file for `fuel_price_submissions`
- `anon SELECT` is the risk; `authenticated SELECT/INSERT/UPDATE` is correct and needed
- Without RLS, anon SELECT exposes ALL rows and ALL columns

### Blocking Risks
- F-005 migration must not ship without RLS policies — applying only the GRANT without row-level filters exposes full submission data to unauthenticated clients

---

## Execution Summary

**Completed:** 2026-05-26

### What was done

| Slice | Finding | File | Change |
|---|---|---|---|
| 1 | F-005 | `_ACTIVE/migrations/2026-05-10_fix_fuel_price_submissions_grants.sql` | Removed anon SELECT grant; updated header comment with VENOM F-005 rationale |
| 1 | F-005 | `supabase/migrations/20260526010000_fuel_price_submissions_rls.sql` | CREATED — RLS enabled + 4 policies (select-manager, select-submitter, insert-citizen, update-manager) |
| 2 | F-010 | `controller/gas/publishFuelPriceUpdateAsPost.controller.js` | Added `validFuels` filter (enum + price sanity) after ownership gate; `validFuels` passed to `buildPostText` |

### Why this way
- Carnage migration uses `actor_can_manage_profile` + `vc.current_actor_id()` — matches established RLS convention across all vport.* tables
- INSERT WITH CHECK uses `submitted_by_actor_id = vc.current_actor_id()` — prevents caller from submitting under another actor's identity
- Staged migration updated (not deleted) — preserves the `authenticated` grant fix that was the original purpose; only the anon SELECT line removed
- F-010 filter reuses `FUEL_LABELS` as the enum source — no second constant introduced; DRY
- Validation placed after ownership gate, before throttle check — invalid fuels rejected before any DB read
- `no_valid_fuels` reason is distinct from `no_fuels` — caller can distinguish the two failure modes

### SENTRY Review
- **SENTRY Status: ALIGNED**
- **SENTRY Report:** `audits/compliance/2026-05-26_sentry_vport-gas-prices-f005-f010.md`
- **SENTRY Reviewed Files:** `publishFuelPriceUpdateAsPost.controller.js`
- **SENTRY Follow-Up:** NONE

### Blockers / Follow-up
- VENOM F-007 (PUBLIC_REALM_ID hardcoded) — P3
- VENOM F-008 (dual Supabase client doc) — P3
- F-005 migration requires DB deployment — Carnage plan complete; apply via normal migration process

NOTE OF COMPLETITION
