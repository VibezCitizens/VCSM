# Planning — May / 18 / Sequence 01

_Created:_ 2026-05-18  
_Wolverine run:_ Feed DAL RLS Migration — P2 / P1 / P3  

---

## Task

Write three RLS migration files for the feed DAL security surfaces identified by CARNAGE (2026-05-14) and verified by DB (2026-05-18):

- **P2** — `vc.actor_onboarding_steps` — enable RLS + ALL policy (SELECT + UPSERT WITH CHECK)
- **P1** — `moderation.actions` — enable RLS + SELECT + INSERT policies
- **P3** — `vc.actor_follows` SF-07 — create `vc.get_follower_count()` SECURITY DEFINER function + FORCE RLS + drop `actor_follows_select_public_subscriber_count`

---

## Execution Plan

Task class: IMPLEMENTATION  
Application scope: VCSM  
Execution type: Split (3 slices + doc append)

Slices:
- P2 → `20260518010000_actor_onboarding_steps_rls.sql`
- P1 → `20260518020000_moderation_actions_rls.sql`
- P3 → `20260518030000_actor_follows_sf07_resolution.sql`
- LOGAN append → `vcsm.dal.feed.md`

---

## Execution Summary

**Completed:** 2026-05-18

### What was completed
- Created `20260518010000_actor_onboarding_steps_rls.sql` — enables RLS + SELECT + ALL WITH CHECK scoped to actor_owners. Addresses BW-FEED-02 / V3 (unprotected UPSERT write path).
- Created `20260518020000_moderation_actions_rls.sql` — enables RLS + SELECT + INSERT WITH CHECK scoped to actor_owners. Addresses BW-FEED-03 / V1 (behavioral PII leak + silent hidden-post regression risk).
- Created `20260518030000_actor_follows_sf07_resolution.sql` — creates `vc.get_follower_count()` SECURITY DEFINER function + FORCE RLS + drops `actor_follows_select_public_subscriber_count`. Addresses BW-FEED-01 / SF-07 (social graph enumeration, 8 days open). Code prerequisite (`subscriberCount.dal.js` on RPC) was already complete.
- Appended Wolverine migration pass summary to `vcsm.dal.feed.md`.

### Why it was done this way
Migration files are SQL text source artifacts — not live database executions. This allows SENTRY review and version control before staging application. Staging validation checklist embedded in each file. P3 execution order documented explicitly to prevent the function-missing failure mode.

### Files changed
- NEW: `apps/VCSM/supabase/migrations/20260518010000_actor_onboarding_steps_rls.sql`
- NEW: `apps/VCSM/supabase/migrations/20260518020000_moderation_actions_rls.sql`
- NEW: `apps/VCSM/supabase/migrations/20260518030000_actor_follows_sf07_resolution.sql`
- UPD: `zNOTFORPRODUCTION/_CANONICAL/logan/vcsm/dal/vcsm.dal.feed.md`

### SENTRY Status: ALIGNED
SENTRY Reviewed Files:
- `apps/VCSM/supabase/migrations/20260518010000_actor_onboarding_steps_rls.sql`
- `apps/VCSM/supabase/migrations/20260518020000_moderation_actions_rls.sql`
- `apps/VCSM/supabase/migrations/20260518030000_actor_follows_sf07_resolution.sql`
SENTRY Follow-Up: None required.

### Blockers / Follow-Up
- Engineer must apply migrations to staging in order (P2 → P1 → P3) with validation at each step
- P3 Step 3 (DROP POLICY) must not run before Step 1 (function) is confirmed live
- VENOM re-verify required after staging application
- THOR release gate after VENOM confirms clean
- P4 (`vc.post_reactions` SELECT verification) still requires live schema query

### Logan Docs Reviewed
- `zNOTFORPRODUCTION/_CANONICAL/logan/vcsm/dal/vcsm.dal.feed.md`

### Logan Docs Updated
- `zNOTFORPRODUCTION/_CANONICAL/logan/vcsm/dal/vcsm.dal.feed.md` — Wolverine migration pass appended

### Documentation Drift Status
NONE — doc updated in same pass as implementation.

NOTE OF COMPLETITION

