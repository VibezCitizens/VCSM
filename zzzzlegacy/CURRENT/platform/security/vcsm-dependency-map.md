# VCSM Dependency Map

Generated by: Batman
BAT Reference: BAT-13-02
Date: April 13, 2026

---

## Dependency Direction Rule

```
DAL → Model → Controller → Hook → Screen
Apps → Engines → Shared
Engines NEVER import from Apps
```

---

## Cross-Feature Communication Pattern

All inter-feature communication uses the adapter pattern:

```
features/A/screens/
  → features/A/hooks/
  → features/A/controllers/
  → features/A/dal/
  + features/B/adapters/   ← only cross-feature import allowed
```

**49 adapter files** enforce all cross-feature boundaries.
**No direct cross-feature imports detected** outside of adapters.

---

## Cross-Feature Dependency Graph

### moderation → consumers
```
moderation/adapters
  → post (ReportModal, ReportedObjectCover, useReportFlow)
  → feed (ReportModal, ReportThanksOverlay, usePostVisibility)
  → chat (ReportModal, ChatSpamCover, useConversationCover)
  → profiles (usePostVisibility, useCommentVisibility)
```

### social → consumers
```
social/adapters
  → feed (useFollowActorToggle, useFollowStatus)
  → settings (useFollowRequestActions, useIncomingFollowRequests)
  → notifications (useFollowRequestActions)
```

### booking → consumers
```
booking/adapters
  → dashboard (useOwnerBookingResources)
  → profiles/kinds/vport (booking resource management)
```

### post → consumers
```
post/adapters
  → feed (post card rendering)
  → notifications (PostDetail.view.adapter)
  → profiles (post display in profile tabs)
```

### upload → consumers
```
upload/adapters
  → post (LinkifiedMentions, mention suggestions)
```

### block → consumers
```
block/adapters
  → chat (BlockConfirmModal)
  → settings (useMyBlocks)
```

### profiles → consumers
```
profiles/adapters
  → dashboard/vport (vport-specific screens and hooks)
  → vport (VPORT_TYPE_GROUPS, useUpsertVportServices)
```

### settings → consumers
```
settings/adapters
  → dashboard (VportAboutDetails, Card components)
  → ads (OnemoredaysAd)
```

---

## Engine Dependency Map (VCSM → Engines)

```
VCSM app
  → @identity     (identityContext, identity.controller, setup.js)
  → @hydration    (actorStore, useActorSummary, hydrateActors, feed, notifications, chat, explore, profiles)
  → @chat         (all chat/* hooks, chat/setup.js, inbox search)
  → @reviews      (reviews/setup.js, VportReviews.controller.js)
  → @portfolio    (portfolio/setup.js, VportDashboardPortfolioScreen.jsx)
  → @notifications (notifications/setup.js, publish.js, all inbox controllers)
```

### Engine Consumer File Count
| Engine | Consumer Files |
|--------|---------------|
| @hydration | 15 files |
| @chat | 9 files (+ 5 dev diagnostics) |
| @notifications | 5 files |
| @identity | 5 files |
| @reviews | 2 files |
| @portfolio | 2 files |

---

## Engines → Apps (REVERSE — NONE)

Result: **CLEAN** — No engine imports from apps/VCSM detected.
Dependency direction is fully enforced.

---

## Layer Violation Detection

**DAL Layer:**
- ✅ All Supabase queries confined to DAL files
- ✅ No direct DB calls in screens or hooks
- ✅ Controllers call DALs, not the reverse

**Controller Layer:**
- ⚠️ moderation/controllers/moderationActions.controller.js — accepts moderatorActorId as param with NO permission check (security issue — see VENOM report)
- ⚠️ dashboard/vport/controller/saveVportPublicDetailsByActorId — no ownership assertion before upsert (security issue — see VENOM report)

**Screen Layer:**
- ✅ Screens are composition-only
- ✅ No direct Supabase calls in screens

---

## Circular Dependency Detection

**Result: NONE DETECTED**

All detected cross-feature flows are unidirectional:
- moderation → post/feed/chat (not reverse)
- social → feed/settings/notifications (not reverse)
- booking → dashboard/profiles (not reverse)

No circular dependency chains found in the adapter graph.

---

## Notable Structural Notes

1. `chat` feature is a thin wrapper — all logic lives in the `@chat` engine
2. `reviews` and `portfolio` features are setup-only wrappers (2 files each)
3. `hydration` feature is a setup file + one hydrator adapter (`vcsmActorHydrator.js`)
4. `actors` feature has DAL/controller/model but no screens — pure internal utility
5. `vport` feature has no screens — rendering is handled by `profiles/kinds/vport/` screens
