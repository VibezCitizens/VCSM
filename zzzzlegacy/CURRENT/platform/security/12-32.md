# Planning — April / 12 / Sequence 32

> Task: Update VPORT creation flow to new actor-backed + vport.* contract

---

## Execution Plan

**Task:** Update VPORT creation flow to new actor-backed + vport.* contract
**Application Scope:** VCSM
**Task Class:** IMPLEMENTATION
**Execution Type:** Direct

**Changes:**

`vport.core.dal.js` — createVport():
- RPC call is already compatible (same params, same endpoint)
- Add `profileId: data.vport_id` and `profile_id: data.vport_id` to return object
  (vport_id from new RPC = vport.profiles.id, not vc.vports.id)
- Preserve all existing keys for backward compatibility

`CreateVportForm.jsx`:
- 2x `res?.actor_id` → `res?.actorId`
- Remove `console.error` in service upsert catch block
- Remove `console.error` in outer catch block

---

## Execution Summary

**What was completed:**

Two files updated — creation flow now aligned with the new `vc.create_vport` RPC contract.

| File | Change |
|---|---|
| `apps/VCSM/src/features/vport/dal/vport.core.dal.js` | Added `profileId` and `profile_id` to `createVport()` return shape. `vport_id` now = `vport.profiles.id`. All prior keys preserved. |
| `apps/VCSM/src/features/vport/CreateVportForm.jsx` | Updated 2x `res?.actor_id` → `res?.actorId`. Removed two `console.error` calls (violates CLAUDE.md no-console rule). |

**Why it was done this way:**
- The DB-side `vc.create_vport` function was already rewritten and deployed this session to write actor first → vport.profiles second → backfill actor.vport_id. The app layer just needed to expose `profileId` from the returned `vport_id` and use the camelCase `actorId` consistently.
- No controller, hook, or screen changes were needed — the form calls `createVport()` directly via the model re-export, and the return shape addition is additive.

**Blockers / Follow-up:**
- `vc.create_vport` must be deployed with the three fixes from the DB review session: (1) `vport.profile_public_details` row creation, (2) cross-table slug uniqueness guard, (3) `pg_temp` removed from search_path.
- `vc.get_actor_summaries` still joins only `vc.vports` — new VPORTs will show blank display names in feeds until that function is updated.

**Files changed:**
- `apps/VCSM/src/features/vport/dal/vport.core.dal.js`
- `apps/VCSM/src/features/vport/CreateVportForm.jsx`

NOTE OF COMPLETITION
