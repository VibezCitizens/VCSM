# Planning — May / 14 / Sequence 03

> SF-07 P3b — subscriberCount.dal.js RPC migration

---

## Execution Plan

### PLAN PROPOSAL

**Task:** SF-07 P3b — replace direct `vc.actor_follows` table query in `subscriberCount.dal.js` with a call to `vc.get_follower_count(p_actor_id)` SECURITY DEFINER RPC.

**Application Scope:** VCSM

**Task Class:** IMPLEMENTATION

**Estimated Time:** < 2 minutes (single-file, minimal change)

**Execution Type:** Direct (no slices)

**Files Expected To Change:**
- `apps/VCSM/src/features/social/friend/subscribe/dal/subscriberCount.dal.js`

**Specialist Commands Required:** None (single DAL file, no architecture boundary crossed)

**Relevant Logan Docs:** None (no new pattern introduced — RPC call convention already established)

**Documentation Mode:** None required

**SENTRY Review:** Optional
**SENTRY Scope:** `subscriberCount.dal.js` — DAL file, RPC call substitution only
**SENTRY Timing:** Post-Execution

**Assumptions:**
- P3a (`vc.get_follower_count` SECURITY DEFINER function) will be deployed to staging before this code is tested
- P3c (policy drop) occurs only after P3a + P3b are confirmed in production
- 60s TTL cache and `invalidateFollowerCount` remain unchanged
- RPC convention: `supabase.rpc('get_follower_count', { p_actor_id: actorId })` — no schema prefix (matches existing codebase pattern)
- RPC returns a `bigint` — read via `.data` (not `.count`)

**Blocking Risks:** None at source level. P3a DB function must exist in the environment before runtime can succeed.

---

## Execution Summary

**Completed:** SF-07 P3b — `subscriberCount.dal.js` RPC migration

**What changed:**
- `apps/VCSM/src/features/social/friend/subscribe/dal/subscriberCount.dal.js`
  - Removed direct `.schema('vc').from('actor_follows').select(...).eq(...).eq(...)` query
  - Replaced with `supabase.rpc('get_follower_count', { p_actor_id: actorId })`
  - Read result from `.data` (bigint) wrapped in `Number(data ?? 0)` — not `.count`
  - 60s TTL cache and `invalidateFollowerCount` unchanged

**Why this way:**
- Eliminates dependency on the broad SF-07 policy (`actor_follows_select_public_subscriber_count`)
- SECURITY DEFINER function encapsulates the count logic server-side — no client-visible row access
- Unblocks P3c: the broad policy can be dropped after P3a + P3b are confirmed in production

**Blockers / Follow-up:**
- P3a: Engineer must deploy `vc.get_follower_count(p_actor_id uuid) RETURNS bigint SECURITY DEFINER` + `GRANT EXECUTE TO authenticated` before this code path is runtime-testable
- P3c: Drop `actor_follows_select_public_subscriber_count` policy — staging only after P3a+P3b confirmed; production after full verification
- P4: Engineer runs pre-check query on `vc.post_reactions` SELECT policy shape (verification only — no change expected)
- P1: Engineer enables RLS on `moderation.actions` + actor-scoped SELECT policy
- P2: Engineer enables RLS on `vc.actor_onboarding_steps` + actor-scoped ALL policy
- IM6 (deferred): CentralFeedScreen Final/View split

**Files changed:**
- `apps/VCSM/src/features/social/friend/subscribe/dal/subscriberCount.dal.js`

NOTE OF COMPLETITION
