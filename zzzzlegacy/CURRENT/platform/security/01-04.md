Debug the Parent Dashboard student detail/settings screen in Wentrex.

Current symptom on the page:
	•	Student header loads correctly
	•	Link/relationship section loads correctly
	•	Guardian phone and relationship show
	•	But these fields are missing or wrong:
	•	School Account shows: Student identity not found
	•	Login ID is blank
	•	School Managed shows No
	•	Must Change Password shows No
	•	Guardian Name is blank
	•	Guardian Email is blank

Goal:
Find the exact break in the data pipeline for:
	1.	School Account block
	2.	Guardian Contact block

Do not patch the UI blindly.
Do not add fake fallback values.
Do not modify database schema.
Fix the code/data path only.

Constraints:
	•	read-only with respect to database structure
	•	no schema changes
	•	no fake defaults
	•	no hardcoded guardian values
	•	preserve current architecture
	•	investigation first, modification only after root cause is proven

Debug scope:
	•	parent dashboard student detail/settings screen
	•	DAL / controller / hook / adapter / UI path feeding this page
	•	student identity lookup path
	•	guardian contact lookup path
	•	learning.actor_identities usage
	•	learning.actor_profiles usage
	•	parent link / student link source path

What to inspect:

A. School Account block
	1.	Trace the exact data source for:
	•	login_id
	•	is_school_managed
	•	must_change_password
	•	“Student identity not found”
	2.	Confirm whether this block reads from:
	•	learning.actor_identities
	•	learning.actors
	•	another derived mapper
	3.	Verify the student actor id coming from the route:
	•	confirm the route param actor id
	•	confirm it matches the actor id used in the identity query
	4.	Prove whether the issue is:
	•	wrong actor id
	•	wrong join
	•	wrong schema/table
	•	null row in learning.actor_identities
	•	controller not selecting fields
	•	model/adapter dropping identity fields
	•	UI reading wrong prop names

B. Guardian Contact block
	1.	Trace the exact data source for:
	•	guardian name
	•	guardian email
	•	guardian phone
	•	relationship
	2.	Confirm whether guardian name/email are expected from:
	•	learning.actor_identities.parent_name
	•	learning.actor_identities.parent_email
	•	parent actor profile
	•	parent auth/public profile
	•	parent_student_links
	3.	Prove why phone and relationship render while guardian name/email are blank
	4.	Determine whether:
	•	the query never selects the guardian fields
	•	the selected fields are null in DB
	•	a mapper renames/drops them
	•	the UI is bound to the wrong property names

C. Required forensic trace
Trace the exact active path used by the screen:
	•	DAL query
	•	controller/service output
	•	hook result
	•	screen props
	•	final rendered fields

D. Temporary dev-only debugging
If static inspection is not enough, add temporary dev-only visible logs showing:
	•	route student actor id
	•	fetched actor_identities row
	•	fetched actor_profiles row
	•	parent link row
	•	final object sent to UI for school account
	•	final object sent to UI for guardian contact

Debugger rules:
	•	local/dev only
	•	automatic console/screen output
	•	no production leakage
	•	remove after root cause is confirmed unless explicitly approved to keep guarded dev diagnostics

Required output format:

BUG SUMMARY
Observed behavior:
Expected behavior:
Where it occurs:
Visible error/logs:
Initial hypotheses:

PIPELINE TRACE
Route param:
DAL output:
Controller output:
Hook result:
Screen props:
Rendered values:

BREAK POINT FOUND
Location:
Issue:
Proof:
Impact:

ROOT CAUSE
Cause:
Why it happened:
Why the system allowed it:
Whether similar bugs may exist elsewhere:

FIX PLAN
Files to change:
Change required:
Reasoning:
Risk level:
Temporary debug code to remove:
Any follow-up checks:

BUG FIX PROPOSAL
Root cause:
Fix location:
Files affected:
Estimated time:
Debug probe needed: Yes/No

Important:
Do not stop at “row missing in DB” unless you prove the page is correctly looking up the right actor and the right source of truth.
If the page is reading the wrong actor or wrong source table, identify that as the real bug.
:::

## Execution Plan

**PLAN PROPOSAL**
- **Task:** Fix Guardian Contact to read from parent's own profile; make School Account resilient to missing actor_identities
- **Application Scope:** WENTREX
- **Estimated Time:** ~3 minutes
- **Execution Type:** Direct
- **Files Expected To Change:**
  - `apps/wentrex/src/learning/parent/screens/ParentStudentScreen.jsx`
- **Assumptions:**
  - Parent name/email should come from parent's own actor_profiles (full_name) and actor_identities (login email), not from student's actor_identities.parent_name/parent_email
  - Student actor_identities may legitimately be missing for pre-migration students
  - parentActorId is available from identity context

**Root cause (BUGSBUNNY):**
1. School Account: `identity` is null because the student has no `learning.actor_identities` row (pre-migration student or never provisioned via create-student)
2. Guardian Name/Email: reads from `identity?.parent_name` and `identity?.parent_email` which are on the STUDENT's actor_identities row — wrong source. Should read from the PARENT's own profile.
3. Guardian Phone/Relationship work because they read from `actor_profiles` and `parent_student_links` respectively — correct sources.

## Execution Summary

**What was completed:**
- Added `parentProfile` and `parentIdentity` state + fetches in `ParentStudentScreen.jsx`
- Parent profile fetched from `learning.actor_profiles` WHERE `actor_id = parentActorId` (the parent's own record)
- Parent identity fetched from `learning.actor_identities` WHERE `actor_id = parentActorId` (the parent's own login record)
- Both fetched in parallel alongside the existing parent_student_links query
- Guardian Contact block updated:
  - Guardian Name: now reads `parentProfile.full_name` (was `identity.parent_name` from student's record)
  - Guardian Email: now reads `parentIdentity.synthetic_email` or `parentIdentity.login_id` (was `identity.parent_email` from student's record)
  - Guardian Phone: now reads `parentProfile.guardian_phone` with fallback to `student.guardian_phone`
  - Relationship: unchanged (already reads from correct `parent_student_links` source)
- School Account block: unchanged — it correctly reads from student's `actor_identities`. If that row is missing, fields show "—"/"No" which is accurate (no school account provisioned yet).

**Why:** The Guardian Contact was reading denormalized parent data from the student's `actor_identities` row (`parent_name`, `parent_email`). These fields are set during student creation and may be stale or unpopulated. The parent's actual name and email live on the parent's own `actor_profiles` and `actor_identities` records.

**Files changed:**
- `apps/wentrex/src/learning/parent/screens/ParentStudentScreen.jsx`

**Build:** Passes cleanly.

**Follow-up:** School Account showing "—" for Login ID is correct when `actor_identities` is missing. If students need identity rows backfilled, that's a separate data migration task, not a code bug.

## CAPTAIN — Orders for Next Session

---

### 2026-04-01 19:30

**Idea:**
Harden student no-enrollment fallback — explicit empty state instead of broken navigation

**Context:**
Requiring active course enrollment before showing the student dashboard is correct authorization. The problem is the failure mode: when a student has no enrollment, roleKeys resolves empty, defaultDestination falls back to `"wentrex_dashboard"` (a raw platform key, not a route), and the app navigates to a non-existent route — making the student appear logged out. This is bad UX and weak defensive coding.

**Next Action:**
1. Fix the identity resolver or routing layer so that empty roleKeys for a known student actor produces a clear "not enrolled" state instead of navigating to an invalid route
2. Never let `platform.user_app_state.default_destination_key` be used as a raw navigation target — it must be mapped to a valid route or ignored
3. Add an explicit `/student/not-enrolled` route (or equivalent) that shows: "Your account is active, but you are not enrolled in any class yet. Contact your school administrator."
4. Ensure the flow is: auth succeeds → actor resolves → no enrollment detected → clear message, not: auth succeeds → empty roles → broken fallback → silent redirect to landing page

**Scope:**
WENTREX

**Notes:**
- The enrollment requirement is good practice — keep it
- The fallback behavior is the bug, not the rule
- `wentrex_dashboard` as a destination key should never reach `navigate()` — it's a platform-level key, not a frontend route
- Consider whether `defaultDestination` in the identity context should be validated against a known route list before being used

---

### 2026-04-01 21:00

**Idea:**
VCSM auth migration to platform/identity engine — complete plan designed

**Context:**
Full forensic trace completed of VCSM's current auth/identity flows (24 files). Current state: VCSM uses vc.actors + vc.actor_owners + public.profiles directly. Does NOT use engines/identity at all. Engines/identity is proven working via Wentrex. VCSM needs to adopt the engine so both apps share identity infrastructure.

Key findings from the trace:
- Registration: auth.signUp → profiles upsert → onboarding → RPC create_actor_for_user → vc.actor_owners upsert
- Login: auth.signInWithPassword → session hydrate → ensure discoverable
- Identity: lists vc.actor_owners → hydrates with profiles/vports/privacy/realms → provides useIdentity()
- No platform.* tables touched anywhere in VCSM auth/identity flow
- No provisioning RPC exists for VCSM (provision_vcsm_identity does not exist yet)
- The engine already expects appKey='vcsm' as valid
- Wentrex pattern is proven: setup.js → resolver → context → adapter

**Next Action:**
1. Create `platform.provision_vcsm_identity` RPC (mirrors provision_wentrex_identity but for vc.actors)
2. Ensure `platform.apps` has row with key='vcsm'
3. Build VCSM identity adapter: features/identity/setup.js, resolvers/vcsmIdentity.resolver.js, VcsmIdentityContext.jsx
4. Add @identity Vite alias to VCSM vite.config.js
5. Phase 1: dual-write on signup (existing vc.* flow + platform provisioning)
6. Phase 2: login resolves via engine first, fallback to legacy
7. Phase 3: identity context wraps engine instead of direct vc.* queries
8. Phase 4: reduce legacy identity dependence

**Scope:**
VCSM + ENGINE

**Notes:**
- Migration plan file: /docs/VCSM_ENGINE_ARCHITECTURE_INSPECTION.md
- Current auth files: features/auth/ (14 files), state/identity/ (7 files), app/providers/AuthProvider.jsx, app/guards/ProtectedRoute.jsx, features/auth/screens/CompleteProfileGate.jsx
- First 5 files to change: onboarding.controller.js (add platform provisioning after actor creation), identity.controller.js (add engine fallback), identityContext.jsx (wrap engine), vite.config.js (add alias), features/identity/setup.js (new)
- Lowest-risk start: add @identity alias + create features/identity/setup.js (zero behavior change)
- Biggest risk: identity resolution must never break — entire app depends on useIdentity()
- Legacy users without platform rows must still work via self-healing pattern (same as Wentrex)
- Do NOT rewrite — incremental migration only