# Planning — May / 26 / Sequence 05

> Session: 2026-05-26
> Task: Governance — promote staged grants migration to official supabase/migrations/ path

---

## Execution Plan

**Task:** Write `20260526020000_fix_fuel_price_submissions_grants.sql` to `apps/VCSM/supabase/migrations/` — content from `_ACTIVE/migrations/2026-05-10_fix_fuel_price_submissions_grants.sql` with stale validation comment corrected  
**Application Scope:** VCSM  
**Task Class:** IMPLEMENTATION  
**Estimated Time:** ~3 minutes  
**Execution Type:** Direct  
**SENTRY Review:** Not needed  

### Changes

1. `apps/VCSM/supabase/migrations/20260526020000_fix_fuel_price_submissions_grants.sql` — CREATE
   - GRANTs: SELECT, INSERT, UPDATE on `vport.fuel_price_submissions` and `vport.fuel_price_submission_reviews` for authenticated role
   - anon: NO access (confirmed by VENOM F-005 review)
   - Validation comment: remove stale "anon row for SELECT on fuel_price_submissions" line

### Assumptions

- Last migration is `20260526010000_fuel_price_submissions_rls.sql` — `020000` is the next available slot
- GRANTs are idempotent — safe no-op if already applied
- Staged file at `_ACTIVE/migrations/` stays in place (non-production directory, out of scope to remove)

### Blocking Risks

- None

---

## Execution Summary

**Completed:** 2026-05-26

### What was completed

- Created `apps/VCSM/supabase/migrations/20260526020000_fix_fuel_price_submissions_grants.sql`
- Content: `GRANT SELECT, INSERT, UPDATE ON vport.fuel_price_submissions TO authenticated` + same for `vport.fuel_price_submission_reviews`
- Corrected stale validation comment: replaced "anon row for SELECT on fuel_price_submissions" with "anon: NO rows (anon access intentionally withheld)"
- Migration sits immediately after `20260526010000_fuel_price_submissions_rls.sql` — correct sequence

### Why it was done this way

PostgreSQL evaluates GRANTs before RLS. These two tables had valid RLS policies (deployed in 010000) but no GRANT for the authenticated role, meaning the policies would never fire and authenticated users would receive "permission denied". The grants migration must exist in the official migrations path to be part of the tracked migration history alongside its paired RLS migration.

### Files changed

1. `apps/VCSM/supabase/migrations/20260526020000_fix_fuel_price_submissions_grants.sql` — CREATED

NOTE OF COMPLETITION
