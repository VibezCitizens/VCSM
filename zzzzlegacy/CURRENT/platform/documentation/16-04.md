# Planning — April / 16 / Sequence 04

> Promoted from active queue: fix upsertVportServicesByActorDal missing actorId.

## Prompt Registry (AI Directive Log)

---

### Prompt Entry
Timestamp: 2026-04-16
Source: User Input (active queue)

```
Fix the Vport Dashboard bug:
Error: `upsertVportServicesByActorDal: actorId is required`
The dashboard is reaching the service upsert path, but actorId is missing.
Trace the full flow, fix propagation, add defensive guard upstream.
```

---

## Execution Plan

Task: Fix missing actorId in upsertVportServices controller → DAL call
Scope: VCSM
Class: DEBUGGING

Root Cause:
`upsertVportServices.controller.js` line 76 called:
  `upsertVportServicesByActorDal({ rows: payload })`
`actorId: targetActorId` was not passed. `targetActorId` was in scope and already validated on line 23-25.

File Changed:
- apps/VCSM/src/features/profiles/kinds/vport/controller/services/upsertVportServices.controller.js

---

## Execution Summary

### Completed

**Fix — upsertVportServices.controller.js: pass actorId to DAL**

Before:
```js
const saved = await upsertVportServicesByActorDal({ rows: payload });
```

After:
```js
const saved = await upsertVportServicesByActorDal({ actorId: targetActorId, rows: payload });
```

Why: `upsertVportServicesByActorDal` requires `actorId` to resolve `profile_id` via `resolveProfileId(actorId)` before writing to `vport.services`. The controller had `targetActorId` in scope and already validated it — it just wasn't forwarded to the DAL call.

### Call chain confirmed correct (no changes):
- `useUpsertVportServices.js` — passes `targetActorId` to controller ✓
- `upsertVportServicesByActorDal` — guard validates `actorId` ✓
- Upstream hook call sites — not the source of the bug ✓

### Files Changed
- apps/VCSM/src/features/profiles/kinds/vport/controller/services/upsertVportServices.controller.js

### Logan Docs Reviewed
None (no Logan doc for vport services feature)

### Logan Docs Updated / Created
None

### Documentation Drift Status
Not applicable

### SENTRY Status: ALIGNED
SENTRY Reviewed Files:
  - apps/VCSM/src/features/profiles/kinds/vport/controller/services/upsertVportServices.controller.js

SENTRY Notes:
- Controller remains pure: no DB imports, no React, no cross-feature calls outside its own DAL.
- Layer boundary: controller → DAL only. No layer violations introduced.
- `targetActorId` is the vport actor id (correct type — not profileId, not userId).
SENTRY Follow-Up: None

NOTE OF COMPLETITION
