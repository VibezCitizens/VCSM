# Planning — May / 03 / Sequence 13

## Task

TRAZE PWA/mobile polish — hide scrollbars, safe-area support, PWA meta

## Execution Plan

PLAN PROPOSAL
Task: TRAZE PWA/mobile polish — hide scrollbars, safe-area support, PWA meta
Application Scope: TRAFFIC
Task Class: IMPLEMENTATION
Estimated Time: ~8 min
Execution Type: Direct
Slices: N/A
Files Expected To Change:
  - apps/Traffic/src/app/globals.css
  - apps/Traffic/src/app/layout.jsx
Specialist Commands Required: None
Relevant Logan Docs: None
Documentation Mode: None required
SENTRY Review: Not Needed
SENTRY Scope: N/A
SENTRY Timing: None
Assumptions:
  - Next.js 14 export const viewport is supported
  - Cloudflare Pages serves Traffic; no server-side concerns for CSS
  - Scrollbar hiding applied globally via globals.css
  - Horizontal chip rows use class-based CSS in globals.css
Blocking Risks:
  - 100svh may cause layout issues on older browsers — mitigated by vh fallback

## Execution Summary

What was completed:
- Added `--safe-top/right/bottom/left` env() vars to `:root` in globals.css
- Updated `html, body` min-height to `100svh` with `100vh` fallback
- Added full scrollbar hiding on `html, body` (scrollbar-width, -ms-overflow-style, ::-webkit-scrollbar)
- Updated `main` bottom padding to include `var(--safe-bottom)` for Safari toolbar clearance
- Added `padding-top: var(--safe-top)` to `.traffic-shell-header` for PWA status bar
- Added scrollbar hiding + `-webkit-overflow-scrolling: touch` + `padding-inline-end` safe-right to:
  - `.homepage-quick-actions` / `.homepage-filter-chips` (existing mobile media query)
  - `.hp-popular-list` / `.hp-quick-filter-links` (existing mobile media query)
  - `.dir-filter-chips` (added new mobile max-width:760px block with flex: 0 0 auto on chips)
- Added `export const viewport` to layout.jsx with `viewportFit: cover`, `themeColor`, `maximumScale`
- Added `appleWebApp` and `mobile-web-app-capable` to metadata in layout.jsx
- Build: `npm run build` passed clean — no errors

Files Changed:
  - apps/Traffic/src/app/globals.css
  - apps/Traffic/src/app/layout.jsx

SENTRY: Not Applied

NOTE OF COMPLETITION
