# Planning — May / 09 / Sequence 13

> Active task: Wire upload screen i18n — Phase 2 Batch 3

## Execution Plan

Task: Wire useTranslation() into UploadCard + CaptionCard visible strings only
Application Scope: VCSM
Task Class: IMPLEMENTATION
Estimated Time: 10–14 minutes
Execution Type: Split

Slices:
- S1: Create apps/VCSM/src/i18n/en/upload.json + es/upload.json + update setup.js
- S2: Wire UploadCard.jsx (addPhotosVideos, addPhotoVideo, tapToUpload, selectedCount)
- S3: Wire CaptionCard.jsx (captionPlaceholder, addLocation placeholders only)
- S4: Update Logan Phase 2 doc

Scope note: user confirmed only the visible card strings need translation.
  Excluded: tab labels (VIBE/24DROP/VDROP), ActorPill, spread button, location aria-labels,
  searching text, permission prompt, error messages.

Upload namespace keys:
  upload.addPhotosVideos ("Add photos/videos (up to {{max}})")
  upload.addPhotoVideo ("Add photo/video")
  upload.tapToUpload ("Tap to upload or drag and drop")
  upload.selectedCount ("Selected: {{count}} / {{max}}")
  upload.captionPlaceholder ("Write a caption... (use @ to tag)")
  upload.addLocation ("Add location")

SENTRY Review: Not Needed
Relevant Logan Docs: vcsm.i18n.phase2-string-wiring.md
Documentation Mode: UPDATE existing Phase 2 doc (add Batch 3 section)

## Execution Summary

What was completed:

S1 — upload.json created (en + es), setup.js updated
- 6 keys: addPhotosVideos, addPhotoVideo, tapToUpload, selectedCount, captionPlaceholder, addLocation
- Both en/ and es/ created with EN placeholders
- setup.js: uploadEn/uploadEs imports + added to both dictionaries as `upload` namespace

S2 — UploadCard.jsx wired
- import useTranslation from @i18n added
- addPhotosVideos: t('upload.addPhotosVideos', { max: MAX_VIBES_PHOTOS })
- addPhotoVideo: t('upload.addPhotoVideo')
- tapToUpload: t('upload.tapToUpload')
- selectedCount: t('upload.selectedCount', { count: selectedCount, max: MAX_VIBES_PHOTOS })
  (replaced raw "Selected: {n} / {max}" interpolation)

S3 — CaptionCard.jsx wired
- import useTranslation from @i18n added
- const { t } = useTranslation() inside component
- caption textarea placeholder: t('upload.captionPlaceholder')
- location input placeholder: t('upload.addLocation')

S4 — Logan Phase 2 doc updated
- Status: Batch 3 complete
- Section 3C added covering all Batch 3 changes
- Section 6 keys reference updated (settings.* and upload.* added)

Why done this way:
- selectedCount uses interpolation to preserve the styled <span> on the count number — raw JSX kept as t() result only
- MAX_VIBES_PHOTOS passed as {{max}} param to avoid hardcoding the number in the JSON value; stays in sync with the constant
- Excluded strings (tab labels, spread button, location aria-labels, permission prompt) per product owner instruction

Files changed:
  CREATE: apps/VCSM/src/i18n/en/upload.json
  CREATE: apps/VCSM/src/i18n/es/upload.json
  EDIT: apps/VCSM/src/i18n/setup.js
  EDIT: apps/VCSM/src/features/upload/ui/UploadCard.jsx
  EDIT: apps/VCSM/src/features/upload/ui/CaptionCard.jsx
  EDIT: zNOTFORPRODUCTION/_CANONICAL/logan/vcsm/i18n/vcsm.i18n.phase2-string-wiring.md

Logan Docs Reviewed: vcsm.i18n.phase2-string-wiring.md
Logan Docs Updated: vcsm.i18n.phase2-string-wiring.md (Batch 3 section added)
Documentation Drift Status: None

SENTRY Status: DEFERRED — string replacement + namespace wiring only; no layer boundary risk

NOTE OF COMPLETITION

