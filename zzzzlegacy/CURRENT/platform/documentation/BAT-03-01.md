# Batman Planning — May / 03 / Sequence 01

Generated by: Batman
Scope: VCSM
Task Class: IMPLEMENTATION

## Task

Fix Cloudflare Pages _redirects infinite-loop warnings in VCSM

## Execution Plan

BATMAN PLAN
Task: Fix Cloudflare Pages _redirects infinite-loop warnings in VCSM
Scope: VCSM
Task Class: IMPLEMENTATION
Execution Type: Direct
Slices: N/A
Files Expected To Change:
  - apps/VCSM/public/_redirects
Specialist Commands Required: None
Batman Docs Expected: None
Assumptions:
  - Cloudflare Pages Functions take priority over _redirects static rules automatically
  - A single /* /index.html 200 fallback is safe and will not override active Functions
  - /me is a simple exact-match route and can be removed if /* covers it
Blocking Risks:
  - If Cloudflare Functions are NOT set up for /how-to/*, /p/*, /vport/* etc.,
    removing per-route rewrites without a catch-all could break those routes.
    Mitigated: the fix ADDS /* /index.html 200 as the universal SPA fallback.

## Execution Summary

What was completed:
- Removed all per-route SPA rewrites that caused Cloudflare infinite-loop warnings:
  /profile/*, /us/*, /p/*, /u/*, /me, /how-to/*, /about, /contact, /legal/*, /vport/*
- Replaced with single safe universal fallback: /* /index.html 200
- Added explanatory comment clarifying Cloudflare Functions take priority automatically

Why done this way:
- Cloudflare Pages evaluates static assets and Functions routes before _redirects rules.
  The per-route rewrites were redundant and caused Cloudflare to detect a potential
  infinite loop (e.g., /how-to/* → /index.html 200, and /index.html could match /how-to/*).
  A single /* fallback is the canonical Cloudflare Pages SPA pattern and avoids this entirely.

Files Changed:
  - apps/VCSM/public/_redirects

BATMAN TASK COMPLETE
