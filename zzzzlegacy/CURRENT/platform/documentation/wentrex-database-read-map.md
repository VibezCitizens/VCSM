# Wentrex Database Read Map

Generated by: Batman
BAT Reference: BAT-13-03
Date: April 13, 2026

---

## Audit Summary

| Metric | Result |
|--------|--------|
| DAL Files Scanned | 80 |
| Database Schema | learning.* (single schema) |
| Tables | 27 |
| N+1 Patterns | 1 (minor: listCoursesByActorId .map() flatten) |
| select('*') Overfetch | ZERO |
| Client-Side Caching | NONE implemented |
| RPC Functions | 1 (provision_wentrex_identity) |
| Tenant Filter Gaps | 2 flagged |

---

## DAL Distribution

| Location | Files |
|----------|-------|
| learning/administration/dal/ | 47 files |
| learning/student/dal/ | 9 files |
| learning/staff/dal/ | 6 files |
| features/auth/dal/ | 3 files |
| features/actors/dal/ | 1 file |
| features/identity/dal/ | 2 files |

---

## Column Selection

**Result: ZERO .select('*') violations**

All DAL files define named column constants:
- `COURSE_COLUMNS`, `SUBMISSION_COLUMNS`, `GRADE_COLUMNS`, `LESSON_PROGRESS_COLUMNS`, etc.
- Example: `LESSON_PROGRESS_COLUMNS = "id, lesson_id, actor_id, state, completed_at, updated_at"`

---

## Tenant Scoping Analysis

### Realm-Scoped Queries (COMPLIANT)

| Function | Filter |
|----------|--------|
| listCoursesByRealmId.dal.js | .eq("realm_id", realmId) |
| listOrganizationsByRealmId.dal.js | .eq("realm_id", realmId) |
| getLearningRealmById.dal.js | .eq("id", realmId) |

### Organization-Scoped Queries (COMPLIANT)

| Function | Filter |
|----------|--------|
| listCoursesByOrganizationId.dal.js | .eq("organization_id", organizationId) |
| listTermsByOrganizationId.dal.js | .eq("organization_id", organizationId) |
| listActiveOrgMembershipsByActorRoles.dal.js | actor + org context |

### FLAGGED — Lesson Progress Missing organization_id Filter (HIGH)

| Function | Filter | Gap |
|----------|--------|-----|
| listLessonProgressByCourseAndActor.dal.js | actor_id + course_id | No explicit organization_id |
| getLessonProgressByLessonAndActor.dal.js | lesson_id + actor_id | No explicit organization_id |

These rely on FK chain (lesson → course → organization) and RLS policies.
Risk: if RLS on lesson_progress doesn't enforce organization_id, cross-org leakage is possible.
Action: Verify RLS policies on learning.lesson_progress table.

### FLAGGED — Public Profiles Unscoped (MEDIUM)

| Function | Schema | Gap |
|----------|--------|-----|
| searchProfilesByField.dal.js | public (no .schema()) | No tenant filter |
| listProfilesByIds.dal.js | public (no .schema()) | No tenant filter |

These query `public.profiles` without realm or organization filter.
Contrast: `getActorSummariesByIds.dal.js` correctly uses `learning.actor_profiles` with schema prefix.
Action: Migrate to `learning.actor_profiles` or add org_id filter.

---

## Core DAL Operations

### Student DAL

| Function | Table(s) | Write? |
|----------|----------|--------|
| getLessonProgressByLessonAndActor | lesson_progress | NO |
| listLessonProgressByCourseAndActor | lesson_progress | NO |
| upsertLessonProgress | lesson_progress | YES |
| getSubmissionById | submissions | NO |
| getSubmissionAttempt | submissions | NO |
| listSubmissionsByAssignmentId | submissions | NO |
| upsertSubmissionDraft | submissions | YES |
| submitAssignmentAttempt | submissions | YES |

### Staff DAL

| Function | Table(s) | Write? |
|----------|----------|--------|
| upsertGrade | grades | YES |
| listGradesByAssignmentId | grades | NO |
| getGradeBySubmissionId | grades | NO |
| listAssignmentRubricsByAssignmentId | assignment_rubrics | NO |
| listSubmissionFilesBySubmissionId | submission_files | NO |

### Administration DAL (key reads)

| Function | Table(s) | Scope |
|----------|----------|-------|
| getCourseById | courses | courseId (realm check in controller) |
| listCoursesByRealmId | courses | realmId explicit |
| listCoursesByOrganizationId | courses | organizationId explicit |
| listCourseMembershipsByCourseId | course_memberships | courseId |
| listActiveActorsByActorIds | actors | actorIds .in() batch |
| listOrganizationsByRealmId | organizations | realmId explicit |
| listVisibleOrganizationsDal | organizations | NONE — global (security issue) |
| listVisibleRealmsDal | realms | NONE — global (security issue) |
| searchProfilesByField | public.profiles | NONE — global (security issue) |

---

## N+1 Analysis

**1 minor instance found:**
- `listCoursesByActorId.dal.js` uses `.map()` to flatten nested join result
- Not a true N+1 (no per-item DB call) — array transform only
- Low risk

All other list operations use `.in()` for batch fetching.

---

## Caching Assessment

**Result: NO client-side caching implemented**

All 80 DAL files hit the database on every call. No TTL cache, no memoization, no React Query cache configured.

Recommended caching targets:

| DAL | Suggested TTL |
|-----|---------------|
| getCourseById | 5 min |
| listCoursesByOrganizationId | 5 min |
| actor profiles | 15 min |
| org settings | 10 min |
| submission status | 2 min |

---

## RPC Functions

| RPC | Schema | Purpose |
|-----|--------|---------|
| platform.provision_wentrex_identity() | platform | Identity provisioning during signup |

---

## Ordering Conventions

| Data Type | Order |
|-----------|-------|
| Courses | created_at DESC |
| Lessons/Modules | sort_order ASC, created_at ASC |
| Memberships | created_at ASC |
| Submissions | created_at DESC |
