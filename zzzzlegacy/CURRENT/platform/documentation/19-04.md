# Planning — April / 19 / Sequence 04

**Task:** QR Reviews Access for VPORT Owners
**Scope:** VCSM
**Task Class:** IMPLEMENTATION

---

## Execution Plan

Add a `reviews_qr` card to the VPORT dashboard. Clicking it navigates to a QR view that displays a scannable code pointing to `/profile/:slug/reviews`. Build the public reviews page at that route, reusing all existing public review infrastructure.

**Files:**
- NEW `apps/VCSM/src/features/public/vportMenu/screen/VportPublicReviewsBySlugScreen.jsx`
- NEW `apps/VCSM/src/features/public/vportMenu/view/VportPublicReviewsView.jsx`
- NEW `apps/VCSM/src/features/public/vportMenu/view/VportPublicReviewsQrView.jsx`
- EDIT `apps/VCSM/src/app/routes/public/vportMenu.routes.jsx`
- EDIT `apps/VCSM/src/app/routes/index.jsx`
- EDIT `apps/VCSM/src/features/dashboard/vport/screens/model/buildDashboardCards.js`
- EDIT `apps/VCSM/src/features/dashboard/vport/screens/model/dashboardViewByVportType.model.js`
- EDIT `apps/VCSM/src/features/dashboard/vport/screens/VportDashboardScreen.jsx`

---

## Execution Summary

**What was built:**

### New files
- `apps/VCSM/src/features/public/vportMenu/screen/VportPublicReviewsBySlugScreen.jsx` — resolves slug via `resolveActorBySlugOrUsernameDAL`, renders `VportPublicReviewsView`. Same pattern as menu slug screen.
- `apps/VCSM/src/features/public/vportMenu/screen/VportPublicReviewsQrBySlugScreen.jsx` — same resolution, renders `VportPublicReviewsQrView`.
- `apps/VCSM/src/features/public/vportMenu/view/VportPublicReviewsView.jsx` — standalone public reviews page: vport avatar + name header (via `useVportPublicDetails`), aggregate rating headline (via `useVportPublicReviews`), full `VportPublicReviewsPanel`. Dark radial gradient background, mobile-first, maxWidth 600.
- `apps/VCSM/src/features/public/vportMenu/view/VportPublicReviewsQrView.jsx` — QR view for owners. Title "Scan to View Reviews". URL `origin/profile/{canonicalSlug}/reviews`. Copy link button. Same visual treatment as menu QR view.

### Edited files
- `buildDashboardCards.js` — added `reviews_qr` to `CARD_CATALOG` (`handlerKey: "openReviewsQr"`)
- `dashboardViewByVportType.model.js` — added `"reviews_qr"` after `"reviews"` in all 7 presets
- `VportDashboardScreen.jsx` — added `openReviewsQr` callback (slug-first → `/profile/:slug/reviews/qr`)
- `vportMenu.routes.jsx` — added `/profile/:slug/reviews` and `/profile/:slug/reviews/qr` routes
- `apps/VCSM/src/app/routes/index.jsx` — lazy imports for both new screens, passed into route factory

**Public URL:** `/profile/{slug}/reviews`
**Owner QR URL:** `/profile/{slug}/reviews/qr`
**Slug resolution:** reuses `resolveActorBySlugOrUsernameDAL` — works for any vport, not gated on menu data
**Auth:** none required — all review data served from `reviews.public_vport_reviews_v`

**Regression risk:** Low — additive routes only; no existing logic changed

NOTE OF COMPLETITION
