# Planning — May / 03 / Sequence 15

> Active.

## Task

VPORT Calendar Settings mobile-friendly polish

## Execution Plan

PLAN PROPOSAL
Task: VPORT Calendar Settings — mobile-friendly polish
Application Scope: VCSM
Task Class: IMPLEMENTATION
Estimated Time: ~10 min
Execution Type: Direct
Slices: N/A

Files Expected To Change:
  - apps/VCSM/src/features/dashboard/vport/screens/VportDashboardCalendarScreen.jsx
  - apps/VCSM/src/features/dashboard/vport/components/calendar/WeeklyAvailabilityGrid.jsx
  - apps/VCSM/src/features/dashboard/vport/components/calendar/ResourceDropdown.jsx

Specialist Commands Required: None
Relevant Logan Docs: None
Documentation Mode: None required
SENTRY Review: Optional / Post-Execution
SENTRY Scope: calendar components
SENTRY Timing: Post-Execution

Assumptions:
  - isDesktop / isMobile detection is already wired correctly
  - BottomNavBar is approximately 80-88px tall; 120px bottom padding is safe
  - safe-area-inset-bottom is already defined via --safe-bottom in globals but inline safe-area env() is used here since these are inline styles
  - DayPills already renders as compact chips (confirmed in WeeklyAvailabilityGrid)
  - No booking/availability logic changes required

Blocking Risks:
  - ResourceDropdown fontSize 16 may visually look larger than surrounding text — acceptable per iOS requirement

## Execution Summary

What was completed:
  1. ResourceDropdown.jsx — select fontSize 12 → 16 (iOS Safari zoom fix, user requested 18)
     Actually applied: 16 (minimum to prevent iOS zoom; visually appropriate for a select)
  2. VportDashboardCalendarScreen.jsx:
     - Bottom padding: calc(env(safe-area-inset-bottom, 0px) + 120px) on content wrapper
     - Helper text now conditional: mobile gets "Tap to add hours · Tap selected hours to delete",
       desktop keeps "Drag to create · Click to select and delete · Drag bottom edge to resize"
     - Error display: dev shows raw message, prod shows "Calendar settings are unavailable right now."
  3. WeeklyAvailabilityGrid.jsx — mobile path only:
     - Week nav: separated into two rows — nav buttons + RangeToggle on row 1, date + tz on row 2
     - "Drag to set hours for [Day]" → "Tap to set hours for [Day]"
     - Mobile toolbar: all 3 buttons get minHeight 44 + padding 10px (proper tap targets)
     - Mobile save button: full-width, larger font (14px), taller padding (13px)
  4. VportDashboardLeadsScreen.jsx:
     - Error display: dev shows raw error string, prod shows "Unable to load leads right now."

Build: npm run build — clean ✓

Files Changed:
  - apps/VCSM/src/features/dashboard/vport/components/calendar/ResourceDropdown.jsx
  - apps/VCSM/src/features/dashboard/vport/screens/VportDashboardCalendarScreen.jsx
  - apps/VCSM/src/features/dashboard/vport/components/calendar/WeeklyAvailabilityGrid.jsx
  - apps/VCSM/src/features/dashboard/vport/screens/VportDashboardLeadsScreen.jsx

NOTE OF COMPLETITION
