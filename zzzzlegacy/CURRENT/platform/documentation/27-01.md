# Planning — May / 27 / Sequence 01

> Task: Fix P0 architecture violations — VPORT Restaurant Dashboard Menu QR module

## Execution Plan

Task Class: IMPLEMENTATION
Scope: VCSM
Files changed: 4
SENTRY: Required — Post-Execution

---

## Execution Summary

### What was completed

**Fix 1 — `lib/qrUrlBuilders.js`**
Added `buildMenuShortDisplayUrl(slug)` — a new origin-safe display URL builder for
print flyers. Uses `window.location.hostname + port` instead of any hardcoded domain.
Path matches the canonical public menu route: `/profile/:slug/menu`.
No React. No side effects. Pure lib layer.

**Fix 2 — `features/dashboard/flyerBuilder/screens/VportActorMenuFlyerView.jsx`**
Removed local `buildShortUrl()` function which hardcoded `vibezcitizens.com/menu/${slug}`.
Imported `buildMenuShortDisplayUrl` from `@/lib/qrUrlBuilders` via `@/` alias.
Replaced the `shortUrl` useMemo with origin-safe call + protocol-strip fallback for
the unresolved-slug case. Also fixed the old path (`/menu/:slug` → `/profile/:slug/menu`).

**Fix 3 — `features/public/vportMenu/view/VportPublicMenuView.jsx`**
Added `useActorCanonicalSlug(actorId)` hook via `@/features/profiles/adapters/profiles.adapter`
(cross-feature via adapter — correct boundary). Fixed `onBack` fallback to use
`/profile/${canonicalSlug}/menu` when slug is resolved. Falls back to
`/actor/${actorId}/menu` (internal redirect route) when slug is not yet available —
never exposes UUID in a `/profile/` URL path. Pattern mirrors the existing sibling
`VportPublicMenuQrView`.

**Fix 4 — `features/public/vportMenu/adapters/vportMenu.adapter.js`**
Populated from empty (1-byte) file to a fully documented public adapter surface.
Exports: 4 Views (Menu, MenuQr, Reviews, ReviewsQr) + 3 data hooks (menu, details, reviews)
+ 2 slug resolution hooks (menu slug, vport slug).
Does NOT export: DAL, Models, Controllers, or Screens (all remain internal).

### Build result
✓ 7255 modules transformed — zero errors.
Pre-existing large chunk warning on `index.esm` is unrelated to these changes.

### SENTRY Status: ALIGNED
SENTRY Reviewed Files:
  - apps/VCSM/src/lib/qrUrlBuilders.js
  - apps/VCSM/src/features/dashboard/flyerBuilder/screens/VportActorMenuFlyerView.jsx
  - apps/VCSM/src/features/public/vportMenu/view/VportPublicMenuView.jsx
  - apps/VCSM/src/features/public/vportMenu/adapters/vportMenu.adapter.js
SENTRY Follow-Up: None

### Change Intent Registry
- Entry written: NO
- Files logged: N/A — no deletions, moves, or renames occurred
- Registry path: zNOTFORPRODUCTION/_ACTIVE/change-intent/CHANGE_INTENT.md

---

NOTE OF COMPLETITION
