# Planning — April / 05 / Sequence 04

> Awaiting tasks.
You are auditing the remaining legacy VCSM inbox runtime after chat badge migration.

Project root:
- /Users/vcsm/Desktop/VCSM

Goal:
Determine whether inbox folders and inbox action writes are still legacy-backed on vc.inbox_entries, and migrate them fully to the shared chat engine over chat.inbox_entries if needed.

Focus on these files first:
- apps/VCSM/src/features/chat/inbox/dal/inbox.write.dal.js
- apps/VCSM/src/features/chat/inbox/hooks/useInboxFolder.js
- apps/VCSM/src/features/chat/inbox/hooks/useSpamConversationIds.js
- apps/VCSM/src/features/chat/inbox/realtime/subscribeToInbox.js
- apps/VCSM/src/features/chat/inbox/controllers/inboxActions.controller.js
- apps/VCSM/src/features/chat/conversation/controllers/leaveConversation.controller.js
- apps/VCSM/src/features/chat/conversation/controllers/markConversationSpam.controller.js
- apps/VCSM/src/features/chat/inbox/screens/ArchivedInboxScreen.jsx
- apps/VCSM/src/features/chat/inbox/screens/SpamInboxScreen.jsx
- apps/VCSM/src/features/chat/inbox/screens/RequestsInboxScreen.jsx

Also inspect engine equivalents:
- engines/chat/src/hooks/useInboxActions.js
- engines/chat/src/controller/inboxActions.controller.js
- engines/chat/src/controller/deleteThreadForMe.controller.js
- engines/chat/src/dal/inbox_entries.write.dal.js
- engines/chat/src/dal/subscribeToInbox.js

Tasks:
1. Identify all active runtime reads/writes against vc.inbox_entries
2. Classify each as read, write, realtime, or dead code
3. Determine whether archive/spam/requests/leave/spam-marking are still legacy
4. If still legacy, migrate them to engine/chat.*
5. Preserve UI behavior
6. Remove active runtime dependency on vc.inbox_entries for inbox folder/action flows
7. Do not touch unrelated chat flows

Return exactly:

VCSM INBOX FOLDER / ACTION LEGACY AUDIT RESULT

1. Active runtime files using vc.inbox_entries
2. Read paths still legacy
3. Write paths still legacy
4. Realtime paths still legacy
5. Files migrated
6. Files still pending
7. Whether inbox folder/action flows are now fully migrated
8. Exact blockers if not
9. Final authority summary
- inbox main load
- archived folder
- spam folder
- requests folder
- archive action
- spam action
- leave conversation
- delete thread
- realtime

## Execution Plan

PLAN PROPOSAL
Task: Audit remaining legacy inbox runtime
Application Scope: VCSM (read-only)
Estimated Time: 1 minute
Execution Type: Direct
Files Expected To Change: NONE (everything already migrated)

## Execution Summary

### Audit Result
All inbox folder/action flows are ALREADY FULLY MIGRATED. No further code changes needed.

### Evidence

**Migrated hooks (all delegate to @chat engine):**
- useInbox.js → useInbox from @chat
- useInboxActions.js → useInboxActions from @chat
- useInboxFolder.js → wraps useInbox (engine-backed)
- useInboxEntryForConversation.js → useInboxEntryForConversation from @chat
- useConversationActionsMenu.js → markConversationSpam from @chat

**Legacy files on disk with ZERO runtime imports:**
- inbox/dal/inbox.write.dal.js — only imported by legacy controllers (dead chain)
- inbox/controllers/inboxActions.controller.js — only imported by dev diagnostics
- inbox/realtime/subscribeToInbox.js — zero imports anywhere
- conversation/controllers/leaveConversation.controller.js — only imported by dev diagnostics
- conversation/controllers/markConversationSpam.controller.js — only imported by dev diagnostics
- inbox/hooks/useSpamConversationIds.js — only referenced by language audit files (not runtime)

**Folder screens all use migrated hooks:**
- ArchivedInboxScreen → useInboxFolder (engine)
- SpamInboxScreen → useInboxFolder + useInboxActions (engine)
- RequestsInboxScreen → useInboxFolder + useInboxActions (engine)

### VCSM INBOX FOLDER / ACTION LEGACY AUDIT RESULT

1. Active runtime files using vc.inbox_entries: NONE
2. Read paths still legacy: NONE
3. Write paths still legacy: NONE
4. Realtime paths still legacy: NONE
5. Files migrated: 0 (already done in previous slices)
6. Files still pending: 0
7. Whether inbox folder/action flows are now fully migrated: YES
8. Exact blockers: NONE
9. Final authority summary:
   - inbox main load: chat.* (engine useInbox)
   - archived folder: chat.* (engine useInbox via useInboxFolder)
   - spam folder: chat.* (engine useInbox via useInboxFolder)
   - requests folder: chat.* (engine useInbox via useInboxFolder)
   - archive action: chat.* (engine useInboxActions)
   - spam action: chat.* (engine markConversationSpam)
   - leave conversation: chat.* (engine useInboxActions.leave)
   - delete thread: chat.* (engine useInboxActions.deleteThreadForMe)
   - realtime: chat.* (engine subscribeToInbox inside useInbox)

NOTE OF COMPLETITION