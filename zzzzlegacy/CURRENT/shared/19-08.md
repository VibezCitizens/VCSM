# Planning — April / 19 / Sequence 08

**Task:** DB Migration Cluster — DB-1 through DB-5
**Scope:** ENGINE (Database)
**Task Class:** DB_REVIEW + IMPLEMENTATION

---

## Execution Plan

Apply 4 Supabase migrations to fix vport actor creation and hydration bugs.
All migrations are idempotent. Executed via `supabase db push --workdir apps/VCSM`.

DB-6 (delete_my_account) and DB-7 (delete_my_vport) deferred — both are TBD/design-decision items.

---

## Execution Summary

All 4 migrations applied successfully. `Remote database is up to date.`

### DB-1 + DB-2 — `20260419060000_patch_create_vport_for_actor.sql`
- **DB-1**: Patched `vport.create_vport_for_actor` to INSERT into `vport.profile_categories` (was missing — new vports had no category row, causing `identity.vportType = null` and wrong dashboard loading)
- **DB-2**: Added `PERFORM identity.refresh_actor_directory_row('vc', v_vport_actor_id)` before RETURN — new vport actors are immediately reflected in `identity.actor_directory`
- Used DO block with `pg_get_functiondef` + idempotency guard (skips if already patched)

### DB-3 — `20260419070000_fix_refresh_actor_directory_vport_source.sql`
- Patched `identity.refresh_actor_directory_row` to read vport hydration data from `vport.profiles` instead of `vc.vports` (old empty table)
- Used `replace()` — safe, exact string match, idempotent

### DB-4 — `20260419080000_add_profile_categories_insert_rls.sql`
- Added INSERT RLS policy on `vport.profile_categories`:
  ```sql
  CREATE POLICY profile_categories_insert ON vport.profile_categories
  FOR INSERT TO authenticated
  WITH CHECK (vport.actor_can_manage_profile(vc.current_actor_id(), profile_id));
  ```

### DB-5 — `20260419090000_backfill_actor_owners.sql`
- Backfill `vc.actor_owners` for pre-migration vport actors from `vport.profile_actor_access WHERE is_primary = true`
- `INSERT 0 0` — no rows needed backfilling (all existing vports already have actor_owner rows, or no pre-migration vports exist in this environment)

### Deferred
- **DB-6** (`delete_my_account`): Needs design decision — function missing, DAL rewrite or new function
- **DB-7** (`delete_my_vport`): Likely superseded by `dalDeleteOwnedVportById` — needs verification

### Files Created
- `apps/VCSM/supabase/migrations/20260419060000_patch_create_vport_for_actor.sql`
- `apps/VCSM/supabase/migrations/20260419070000_fix_refresh_actor_directory_vport_source.sql`
- `apps/VCSM/supabase/migrations/20260419080000_add_profile_categories_insert_rls.sql`
- `apps/VCSM/supabase/migrations/20260419090000_backfill_actor_owners.sql`

NOTE OF COMPLETITION
