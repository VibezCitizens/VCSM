# Planning — April / 05 / Sequence 03

> Awaiting tasks.
You are auditing the complete end-to-end **Subscribe / Unsubscribe flow pipeline** in the VCSM application.

Project root:
- /Users/vcsm/Desktop/VCSM

Application:
- /Users/vcsm/Desktop/VCSM/apps/VCSM

Goal:
Perform a full end-to-end diagnostic review of the Subscribe / Unsubscribe flow for actors in VCSM.

This includes:
- subscribing / following a public actor
- unsubscribing / unfollowing an actor
- requesting access to a private actor
- cancelling or reversing follow state
- UI state updates
- profile gate interaction
- follower/subscriber count effects
- notification side effects if any

Do NOT modify code yet.
Do NOT create new code yet.
Only inspect, trace, analyze, and report the pipeline.

I want the full pipeline from:
UI button
→ hook
→ controller
→ DAL / RPC
→ database
→ count/state refresh
→ profile gate effects
→ notifications if any
→ UI re-render

==================================================
STEP 1 — FIND THE ENTRY POINTS
==================================================

Search inside:

- apps/VCSM/src/features/social/**
- apps/VCSM/src/features/profiles/**
- apps/VCSM/src/features/settings/**
- apps/VCSM/src/features/notifications/**
- apps/VCSM/src/shared/**

Find all UI entry points for subscribe / unsubscribe behavior.

Likely names to inspect:
- SubscribeButton
- FollowButton
- useFollowActorToggle
- useSubscribeAction
- useUnsubscribeAction
- ctrlSubscribe
- ctrlUnsubscribe
- follow.controller
- actorFollows.dal
- followRequests.dal
- useProfileGate
- useFollowStatus

Identify:
- where the button lives
- what component renders it
- what hook runs on click
- whether public vs private actors are handled differently

==================================================
STEP 2 — TRACE THE PIPELINE
==================================================

Build the exact path for:

A. Subscribe to public actor
B. Subscribe/request private actor
C. Unsubscribe / unfollow
D. Cancel pending request if supported

For every path, map:

UI component
→ hook
→ controller
→ DAL / RPC
→ database write
→ refresh / state update
→ UI re-render

For every layer list:
- file path
- exported function
- input
- output

==================================================
STEP 3 — IDENTIFY DATABASE AUTHORITY
==================================================

Determine which tables are authoritative for the subscribe system.

Likely candidates:
- vc.actor_follows
- vc.social_follow_requests
- vc.actor_privacy_settings
- vc.friend_ranks
- vc.notifications

For each flow, identify exactly which tables are:
- read
- inserted
- updated
- soft-deactivated
- deleted

Also identify whether follow state is represented by:
- active row
- inactive row
- upsert
- delete
- status enum

==================================================
STEP 4 — VERIFY PUBLIC VS PRIVATE ACTOR LOGIC
==================================================

Determine how the system behaves for:

1. Public actor
Expected:
- direct subscribe/follow
- row becomes active immediately

2. Private actor
Expected:
- follow request row
- access remains gated until accepted

Verify the actual behavior in code.

Inspect how privacy is read:
- vc.actor_privacy_settings
- profile gate logic
- follow status logic

Check whether the subscribe controller branches correctly.

==================================================
STEP 5 — VERIFY FOLLOW STATUS RESOLUTION
==================================================

Find how current follow/subscription state is computed in the UI.

Examples:
- subscribed
- not subscribed
- pending request
- blocked
- self profile
- private but approved

Inspect:
- useFollowStatus
- useProfileGate
- any actor status selectors

Verify:
- how the screen knows whether to render Subscribe / Unsubscribe / Requested
- whether the state is read from actor_follows or follow_requests or both
- whether there are stale state risks after mutation

==================================================
STEP 6 — VERIFY COUNTS / SIDE EFFECTS
==================================================

Check whether subscribe/unsubscribe changes:
- follower count
- following count
- subscriber list
- profile visibility
- feed visibility
- notifications
- friend ranks cleanup
- chat or access gating

Inspect whether follower counts are:
- stored on public.profiles
- derived dynamically
- updated by trigger
- updated in client code

If counts are expected but not directly visible in code, label clearly as inferred.

==================================================
STEP 7 — VERIFY NOTIFICATION SIDE EFFECTS
==================================================

Determine whether subscribing or follow requests generate notifications.

Inspect:
- vc.notifications writes
- triggers
- RPCs
- notification controllers
- any server-side side effects

Check for:
- new follower notification
- follow request notification
- request accepted notification
- request cancelled notification

If notification generation is not visible in app code, say whether it appears server-side or inferred only.

==================================================
STEP 8 — VERIFY BLOCK / PRIVACY INTERACTION
==================================================

Check interaction with:
- vc.user_blocks
- vc.actor_privacy_settings
- profile gating logic

Questions to answer:
- can you subscribe to someone you blocked?
- can you subscribe to someone who blocked you?
- does unsubscribe change private-profile access immediately?
- does pending request bypass privacy gate?
- does block cleanup remove follow rows?

==================================================
STEP 9 — VERIFY MUTATION SAFETY
==================================================

For subscribe/unsubscribe flows, analyze:

- is the write atomic?
- is it a single insert/update?
- is there partial-write risk?
- is there rollback?
- is it idempotent?
- are duplicate rows prevented?
- is silent failure possible?
- does optimistic UI exist?

Pay attention to:
- upserts
- inactive follow rows vs delete
- pending request duplication
- swallowed errors
- stale local state

==================================================
STEP 10 — VERIFY NAVIGATION / UI REFRESH
==================================================

After subscribe/unsubscribe, determine how the UI refreshes.

Check:
- local state patch
- refetch
- query invalidation
- re-run useProfileGate
- re-run useFollowStatus
- manual refresh event
- no refresh at all

Verify that the button, counts, and profile gate all stay consistent after mutation.

==================================================
STEP 11 — BUILD THE FULL PIPELINE MAP
==================================================

Produce a clear pipeline map for:

A. Subscribe public actor
B. Subscribe private actor (request)
C. Unsubscribe
D. Pending request state

Format like:

Subscribe Pipeline
UI
→ Hook
→ Controller
→ DAL
→ Tables
→ State refresh
→ UI result

==================================================
STEP 12 — FIND BUGS
==================================================

Report issues in these categories:

1. Architecture Issues
2. Privacy / gating issues
3. Follow state mismatch issues
4. Duplicate row / idempotency issues
5. Notification side-effect issues
6. Count consistency issues
7. Block interaction issues
8. UI refresh / stale state issues
9. Silent failure issues

Be specific and grounded in the actual code.

==================================================
OUTPUT FORMAT
==================================================

Return exactly:

VCSM SUBSCRIBE / UNSUBSCRIBE PIPELINE REVIEW

1. UI entry points
2. Hook chain
3. Controller chain
4. DAL / RPC layer
5. Database tables involved
6. Public actor subscribe flow
7. Private actor subscribe-request flow
8. Unsubscribe flow
9. Follow status resolution logic
10. Privacy / block interaction
11. Notification side effects
12. Count / profile gate effects
13. Mutation safety review
14. Bugs discovered
15. Recommended fixes
16. Full pipeline map

## Execution Plan

PLAN PROPOSAL
Task: Audit the complete Subscribe/Unsubscribe flow pipeline in VCSM
Application Scope: VCSM (read-only inspection)
Estimated Time: 3 minutes
Execution Type: Direct
Files Expected To Change: NONE (read-only audit, output to logan/)

## Execution Summary

### What was completed
Full end-to-end audit of the Subscribe/Unsubscribe pipeline written to:
logan/VCSM Subscribe Pipeline

### Key findings
- 4 flows traced: subscribe public, subscribe private (request), accept, unsubscribe
- All notifications generated by DB triggers (not app code)
- 6 bugs found (1 medium, 2 low, 1 info, 2 none)
- Medium bug: no block check in subscribe controller

### Files changed: NONE
### Files created: logan/VCSM Subscribe Pipeline

NOTE OF COMPLETITION