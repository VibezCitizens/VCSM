# Planning — April / 09 / Sequence 08

You are a senior full-stack engineer working inside:

/Users/vcsm/Desktop/VCSM/apps/VCSM

Use the following documents as the source of truth:

1. logan/vport-review-pipeline-audit.md
2. logan/vport-review-implementation-plan.md
3. logan/vport-review-focused-plan.md

TASK
Implement the Vport Review improvements described in those documents.

SCOPE
Work only on the Vport review pipeline under:

apps/VCSM/src/features/profiles/kinds/vport/

PRIORITY ORDER
Implement in this exact order:

1. Add explicit citizen-only submit guard in controller
2. Add explicit edit review UI
3. Add pagination to review list
4. Add explicit delete review UI

RULES
- Do not redesign the architecture
- Keep the existing DAL -> Model -> Controller -> Hook -> Screen structure
- Reuse existing controllers/DAL where possible
- Do not remove working RLS assumptions; add defense in depth in controller
- Keep changes focused and incremental
- Do not guess new schema unless absolutely required
- Preserve current behavior unless the plan explicitly says to change it

REQUIRED IMPLEMENTATION DETAILS

PHASE 1 — CITIZEN-ONLY CONTROLLER GUARD
Update:
- `features/profiles/kinds/vport/controller/review/VportReviews.controller.js`

In `ctrlSubmitReview`:
- explicitly validate that `authorActorId` belongs to a `user` actor
- reject non-user actors with:
  `Only citizens can submit reviews.`

Use existing DAL if possible:
- `dalReadReviewTargetActor`

Do not rely only on hook/UI guard or RLS.

PHASE 2 — EDIT REVIEW UI
Update:
- `hooks/review/useVportReviews.js`
- `screens/review/VportReviewsView.jsx`
- `screens/review/components/ReviewsList.jsx`

Requirements:
- add `isEditing`
- add `startEdit()`
- add `cancelEdit()`
- pre-fill body and ratings from `myReview`
- show explicit "Edit my review" action
- on submit, continue using existing idempotent upsert flow
- support edit action from the user's own review card

PHASE 3 — PAGINATION
Update:
- `dal/review/vportReviews.read.dal.js`
- `controller/review/VportReviews.controller.js`
- `hooks/review/useVportReviews.js`
- `screens/review/VportReviewsView.jsx`
- `screens/review/components/ReviewsList.jsx`

Requirements:
- add cursor-based pagination using `created_at`
- DAL should fetch `limit + 1`
- return `hasMore`
- controller returns `{ reviews, hasMore, nextCursor }`
- hook supports `loadMore()`
- UI shows "Load more reviews" when applicable

PHASE 4 — DELETE REVIEW UI
Update:
- `hooks/review/useVportReviews.js`
- `screens/review/VportReviewsView.jsx`
- `screens/review/components/ReviewsList.jsx`

Requirements:
- wire existing `ctrlDeleteMyReview`
- add explicit "Delete my review" action
- add confirmation prompt
- soft delete only
- reload `myReview`, active list, and stats after delete

OUTPUT FORMAT
Return:

1. Summary of files changed
2. Exact code for every changed file
3. Short explanation per file
4. Any assumptions or blockers
5. Final verification checklist

IMPORTANT
When updating code, always provide full updated file contents, not partial snippets.
Do not omit imports.
Do not summarize code without providing the real code.

## Execution Plan

PLAN PROPOSAL
Task: Implement Vport Review improvements — 4 phases
Application Scope: VCSM
Estimated Time: ~16 minutes — split into 4 slices
Execution Type: Split

Slice 1 (~3 min) — Phase 1: Citizen-only controller guard
Slice 2 (~4 min) — Phase 2: Edit review UI
Slice 3 (~5 min) — Phase 3: Pagination
Slice 4 (~4 min) — Phase 4: Delete review UI

Files Expected To Change:
- controller/review/VportReviews.controller.js
- dal/review/vportReviews.read.dal.js
- hooks/review/useVportReviews.js
- screens/review/VportReviewsView.jsx
- screens/review/components/ReviewsList.jsx

## Execution Summary (Slices 1-2)

### Slice 1 — Citizen-only controller guard (DONE)
Added explicit `authorActor.kind !== "user"` check in `ctrlSubmitReview` after the self-review guard. Reuses existing `dalReadReviewTargetActor` to fetch the author actor and reject non-citizen actors with "Only citizens can submit reviews."

File changed: `controller/review/VportReviews.controller.js`

### Slice 2 — Edit review UI (DONE)
**Hook changes** (`useVportReviews.js`):
- Added `myReview` state (full review object, not just boolean)
- Added `isEditing`, `startEdit()`, `cancelEdit()`
- `loadMyReview` now stores full review and skips overwriting form state when editing
- Exports `myReview`, `isEditing`, `startEdit`, `cancelEdit`, `authorActorId`

**Screen changes** (`VportReviewsView.jsx`):
- When `myExists && !isEditing`: shows "You have already reviewed" + "Edit my review" button
- When editing: shows pre-filled compose form with "Update review" + "Cancel" buttons
- Pre-fills ratings from `myReview.ratings` via useEffect when entering edit mode
- Exits edit mode after successful submit

**ReviewsList changes** (`ReviewsList.jsx`):
- Accepts `viewerActorId`, `onEdit`, `onDelete` props
- Shows "Edit" button on the viewer's own review card (matches `authorActorId === viewerActorId`)
- Shows "Delete" button slot (wired in Slice 4)

### Remaining
- Slice 3: Pagination (DAL + controller + hook + UI)
- Slice 4: Delete review UI (hook + UI + confirmation)

### Files changed so far
- features/profiles/kinds/vport/controller/review/VportReviews.controller.js
- features/profiles/kinds/vport/hooks/review/useVportReviews.js
- features/profiles/kinds/vport/screens/review/VportReviewsView.jsx
- features/profiles/kinds/vport/screens/review/components/ReviewsList.jsx

## Execution Summary (Slices 3-4)

### Slice 3 — Pagination (DONE)
**DAL** (`vportReviews.read.dal.js`): Changed `dalListVportReviews` signature from `(targetActorId, limit)` to `(targetActorId, { limit, cursor })`. Fetches `limit + 1` rows to detect `hasMore`. Returns `{ rows, hasMore }`. When `cursor` is provided, adds `.lt("created_at", cursor)`.

**Controller** (`VportReviews.controller.js`): Changed `ctrlListReviews` to accept `{ limit, cursor }` and return `{ reviews, hasMore, nextCursor }`.

**Hook** (`useVportReviews.js`): Added `hasMore`, `nextCursor`, `loadingMore` state. Added `loadMore()` callback that fetches next page and appends to `activeList`. Updated `loadActiveList` to parse the new `{ reviews, hasMore, nextCursor }` return shape.

**UI** (`ReviewsList.jsx`): Accepts `hasMore`, `loadingMore`, `onLoadMore` props. Renders "Load more reviews" button at end of list when more reviews exist.

### Slice 4 — Delete review UI (DONE)
**Hook** (`useVportReviews.js`): Added `isDeleting` state, `deleteMyReview()` callback that calls `ctrlDeleteMyReview(myReview.id, authorActorId)`, clears form state, and reloads list + stats. Imported `ctrlDeleteMyReview` from controller.

**Screen** (`VportReviewsView.jsx`): Added `showDeleteConfirm` state. "Delete" button in ReviewsList triggers confirmation modal. Modal shows "Delete your review?" with Cancel + Delete buttons. Delete action calls `r.deleteMyReview()` and closes modal.

**ReviewsList** (`ReviewsList.jsx`): Already had `onDelete` prop from Slice 2. Now wired to trigger the confirmation modal.

### All files changed
- features/profiles/kinds/vport/controller/review/VportReviews.controller.js (citizen guard + pagination)
- features/profiles/kinds/vport/dal/review/vportReviews.read.dal.js (cursor pagination)
- features/profiles/kinds/vport/hooks/review/useVportReviews.js (edit + pagination + delete)
- features/profiles/kinds/vport/screens/review/VportReviewsView.jsx (edit + pagination + delete)
- features/profiles/kinds/vport/screens/review/components/ReviewsList.jsx (edit + pagination + delete)

NOTE OF COMPLETITION

PLAN PROPOSAL (Slice 3 continuation)
Task: Cursor-based pagination for review list
Application Scope: VCSM
Estimated Time: ~4 minutes
Execution Type: Direct
Files: DAL, controller, hook, screen, ReviewsList
