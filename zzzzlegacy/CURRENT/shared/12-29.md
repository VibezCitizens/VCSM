# Planning — April / 12 / Sequence 29

> Task: AUDIT REMAINING NOTIFICATION WRITE DALS AFTER create_event RPC CUTOVER

---

## Execution Plan

**Task:** Convert all remaining browser-direct notification writes to SECURITY DEFINER RPCs
**Application Scope:** ENGINE
**Task Class:** IMPLEMENTATION
**Execution Type:** Split — Slice 1 (SQL, deployed by user) + Slice 2 (DAL updates, executed)

---

## Execution Summary

**What was completed:**

Slice 1 — DB (deployed by user): 7 SECURITY DEFINER RPCs + RLS UPDATE policies for inbox_items

Slice 2 — Engine DAL updates:

| DAL Function | File | Change |
|---|---|---|
| `dalInsertRecipients` | `recipients.write.dal.js` | `.from('recipients').insert()` → `.rpc('insert_recipients')` |
| `dalUpdateRecipientStatus` | `recipients.write.dal.js` | `.from('recipients').update()` → `.rpc('update_recipient_status')` |
| `dalUpsertRendered` | `rendered.write.dal.js` | `.from('rendered').upsert()` → `.rpc('upsert_rendered')` |
| `dalInsertInboxItem` | `inbox.write.dal.js` | `.from('inbox_items').insert()` → `.rpc('insert_inbox_item')` |
| `dalInsertDeliveryAttempt` | `deliveryAttempts.write.dal.js` | `.from('delivery_attempts').insert()` → `.rpc('insert_delivery_attempt')` |
| `dalUpdateDeliveryAttempt` | `deliveryAttempts.write.dal.js` | `.from('delivery_attempts').update()` → `.rpc('update_delivery_attempt')` |

Inbox mutations unchanged (dalMarkInboxSeen, dalMarkInboxRead, dalDismissInboxItem, dalArchiveInboxItem) — covered by deployed RLS UPDATE policies.

**Why it was done this way:**
- All publish-pipeline writes are now server-owned via SECURITY DEFINER
- No RLS INSERT policies added to any notification.* table
- Actor-owned inbox mutations (user-facing) handled by scoped RLS UPDATE policies
- Return shapes are unchanged — callers consume identical data

**Files changed:**
- `engines/notifications/src/dal/recipients.write.dal.js`
- `engines/notifications/src/dal/rendered.write.dal.js`
- `engines/notifications/src/dal/inbox.write.dal.js`
- `engines/notifications/src/dal/deliveryAttempts.write.dal.js`

**SENTRY Reviewed Files:**
- `engines/notifications/src/dal/recipients.write.dal.js`
- `engines/notifications/src/dal/rendered.write.dal.js`
- `engines/notifications/src/dal/inbox.write.dal.js`
- `engines/notifications/src/dal/deliveryAttempts.write.dal.js`

**SENTRY Status:** ALIGNED — all changes are within DAL layer only. Transport mechanism changed (RPC vs direct table access), architecture layer boundaries preserved.

NOTE OF COMPLETITION

