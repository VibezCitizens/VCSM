# Planning — May / 27 / Sequence 04

> Task: V-002 booking QR engine fix — separate public slug resolution from authenticated dashboard list; add requestActorId authorization to listQrLinks controllers

## Execution Plan

Task Class: IMPLEMENTATION
Scope: VCSM + ENGINE
Files changed: 3 (2 edits, 1 new)
SENTRY: Required — Post-Execution

### Changes

**engines/booking/src/controller/listQrLinks.controller.js**
Add requestActorId to all three list controllers with the same assertActorCanManage* pattern as createQrLink:
  - listQrLinksByOrganization: assertActorCanManageOrganization({ requestActorId, organizationId })
  - listQrLinksByLocation: assertActorCanManageLocation({ requestActorId, locationId })
  - listQrLinksByProfile: dalGetActorByProfileId({ profileId }) → assertActorOwnsVportActor (same as createQrLink)
resolveQrScan + dalGetQrLinkBySlug untouched — public QR routing unaffected.

**NEW apps/VCSM/src/features/booking/controller/resolveVportProfileId.controller.js**
Thin controller wrapping getVportProfileIdByActorIdDAL — fixes SENTRY MINOR DRIFT from 27-03 pass (hook calling DAL directly).

**apps/VCSM/src/features/booking/hooks/useQrLinks.js**
Route actorId → profileId through new controller instead of DAL directly.
Pass requestActorId: actorId to listQrLinksByProfile (VPORT actor authorizing itself).

---

## Execution Summary

### What was completed

**engines/booking/src/controller/listQrLinks.controller.js**

Added imports: assertActorCanManageOrganization, assertActorCanManageLocation, assertActorOwnsVportActor,
dalGetActorByProfileId — all already present in the engine, mirroring createQrLink.controller.js exactly.

listQrLinksByOrganization({ requestActorId, organizationId }):
  - requestActorId now required (throws if missing)
  - assertActorCanManageOrganization called before any DAL read
  - Same pattern as createQrLink organization branch

listQrLinksByLocation({ requestActorId, locationId }):
  - requestActorId now required (throws if missing)
  - assertActorCanManageLocation called before any DAL read

listQrLinksByProfile({ requestActorId, profileId }):
  - requestActorId now required (throws if missing)
  - dalGetActorByProfileId resolves profileId → profileActor
  - Self-check: if requestActorId === profileActor.id, passes immediately (VPORT self-manages)
  - Otherwise: assertActorOwnsVportActor enforces user-actor ownership chain
  - DAL read only after authorization confirmed
  - JSDoc explicitly notes resolveQrScan / dalGetQrLinkBySlug is the PUBLIC slug-routing path
    and is NOT touched — public QR routing is unaffected.

**NEW apps/VCSM/src/features/booking/controller/resolveVportProfileId.controller.js**
Thin controller wrapping getVportProfileIdByActorIdDAL. Resolves the SENTRY MINOR DRIFT from
27-03 where useQrLinks called the DAL directly. Single responsibility, no business logic.

**apps/VCSM/src/features/booking/hooks/useQrLinks.js**
Import changed: getVportProfileIdByActorIdDAL → resolveVportProfileIdController.
listQrLinksByProfile call updated: { requestActorId: actorId, profileId: resolvedProfileId.current }.
Comment explains self-authorization: VPORT actor authorizes access to its own QR links.

**Public QR routing — CONFIRMED UNAFFECTED**
resolveQrScan + dalGetQrLinkBySlug are read-only slug lookups with no caller identity requirement.
These are explicitly untouched. The RLS policy allowing all-active-link reads by authenticated
users is correct for slug routing — it does not apply to the now-gated list functions.

### Security chain

BEFORE:
  useQrLinks({ actorId })
    → getVportProfileIdByActorIdDAL  [hook → DAL directly, SENTRY DRIFT]
    → listQrLinksByProfile({ profileId })  [no requestActorId, no assert, RLS-only]

AFTER:
  useQrLinks({ actorId })
    → resolveVportProfileIdController  [hook → controller → DAL, compliant]
    → listQrLinksByProfile({ requestActorId: actorId, profileId })
      → dalGetActorByProfileId  [resolve profileId → actor]
      → self-check OR assertActorOwnsVportActor  [ownership gate]
      → dalListQrLinksByProfile  [DAL read — after auth]

### Build result
✓ 7259 modules transformed — zero errors.
+2 modules vs. 27-03 (new resolveVportProfileId.controller.js + compiled output).

### SENTRY Status: ALIGNED
SENTRY Reviewed Files:
  - engines/booking/src/controller/listQrLinks.controller.js — ALIGNED
  - apps/VCSM/src/features/booking/controller/resolveVportProfileId.controller.js — ALIGNED
  - apps/VCSM/src/features/booking/hooks/useQrLinks.js — ALIGNED
SENTRY Follow-Up: None

### VENOM V-002 Status: RESOLVED
All three list functions now require and enforce requestActorId ownership before DAL read.
Public QR slug resolution path (resolveQrScan) is unchanged and unaffected.
SENTRY MINOR DRIFT from 27-03 also resolved in this pass.

### Change Intent Registry
- Entry written: NO
- Files logged: N/A — no deletions, moves, or renames occurred

---

NOTE OF COMPLETITION
