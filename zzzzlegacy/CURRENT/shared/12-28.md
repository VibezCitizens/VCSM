# Planning — April / 12 / Sequence 28

> Task: REMOVE DIRECT CLIENT WRITES TO notification.events AND RESTORE ENGINE-OWNED PUBLISH FLOW

---

## Execution Plan

**Task:** Remove direct client writes to `notification.events`, restore engine-owned publish flow
**Application Scope:** VCSM + ENGINE
**Task Class:** IMPLEMENTATION
**Execution Type:** Direct — 1 DAL file, DB function pre-deployed by user

**Architecture Audit (pre-execution):**
All 7 write events already correctly flow through `publishVcsmNotification()` → `publishEvent()`.
No rogue direct writes in feature controllers. The problem was `dalInsertEvent` using the
browser Supabase client to insert into `notification.events` — no INSERT RLS policy existed.

Fix: user deployed `notification.create_event(...)` SECURITY DEFINER RPC.
Engine DAL updated to call `supabase.schema('notification').rpc('create_event', {...})`.
Browser client is preserved for DI; actual DB write is now server-owned.

---

## Execution Summary

**What was completed:**
- `dalInsertEvent` updated to call `supabase.schema('notification').rpc('create_event', {...})` instead of direct `.from('events').insert()`
- Parameter mapping uses `p_` prefix names matching the deployed SECURITY DEFINER function signature
- Return shape unchanged — function returns `{ id, event_key, source_domain, created_at }`, consumed identically via `.single()`

**Why it was done this way:**
- Preserves browser client DI pattern — no app or setup changes needed
- Engine DAL layer intact — only the insert mechanism changes
- Matches established `vc.create_notification()` SECURITY DEFINER precedent
- DB write authority is now server-owned (function executes as postgres/service role)

**Files changed:**
- `engines/notifications/src/dal/events.write.dal.js` — RPC call replacing direct insert

**Specialist commands used:** None

**Follow-up items (not blocking):**
- Audit `notification.recipients`, `notification.inbox_items`, `notification.rendered` write DALs for same RLS gap
- These may also fail once `notification.events` insert succeeds — same RPC pattern applies

**SENTRY Reviewed Files:**
- `engines/notifications/src/dal/events.write.dal.js`

**SENTRY Status:** ALIGNED — DAL → Controller layer boundary preserved. RPC call is a transport detail within the DAL, not an architecture violation.

NOTE OF COMPLETITION
