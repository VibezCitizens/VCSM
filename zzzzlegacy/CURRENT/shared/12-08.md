# Planning — April / 12 / Sequence 08

## Task

Fix performance dashboard trust issue — route-isolated traces, background separation, hide bottom nav on /dev/performance, internal dev navigation.

### Source

Promoted from `.tp-active.md`.

## Execution Summary

### What Was Done

**Slice 1 — Background tagging + hide bottom nav**
- `RootLayout.jsx`: Added `isDevPerfRoute` check — bottom nav + top nav hidden on `/dev/performance`
- `screenTrace.js`: Queries without an active trace tagged `_isBackground: true` and stored in separate `_backgroundQueries` array. Queries within a trace but from a mismatched route also tagged as background.
- Exported `getBackgroundActivity()` returning background query/API counts and timing.

**Slice 2 — Internal dev navigation**
- Created `DevNavBar.jsx` — route inspector bar that replaces the hidden platform nav. Shows all traced routes as filter buttons with trace counts. Includes "Background" filter and quick-access route buttons.
- Route filter state added to dashboard — clicking a route button filters all views (metrics, tables, duplicates, N+1) to that route only.

**Slice 3 — Dashboard UI separation**
- Scope indicator banner shows "Session Totals" vs "Route: /feed" vs "Background activity" with query counts
- Analysis engine now uses filtered data when a route filter is active — metrics, duplicates, N+1 all scoped to selected route
- Session totals note background query count with hint to use route filter
- Alert banner uses filtered traces

### Files Created
- `debuggers/performance/components/DevNavBar.jsx`

### Files Modified
- `apps/VCSM/src/app/layout/RootLayout.jsx` (hide nav on /dev/performance)
- `debuggers/performance/screenTrace.js` (background tagging + getBackgroundActivity)
- `debuggers/performance/PerfDashboardScreen.jsx` (route filter, DevNavBar, scope labels)
- `debuggers/performance/index.js` (export getBackgroundActivity)

### Build: PASSES

### What This Fixes
- "976 queries" no longer appears as if it's from the current route — session totals clearly labeled
- Route filter isolates queries to specific routes (e.g., click "/feed" → see only feed queries)
- Background polling/realtime queries separated from active screen loads
- Learning queries only appear when you filter to "/learning" routes
- Platform bottom nav hidden on /dev/performance — replaced by dev-only route inspector

NOTE OF COMPLETITION
