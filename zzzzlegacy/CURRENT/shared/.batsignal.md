Go to: /Users/vcsm/Desktop/VCSM/apps/Traffic

Do a deep review of the codebase without changing unrelated code.

Tasks:

1. Diagnose and fix location behavior:
   - When I open the site on the internet, it always shows Miami instead of my real/current location.
   - Find where Miami is being used as a fallback/default.
   - Check browser geolocation permission flow, IP/location fallback, persisted location state, SSR/client hydration issues, and any cached/default coordinates.
   - Make the app request/use the user’s actual location correctly.
   - If permission is denied or unavailable, show a clear fallback state instead of silently forcing Miami.

2. Fix mobile search bar layout:
   - The attached screenshot shows the search bar/code gets cut off on mobile.
   - Inspect the responsive layout, viewport width, overflow, padding, fixed widths, and safe-area handling.
   - Fix it so the search bar is fully visible and usable on small screens.

3. Make it PWA compatible:
   - Review manifest, service worker, icons, theme color, display mode, viewport meta, offline behavior, installability, and mobile browser compatibility.
   - Add/fix only what is needed for proper PWA behavior.

4. Reference VCSM Explore:
   - Read the VCSM Explore screen/code for the existing mobile/PWA/settings patterns.
   - Reuse the same compatible settings/patterns in Traffic where appropriate.

Requirements:
- First explain the diagnosis clearly.
- Then implement the fixes.
- Do not rewrite the whole app.
- Do not change unrelated UI or logic.
- Keep existing style and architecture.
- Provide the full updated code for every file changed.
- List every changed file and why it changed.