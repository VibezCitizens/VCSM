# Planning — May / 09 / Sequence 04

## Task
Refactor Traffic UI — build shared Traze public page design system so all public pages inherit the same landing-page visual style.

## Execution Plan

**Task Class:** IMPLEMENTATION
**Application Scope:** TRAFFIC
**SENTRY Review:** Required — Post-Execution
**SENTRY Scope:** All new shared components, CountryHubTemplate.jsx, pages touched
**SENTRY Timing:** Post-Execution
**Estimated Time:** ~35 min
**Execution Type:** Split (4 slices)

---

### Current State

Shell class `<div className="homepage homepage--immersive traze-public-screen">` copied 8× across pages.
Hero has 3 separate CSS systems: `.homepage-hero*`, `.dir-hero*`, `.ch-hero-*`.
Provider card is `HomepageProviderCard` (home/components/) — already reused but misnamed/mislocated.
No shared CSS primitive file exists for cross-page design tokens.

---

### What changes

**Slice 1 — Foundation**

- NEW `apps/Traffic/src/shared/components/TrazePageShell.jsx`
  Server component. Wraps `<div className="homepage homepage--immersive traze-public-screen">`.
  Props: `children`, `className` (optional).

- NEW `apps/Traffic/src/styles/traze-public-system.css`
  Shared design primitives: `.traze-section`, `.traze-section-header`, `.traze-eyebrow`, `.traze-hero-base`, `.traze-stat-pill`.
  NOT duplicating — only extracting what's currently copy-pasted across hero.css / directory.css / country-hub.css.

- MOD `apps/Traffic/src/app/globals.css` — add import for traze-public-system.css

- MOD 8 page/template files — replace shell div with `<TrazePageShell>`.

---

**Slice 2 — Cards**

- NEW `apps/Traffic/src/shared/components/TrazeProviderCard.jsx`
  Move logic from `HomepageProviderCard.jsx`. Export `getCategoryStyle` from same file.
  `"use client"`. Same props shape. Used on homepage, country hub, top-providers.

- NEW `apps/Traffic/src/shared/components/TrazeCategoryCard.jsx`
  Extract card UI from `HomepageCategoryGrid.jsx`. Props: `category`, `lang`, `href`.
  `"use client"`.

- MOD `HomepageProviderCard.jsx` — re-export from TrazeProviderCard (backwards compat shim, 2 lines).
- MOD `HomepageCategoryGrid.jsx` — replace inline card markup with `<TrazeCategoryCard>`.
- MOD `CountryHubTemplate.jsx` — import from `@/shared/components/TrazeProviderCard`.

---

**Slice 3 — Layout primitives**

- NEW `apps/Traffic/src/shared/components/TrazeHero.jsx`
  `"use client"` — uses `useTrafficLanguage`.
  Props: `eyebrowEn`, `eyebrowEs`, `titleEn`, `titleEs`, `subtitleEn`, `subtitleEs`, `stats`, `children`.
  Used by: CountryHubTemplate hero, categories page hero, top-providers page hero.

- NEW `apps/Traffic/src/shared/components/TrazeSection.jsx`
  Server-safe. Props: `eyebrow`, `title`, `actions`, `children`, `className`.
  Renders with `.traze-section` and `.traze-section-header` from traze-public-system.css.

- NEW `apps/Traffic/src/shared/components/TrazeEmptyState.jsx`
  `"use client"`. Props: `titleEn`, `titleEs`, `subtitleEn`, `subtitleEs`, `icon`.

---

**Slice 4 — Integration + CSS cleanup**

- MOD `CountryHubTemplate.jsx` — replace inline hero with `<TrazeHero>`, section headers with `<TrazeSection>`.
- MOD `CategoriesDiscoveryClient.jsx` — add `<TrazeHero>` at top if no hero exists.
- MOD `TopProvidersDiscoveryClient.jsx` — add `<TrazeHero>` at top if no hero exists.
- CSS: Remove duplicated `.ch-section`, `.ch-section-header`, `.ch-section-title` from country-hub.css (covered by traze-public-system.css). Keep page-specific overrides.
- Manual test all 6 pages.

---

### Files Expected To Change

NEW:
- `apps/Traffic/src/shared/components/TrazePageShell.jsx`
- `apps/Traffic/src/shared/components/TrazeProviderCard.jsx`
- `apps/Traffic/src/shared/components/TrazeCategoryCard.jsx`
- `apps/Traffic/src/shared/components/TrazeHero.jsx`
- `apps/Traffic/src/shared/components/TrazeSection.jsx`
- `apps/Traffic/src/shared/components/TrazeEmptyState.jsx`
- `apps/Traffic/src/styles/traze-public-system.css`

MOD:
- `apps/Traffic/src/app/globals.css`
- `apps/Traffic/src/app/page.jsx`
- `apps/Traffic/src/app/categories/page.jsx`
- `apps/Traffic/src/app/top-providers/page.jsx`
- `apps/Traffic/src/app/directory/page.jsx`
- `apps/Traffic/src/(seo)/[city]/categories/page.jsx`
- `apps/Traffic/src/(seo)/[city]/top-providers/page.jsx`
- `apps/Traffic/src/features/directories/templates/DirectoryPageTemplate.jsx`
- `apps/Traffic/src/features/directories/templates/CountryHubTemplate.jsx`
- `apps/Traffic/src/features/home/components/HomepageProviderCard.jsx`
- `apps/Traffic/src/features/home/components/HomepageCategoryGrid.jsx`
- `apps/Traffic/src/styles/pages/country-hub.css`

---

### Specialist Commands
- SENTRY: Post-execution

### Blocking Risks
- `TrazeHero` must support both the simple static title (country hub) and the dynamic client pattern (categories/top-providers) — use `"use client"` throughout.
- `TrazeCategoryCard` must not change category card appearance — pure extraction.
- `TrazeProviderCard` must maintain the `getCategoryStyle` export used elsewhere.
- Do NOT touch homepage 2-column hero layout — it is unique and working.

---

## Execution Summary

**Completed:** Traze public design system built. All 4 slices executed.

### Slice 1 — Foundation
- NEW `src/shared/components/TrazePageShell.jsx` — single source of truth for the page shell wrapper
- NEW `src/styles/traze-public-system.css` — shared primitives: `.traze-eyebrow`, `.traze-stat-pill`, `.traze-stats-row`, `.traze-hero-card`, `.traze-hero-title`, `.traze-hero-sub`, `.traze-section`, `.traze-section-header`, `.traze-section-title`, `.traze-section-link`, `.traze-empty-state`
- MOD `globals.css` — added import for traze-public-system.css (position 2, after tokens)
- MOD 8 files — replaced `<div className="homepage homepage--immersive traze-public-screen">` with `<TrazePageShell>`: page.jsx, categories/page.jsx, top-providers/page.jsx, directory/page.jsx, (seo)/[city]/categories/page.jsx, (seo)/[city]/top-providers/page.jsx, DirectoryPageTemplate.jsx, CountryHubTemplate.jsx

### Slice 2 — Cards
- NEW `src/shared/components/TrazeProviderCard.jsx` — full provider card logic with `getCategoryStyle` export
- NEW `src/shared/components/TrazeCategoryCard.jsx` — extracted category card (props: categoryKey, label, description, isLive, href, pills, lang)
- MOD `HomepageProviderCard.jsx` — thin re-export shim (2 lines) pointing to TrazeProviderCard
- MOD `HomepageCategoryGrid.jsx` — replaced inline card markup with `<TrazeCategoryCard>`
- MOD `CountryHubTemplate.jsx` — imports TrazeProviderCard from shared location

### Slice 3 — Layout primitives
- NEW `src/shared/components/TrazeHero.jsx` — bilingual hero ("use client", uses useTrafficLanguage); props: eyebrowEn/Es, titleEn/Es, subtitleEn/Es, stats, children
- NEW `src/shared/components/TrazeSection.jsx` — server-safe section wrapper with title + optional "view all" link; uses .traze-section / .traze-section-header
- NEW `src/shared/components/TrazeEmptyState.jsx` — bilingual empty/no-results state

### Slice 4 — Integration + CSS cleanup
- MOD `CountryHubTemplate.jsx` — full rewrite: hero uses `.traze-hero-card` + `.traze-hero-title` + `.traze-hero-sub`; all three sections use `<TrazeSection>` (replaces local SectionHeader component)
- MOD `country-hub.css` — removed `.ch-hero-title`, `.ch-hero-sub`, `.ch-hero-stats`, `.ch-stat-pill`, `.ch-section`, `.ch-section-header`, `.ch-section-title`, `.ch-section-link` (all covered by traze-public-system.css)
- Verified: zero remaining references to removed CSS classes in any .jsx/.js/.css file

**Contract compliance:**
- All files under 300 lines (§4.1 ✓)
- All imports use `@/...` (§1.1 ✓)
- No data logic changed
- No routing changed
- Spanish/English labels preserved

**SENTRY:** Post-execution required — architecture review of shared/ components.

NOTE OF COMPLETITION

