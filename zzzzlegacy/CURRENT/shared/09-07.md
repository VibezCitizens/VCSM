# Planning — April / 09 / Sequence 07

> Awaiting tasks.
You are a senior full-stack code auditor and architecture analyst.

TASK
Review all screens inside:

/Users/vcsm/Desktop/VCSM/apps/VCSM

and explain the full pipeline of how actor switching is consumed across the app.

PRIMARY GOAL
I want a real project-specific audit of how “switch actor” flows through the VCSM app after the active actor changes.

You must inspect the actual code and explain:

- where actor switching starts
- how the active actor is resolved
- how the switched actor is committed
- how screens consume the switched actor
- which screens correctly react to actor changes
- which screens keep stale actor state
- which providers/hooks/query layers depend on actor identity
- which parts of the app are healthy vs unhealthy

SCOPE
Review all relevant app screens and actor-dependent pipelines inside:

/Users/vcsm/Desktop/VCSM/apps/VCSM

This includes, but is not limited to:
- main app bootstrap
- auth provider
- identity provider/context/store
- feed screens
- profile screens
- vport screens
- booking screens
- review screens
- tab systems
- route-level wrappers
- global providers
- hooks and viewer builders used by screens

WHAT TO ANALYZE

1. BOOTSTRAP / ENTRY
Find:
- app entry point
- identity engine setup
- auth setup
- global providers
- where identity is first loaded on app start

2. SWITCH ACTOR PIPELINE
Trace the full switch flow:
- actor switch trigger
- switch action/controller/service
- identity engine call
- active actor selection
- commit to app state
- propagation to providers/hooks/screens

Show the exact sequence.

3. SCREEN CONSUMPTION
Review all major screens and explain how they consume active actor identity.

For each screen or screen family, document:
- file path
- whether it consumes `useIdentity()` or another source
- whether it builds a viewer object
- whether it depends on actorId/kind/userAppAccountId/realmId
- whether it refreshes on actor switch
- whether it risks stale actor state

4. PROVIDERS / STORES / CONTEXTS
Identify all providers and stores involved in actor consumption.

For each one:
- file path
- role
- what identity fields it reads
- whether it derives local actor/viewer state
- whether it rebuilds correctly after switch
- whether it can go stale

5. QUERY / CACHE CONSUMPTION
Identify actor-dependent queries and cache keys.

Check:
- feed queries
- profile queries
- booking queries
- review queries
- any actor-based data loaders

Explain:
- whether actorId is part of the query key
- whether actor switch invalidates/refetches correctly
- whether stale data can persist

6. VIEWER MODEL PIPELINES
Find all “viewer” objects or equivalent derived identity objects.

Examples:
- feed.viewer
- booking viewer
- profile viewer
- screen-local actor models

For each:
- where built
- what source it uses
- whether it derives from current global identity
- whether it can drift from the active actor

7. HEALTH CHECK
For each major feature/screen area, determine:

- healthy
- partially healthy
- unhealthy

Use concrete reasons based on the code.

8. SWITCH CONSISTENCY RULE
Check whether this invariant is respected:

globalIdentity.actorId === localScreenActorId/viewerActorId

For each actor-dependent feature, state whether this is guaranteed or not.

9. FILE MAP
List the exact files directly involved in actor-switch consumption.

For each file include:
- path
- layer (screen / hook / controller / provider / store / adapter / utility)
- what role it plays in switch propagation

10. RISKS / GAPS
Identify:
- stale viewer risks
- stale local state
- bad memo dependencies
- query keys missing actorId
- route state not rebuilding
- tab state not rebuilding
- providers initialized once and never refreshed
- action hooks capturing old actorId
- screens that are split-brain risks

11. FINAL VERDICT
At the end provide:
- overall actor-switch health
- top screens/providers causing risk
- top 10 files to change
- recommended fix order

DELIVERABLE
Write a full markdown document and save it to:

/Users/vcsm/Desktop/VCSM/logan

Recommended filename:
vcsm-actor-switch-consumption-audit.md

OUTPUT FORMAT
Use these exact sections:

1. Actor Switch Architecture Overview
2. App Bootstrap and Identity Entry
3. Switch Actor Pipeline
4. Screen-by-Screen Consumption Audit
5. Providers, Stores, and Contexts
6. Query and Cache Consumption
7. Viewer Model Pipelines
8. Health Assessment by Feature
9. File Map
10. Risks and Gaps
11. Final Verdict
12. Recommended Fix Order

IMPORTANT RULES
- Do not guess.
- Inspect the real code.
- Use exact file paths and exact function names.
- Explain the actual flow, not a generic React explanation.
- Be explicit about what consumes actor identity and how.
- Clearly separate healthy vs stale vs risky pipelines.
- Focus on how switch actor is consumed across screens.

FINAL ACTION
After completing the audit, save the markdown file here:

/Users/vcsm/Desktop/VCSM/logan/vcsm-actor-switch-consumption-audit.md

Audit all actor-switch consumption across /Users/vcsm/Desktop/VCSM/apps/VCSM.

I want a real code-level analysis of how active actor switching is consumed by all screens, providers, hooks, and queries.

Find:
- where switch starts
- how identity is committed
- how screens consume switched actor
- which screens/providers are stale-risk
- which queries/cache keys are actor-dependent
- which viewer objects can drift from global identity

Write a markdown document to:
 /Users/vcsm/Desktop/VCSM/logan/vcsm-actor-switch-consumption-audit.md

Required sections:
- Overview
- Bootstrap
- Switch pipeline
- Screen-by-screen consumption
- Providers/stores
- Query/cache usage
- Viewer models
- Health by feature
- File map
- Risks/gaps
- Final verdict
- Recommended fix order

## Execution Plan

PLAN PROPOSAL
Task: Full actor-switch consumption audit across all VCSM screens — produce logan document
Application Scope: VCSM (read-only inspection, output to logan/)
Estimated Time: ~10 minutes — split into 2 slices
Execution Type: Split

Slice 1 (~5 min) — Research: trace switch pipeline + categorize all 59 consumers
Slice 2 (~5 min) — Write 12-section logan audit document

Files Expected To Change:
- logan/vcsm-actor-switch-consumption-audit.md (new)
- planning/april/09/09-07.md (execution plan + summary)

Assumptions:
- Builds on 09-05 audit (all 59 consumers clean, no stale patterns)
- No app code modifications — documentation only

## Execution Summary

### What was completed
Full 12-section actor-switch consumption audit covering all 59 `useIdentity()` consumers.

### Output file
logan/vcsm-actor-switch-consumption-audit.md

### Key findings
- **All 59 consumers are healthy** — every one derives identity from React context inline on every render
- **Zero stale patterns** — no useRef holding actorId, no useState from identity, no empty-dep effects, no secondary stores
- **Switch pipeline is correct**: `switchActor()` → engine resolve → platform write → hydrate → `setIdentity()` → React context propagation → all 59 consumers re-render
- **No centralized query cache** — all queries are hook-local useState+useEffect, so no stale cache to invalidate
- **4 consistency guards active** on feed, booking, reviews, profile screens
- **No fixes needed** — the pipeline is architecturally sound

### Categorized consumers
- Feed: 5 files (healthy)
- Profile: 3 files (healthy)
- Booking: 1 file (healthy)
- Reviews: 2 files (healthy)
- Chat: 9 files (healthy)
- Post/Comments: 7 files (healthy)
- Upload: 3 files (healthy)
- Settings: 4 files (healthy)
- Dashboard: 8 files (healthy)
- Notifications: 2 files (healthy)
- Navigation/Other: 10 files (healthy)
- Internal: 5 files (source of truth)

### Files created
- logan/vcsm-actor-switch-consumption-audit.md

NOTE OF COMPLETITION