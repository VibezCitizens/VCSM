# Planning — April / 13 / Sequence 05

> Awaiting tasks.

## Execution Plan

Task: Migration drift cluster — remaining DB items + RPC schema prefix fixes
Application Scope: VCSM
Task Class: MIXED (DB_REVIEW + IMPLEMENTATION)
Execution Type: Split (2 slices)

Slice 1 — Code (no DB approval needed):
  - Fix RPC schema prefix mismatches (3 files, low effort)
    - block_actor / unblock_actor → supabase.schema('moderation').rpc(...)
    - search_actor_directory → supabase.schema('identity').rpc(...)
    - provision_vcsm_identity → supabase.schema('platform').rpc(...)

Slice 2 — DB (each item requires explicit SQL approval before execution):
  - Item A: vport.create_vport_for_actor — add INSERT into vport.profile_categories
  - Item B: vport.create_vport_for_actor — add perform identity.refresh_actor_directory_row call
  - Item C: identity.refresh_actor_directory_row — fix vport hydration source to vport.profiles
  - Item D: vport.profile_categories INSERT RLS policy
  - Item E: vc.actor_owners backfill for pre-migration vport actors (fixes inbox ownerActorId)

Files Expected To Change (Slice 1):
  - apps/VCSM/src/features/block/dal/block.write.dal.js
  - apps/VCSM/src/features/settings/privacy/dal/blocks.dal.js
  - apps/VCSM/src/features/explore/dal/search.data.js
  - apps/VCSM/src/features/identity/dal/provision.rpc.dal.js

Specialist Commands Required: DB (for Slice 2 SQL proposals)
SENTRY Review: Optional — Post-Execution (DAL files changed)
SENTRY Timing: Post-Execution
Blocking Risks: DB items need per-item SQL approval

## Execution Summary

### Code Changes — Completed

CODE-1: vcsmActorHydrator.js
- Added `vport.profile_actor_access` fallback for ownerActorId resolution
- When `vc.actor_owners` row is missing, reads `vport.profile_actor_access` where `profile_id = vport.id AND is_primary = true AND status = 'active'`
- Uses `actor_id` directly — no legacy column references
- Fixes inbox INVALID vport identity / missing ownerActorId log

CODE-2: RPC schema prefixes — already correct
- block.write.dal.js: already uses `.schema('moderation').rpc(...)`
- blocks.dal.js: already uses `.schema('moderation').rpc(...)`
- search.data.js: already uses `.schema('identity').rpc(...)`
- provision.rpc.dal.js: already uses `.schema('platform').rpc(...)`
- No changes required

Files changed:
- apps/VCSM/src/features/hydration/vcsmActorHydrator.js

### DB Pending — Requires Explicit SQL Approval Per Item

DB-1: vport.create_vport_for_actor — add INSERT into vport.profile_categories
DB-2: vport.create_vport_for_actor — add perform identity.refresh_actor_directory_row call
DB-3: identity.refresh_actor_directory_row — fix vport source from vc.vports → vport.profiles
DB-4: vport.profile_categories — add INSERT RLS policy
DB-5: vc.actor_owners backfill for pre-migration vport actors
DB-6: delete_my_account — function missing, needs implementation or DAL rewrite
DB-7: delete_my_vport — function missing, may be replaced by dalDeleteOwnedVportById

Full SQL proposals in: zNOTFORPRODUCTION/planning/.tp-ready.md
