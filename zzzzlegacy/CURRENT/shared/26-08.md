# Planning — May / 26 / Sequence 08

> Session: 2026-05-26
> Task: P3 — centralize resolveProfileId across 14 vport DAL files

---

## Execution Plan

**Task:** Replace 14 private `resolveProfileId` copies with shared `resolveVportProfileId` from `dal/services/resolveVportProfileId.dal.js`  
**Application Scope:** VCSM  
**Task Class:** IMPLEMENTATION  
**Estimated Time:** ~10 minutes  
**Execution Type:** Split  
**SENTRY Review:** Optional  

### Files (14 total)

**Slice 1 — gas (6):**
1. `dal/gas/vportFuelPrices.read.dal.js`
2. `dal/gas/vportFuelPrices.write.dal.js`
3. `dal/gas/vportFuelPriceSubmissions.read.dal.js`
4. `dal/gas/vportFuelPriceSubmissions.write.dal.js`
5. `dal/gas/vportFuelPriceHistory.write.dal.js`
6. `dal/gas/vportStationPriceSettings.read.dal.js`

**Slice 2 — rates/content/menu (8):**
7. `dal/rates/readVportRatesByActor.dal.js`
8. `dal/rates/upsertVportRate.dal.js`
9. `dal/content/createVportContentPage.dal.js`
10. `dal/menu/createVportMenuItemMedia.dal.js`
11. `dal/menu/createVportActorMenuItem.dal.js`
12. `dal/menu/listVportActorMenuCategories.dal.js`
13. `dal/menu/listVportActorMenuItems.dal.js`
14. `dal/menu/createVportActorMenuCategory.dal.js`

### Assumptions
- All 14 copies are identical in logic; shared utility is identical
- `createVportMenuItemMedia.dal.js` uses `vport` alias instead of `vportSchema` — private copy uses `vport`; shared utility uses `vportSchema` internally; no impact on callers
- Shared utility has its own 30s TTL cache — bonus: profile lookups deduplicated across all DAL calls within a page load

### Blocking Risks
- None — behavior identical; shared utility is already in production use by services DAL

---

## Execution Summary

**Completed:** 2026-05-26  
**Scope:** VCSM — `apps/VCSM/src/features/profiles/kinds/vport/dal/`  
**Result:** All 14 private `resolveProfileId` copies eliminated. 100% unified.

### What was done

**Slice 1 — gas (6 files) — previously completed this session:**  
Import added, private function removed, call sites renamed via sed for:
- `dal/gas/vportFuelPrices.read.dal.js`
- `dal/gas/vportFuelPrices.write.dal.js`
- `dal/gas/vportFuelPriceSubmissions.read.dal.js`
- `dal/gas/vportFuelPriceSubmissions.write.dal.js`
- `dal/gas/vportFuelPriceHistory.write.dal.js`
- `dal/gas/vportStationPriceSettings.read.dal.js`

**Slice 2 — rates/content/menu (8 files) — completed this continuation:**  
For rates/content (imports already added earlier this session) — private function removed + call sites renamed:
- `dal/rates/readVportRatesByActor.dal.js`
- `dal/rates/upsertVportRate.dal.js`
- `dal/content/createVportContentPage.dal.js`

For menu (full treatment — import + remove + rename):
- `dal/menu/createVportMenuItemMedia.dal.js` — single-quote / `vport` alias style preserved; logic identical
- `dal/menu/createVportActorMenuItem.dal.js`
- `dal/menu/listVportActorMenuCategories.dal.js`
- `dal/menu/listVportActorMenuItems.dal.js`
- `dal/menu/createVportActorMenuCategory.dal.js`

### Verification

- `grep -r "async function resolveProfileId"` → **0 results** — no private copies remain
- `grep -rl "resolveVportProfileId"` → **14 target files** confirmed + 3 pre-existing services consumers + shared utility itself = 18 total
- Shared utility (`dal/services/resolveVportProfileId.dal.js`) has 30s TTL cache — all profile lookups across all DAL calls in a page load now deduplicate

### Why this way

Every file used an identical private 8-line function with no cache. The shared utility is already in production use by the services DAL layer, has its own TTL cache, and handles the null case identically. No behavior change — pure deduplication of maintenance surface.

### SENTRY Review

SENTRY not run — purely mechanical deduplication with no layer responsibility changes. All files remain read-path DAL utilities calling the same shared service-layer resolver.

### Files Changed (14)

- `dal/gas/vportFuelPrices.read.dal.js`
- `dal/gas/vportFuelPrices.write.dal.js`
- `dal/gas/vportFuelPriceSubmissions.read.dal.js`
- `dal/gas/vportFuelPriceSubmissions.write.dal.js`
- `dal/gas/vportFuelPriceHistory.write.dal.js`
- `dal/gas/vportStationPriceSettings.read.dal.js`
- `dal/rates/readVportRatesByActor.dal.js`
- `dal/rates/upsertVportRate.dal.js`
- `dal/content/createVportContentPage.dal.js`
- `dal/menu/createVportMenuItemMedia.dal.js`
- `dal/menu/createVportActorMenuItem.dal.js`
- `dal/menu/listVportActorMenuCategories.dal.js`
- `dal/menu/listVportActorMenuItems.dal.js`
- `dal/menu/createVportActorMenuCategory.dal.js`

---

NOTE OF COMPLETITION
