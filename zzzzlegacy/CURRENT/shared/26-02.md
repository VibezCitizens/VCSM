# Planning — April / 26 / Sequence 02

> Booking Engine Extraction + Complete Status Transitions


## Execution Plan

SCOPE: VCSM + ENGINE
TASK: Extract booking feature into engines/booking/ + add completeBooking + markNoShow

## Execution Summary

### What was completed

**engines/booking/** — 35 new files:
- `CLAUDE.md`, `CONTRACT.md`, `index.js`
- `src/config.js` — `configureBookingEngine({ supabaseClient, vportClient, notifyFn })`
- `src/events.js` — BOOKING_EVENTS constants
- `src/types/index.js` — JSDoc domain types
- 9 DAL files (booking.read, booking.write, resource.read, resource.write, availability.read, availability.write, serviceProfile.read, serviceProfile.write, actor.read)
- 4 model files (Booking, BookingResource, BookingServiceProfile, BookingAvailability)
- 14 controller files — 12 ported + 2 NEW: completeBooking, markNoShow
- `src/adapters/index.js` — full public API

**apps/VCSM** — wired up:
- `src/features/booking/setup.js` — NEW, calls configureBookingEngine
- `vite.config.js` — @booking alias added
- `jsconfig.json` — @booking path added
- `src/main.jsx` — setupVcsmBookingEngine() called at startup
- 7 hooks updated to import from @booking
- `booking.adapter.js` — now exports useBookingHistory + all 6 hooks

### New status transitions
- `completeBooking({ bookingId, requestActorId, internalNote })` — owner only, status → completed
- `markNoShow({ bookingId, requestActorId, internalNote })` — owner only, status → no_show
Both emit notifications to customer via notifyFn, guard against invalid state transitions.

### Documentation drift status
Logan doc (vcsm.booking.pipeline.md) needs update to reflect engine extraction. MINOR DRIFT.

NOTE OF COMPLETITION
