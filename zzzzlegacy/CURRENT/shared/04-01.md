# Planning — May / 04 / Sequence 01

> Session started: 2026-05-04

## Task

Unified Taxonomy Naming Contract — VPort + Traze/Traffic.

Define and implement one canonical taxonomy model, resolve inconsistent naming
across `traffic.*`, `vport.*`, DALs, mappers, and UI labels.

## Execution Plan

**Task Class:** MIXED (DB_REVIEW + DOCUMENTATION + IMPLEMENTATION)
**Application Scope:** TRAFFIC + VCSM (vport schema views shared by both)
**Multi-root:** YES — requires explicit approval before execution

### Slices

| # | Slice | Type | Scope |
|---|-------|------|-------|
| S1 | Inconsistency report + canonical naming contract doc in logan/ | DOCUMENTATION | BOTH |
| S2 | SQL: create `vport.public_traze_catalog_services_v` + `vport.public_traze_provider_taxonomy_v` | DB | vport schema |
| S3 | Traffic mapper: assess `service_group` rename to `specialty_group` in SERVICES_SELECT | TRAFFIC | apps/Traffic |
| S4 | VCSM source: no source changes required — views handle canonical translation | N/A | — |

### Files Expected To Change

**Traffic:**
- `apps/Traffic/src/data/dal/trazeCategories.read.dal.js` (add `specialty_group` if renamed)
- `apps/Traffic/src/data/repositories/category.repo.js` (align field names)
- `apps/Traffic/src/data/types.js` (update typedef comments)
- `apps/Traffic/src/data/mappers/vportToProvider.model.js` (confirm canonical output shape)

**SQL (output only until approved):**
- `vport.public_traze_catalog_services_v` (new view)
- `vport.public_traze_provider_taxonomy_v` (new view)

**Documentation:**
- `logan/traffic/traffic.taxonomy.naming-contract.md` (new doc)

**VCSM Source:** No changes — existing source code is not touched.

### Specialist Commands
- CARNAGE: SQL view authoring
- LOGAN: post-execution doc update

### SENTRY Review
- Required: Optional
- Scope: `apps/Traffic/src/data/`
- Timing: Post-Execution

---

## Execution Summary

### Slice 1 — COMPLETE (2026-05-04)

**What was completed:**
Created `zNOTFORPRODUCTION/_CANONICAL/logan/traffic/traffic.taxonomy.naming-contract.md`

**What it contains:**
- 4-level canonical taxonomy definitions (category / service / business_type / specialty)
- Banned aliases list with reasons and replacements
- Full current schema → canonical name mapping for all affected tables and fields
- Canonical provider taxonomy JS object shape
- UI form label standards
- Source of truth rules (VCSM → views → TRAZE)
- 4 open questions that must be resolved before Slice 2 can begin

**No code changed. No SQL written. No DB altered.**

**Slices 2–3 blocked pending answers to Open Questions Q1–Q4.**

---

Awaiting approval for Slice 2 after open questions are resolved.
