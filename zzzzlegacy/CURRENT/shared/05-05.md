# Planning — April / 05 / Sequence 05

> Awaiting tasks.
You are auditing and fixing the remaining legacy VCSM inbox action/runtime paths after partial chat migration.

Project root:
- /Users/vcsm/Desktop/VCSM

Application:
- /Users/vcsm/Desktop/VCSM/apps/VCSM

Goal:
Migrate the remaining ACTIVE inbox/conversation mutation paths off legacy `vc.inbox_entries` and onto the shared chat engine over `chat.*`.

This is not a broad chat migration.
This is a targeted cleanup of the remaining inbox action + spam/leave mutation paths.

Do NOT modify unrelated chat flows.
Do NOT touch Wentrex.
Do NOT change database schema unless explicitly required and approved.
Prefer forward migration, not rollback.

==================================================
CURRENT PROVEN STATE
==================================================

Already migrated:
- main inbox read path is engine-backed
- inbox folder screens likely read through engine via:
  - useInboxFolder -> useInbox
- unread badge query is engine-backed
- unread badge realtime is engine-backed

Still PROVEN legacy:
- apps/VCSM/src/features/chat/conversation/screen/handlers/conversationView.handlers.js
  imports markConversationSpam
- apps/VCSM/src/features/chat/conversation/controllers/markConversationSpam.controller.js
  imports moveConversationToFolder from legacy inbox.write.dal
- apps/VCSM/src/features/chat/conversation/controllers/leaveConversation.controller.js
  imports archiveConversationForActor + resetUnread from legacy inbox.write.dal
- apps/VCSM/src/features/chat/inbox/controllers/inboxActions.controller.js
  imports updateInboxFlags / archiveConversationForActor / moveConversationToFolder from legacy inbox.write.dal
- apps/VCSM/src/features/chat/inbox/dal/inbox.write.dal.js
  writes directly to:
    schema('vc').from('inbox_entries')

This means the app is still HYBRID, not fully engine-backed.

==================================================
PRIMARY OBJECTIVE
==================================================

Remove ACTIVE runtime dependency on:

- vc.inbox_entries

for these mutation flows:

1. archive conversation
2. move conversation to spam
3. move conversation between folders if supported
4. pin / mute / inbox flags
5. leave conversation
6. reset unread if still routed through legacy inbox write DAL

==================================================
FILES TO INSPECT FIRST
==================================================

Inspect these exact files first:

- apps/VCSM/src/features/chat/conversation/screen/handlers/conversationView.handlers.js
- apps/VCSM/src/features/chat/conversation/controllers/markConversationSpam.controller.js
- apps/VCSM/src/features/chat/conversation/controllers/leaveConversation.controller.js
- apps/VCSM/src/features/chat/inbox/controllers/inboxActions.controller.js
- apps/VCSM/src/features/chat/inbox/dal/inbox.write.dal.js

Also inspect engine equivalents:

- engines/chat/src/hooks/useInboxActions.js
- engines/chat/src/controller/inboxActions.controller.js
- engines/chat/src/controller/deleteThreadForMe.controller.js
- engines/chat/src/dal/inbox_entries.write.dal.js
- engines/chat/src/controller/leaveConversation.controller.js (if exists)
- engines/chat/src/controller/markConversationSpam.controller.js (if exists)
- engines/chat/src/adapters/chat.adapter.js

Also verify whether these are active or dead:
- apps/VCSM/src/features/chat/inbox/realtime/subscribeToInbox.js
- apps/VCSM/src/features/chat/inbox/hooks/useSpamConversationIds.js
- apps/VCSM/src/features/chat/inbox/hooks/useInboxFolder.js
- apps/VCSM/src/features/chat/inbox/constants/inboxFlags.js

==================================================
TASKS
==================================================

1. Audit all ACTIVE runtime imports connected to:
   - inbox.write.dal.js
   - markConversationSpam.controller.js
   - leaveConversation.controller.js
   - inboxActions.controller.js

2. Classify each file as:
   - ACTIVE RUNTIME
   - DEAD CODE
   - DEV / DIAGNOSTIC ONLY
   - COMMENT / DOC ONLY

3. Migrate ACTIVE runtime inbox mutation paths to engine-backed chat.* authority.

4. Preserve current UI behavior:
   - archive still archives
   - spam still moves to spam and still keeps report side effects if present
   - leave conversation still behaves the same for direct vs group
   - pin/mute/folder actions still behave the same

5. Replace legacy runtime imports so active UI no longer depends on:
   - apps/VCSM/src/features/chat/inbox/dal/inbox.write.dal.js

6. After migration, verify whether these legacy files are now dead:
   - apps/VCSM/src/features/chat/inbox/dal/inbox.write.dal.js
   - apps/VCSM/src/features/chat/conversation/controllers/markConversationSpam.controller.js
   - apps/VCSM/src/features/chat/conversation/controllers/leaveConversation.controller.js
   - apps/VCSM/src/features/chat/inbox/controllers/inboxActions.controller.js
   - apps/VCSM/src/features/chat/inbox/realtime/subscribeToInbox.js

7. Do NOT delete anything unless it is confirmed to have zero runtime consumers.

==================================================
BEHAVIOR REQUIREMENTS
==================================================

- Spam action must remain functional from ConversationView
- Archive action must remain functional
- Leave conversation must remain functional
- Folder screens must still work
- No visible UI regression
- No schema authority split for these inbox mutation flows

==================================================
VERIFICATION REQUIREMENTS
==================================================

After implementing, verify:

1. conversationView.handlers no longer routes spam through legacy vc inbox DAL
2. markConversationSpam flow no longer depends on vc.inbox_entries
3. leaveConversation flow no longer depends on vc.inbox_entries
4. inboxActions.controller no longer depends on vc.inbox_entries
5. active runtime imports of inbox.write.dal are zero, or only dev/diagnostic
6. active runtime authority for archive/spam/flags/leave is now chat.*
7. no unrelated chat flow was changed

==================================================
OUTPUT FORMAT
==================================================

Return exactly:

VCSM INBOX ACTION MIGRATION FIX RESULT

1. Files modified
2. Files created
3. Files deleted
4. Active legacy runtime found before fix
5. What was migrated
6. What remains legacy
7. Whether active runtime still depends on vc.inbox_entries
8. Exact blockers if yes
9. Verification summary
10. Final authority summary
- inbox main read
- inbox folder reads
- archive action
- spam action
- leave conversation
- inbox flags
- unread reset
- realtime
- badge

## Execution Plan

PLAN PROPOSAL
Task: Audit remaining legacy inbox action/mutation paths
Application Scope: VCSM (read-only audit)
Estimated Time: 1 minute
Execution Type: Direct
Files Expected To Change: NONE
Assumptions: All hooks already delegate to @chat; legacy files have zero runtime imports

## Execution Summary

### Audit Result
All "proven legacy" files listed in this task are already DEAD CODE at runtime. They have zero active runtime consumers. Every runtime hook was already migrated in previous slices.

### Evidence by file

**conversationView.handlers.js** — imports legacy markConversationSpam
  Runtime imports: ZERO (file is never imported anywhere)
  Status: DEAD CODE

**markConversationSpam.controller.js** — imports legacy inbox.write.dal
  Runtime imports: ZERO (only imported by dead handlers.js and dev diagnostics)
  Status: DEAD CODE

**leaveConversation.controller.js** — imports legacy inbox.write.dal
  Runtime imports: ZERO (only imported by dev diagnostics)
  Status: DEAD CODE

**inboxActions.controller.js** — imports legacy inbox.write.dal
  Runtime imports: ZERO (only imported by dev diagnostics)
  Status: DEAD CODE

**inbox.write.dal.js** — writes to vc.inbox_entries
  Runtime imports: ZERO (only imported by above dead controllers)
  Status: DEAD CODE

**subscribeToInbox.js** — subscribes to vc.inbox_entries
  Runtime imports: ZERO
  Status: DEAD CODE

**useSpamConversationIds.js** — fetches spam inbox via /api endpoint
  Runtime imports: ZERO (only in language audit files)
  Status: DEAD CODE

### What is ACTUALLY active at runtime

All inbox mutation hooks delegate to `@chat` engine:
- useInboxActions → @chat useInboxActions (archive, spam, pin, mute, leave, delete)
- useConversationActionsMenu → @chat markConversationSpam
- useInbox → @chat useInbox (reads + realtime)
- useInboxFolder → wraps useInbox
- useInboxEntryForConversation → @chat useInboxEntryForConversation

### VCSM INBOX ACTION MIGRATION FIX RESULT

1. Files modified: 0
2. Files created: 0
3. Files deleted: 0
4. Active legacy runtime found before fix: NONE (all were already dead)
5. What was migrated: Nothing new (previous slices completed this)
6. What remains legacy: Only dead files on disk (dev diagnostics, language audits)
7. Whether active runtime still depends on vc.inbox_entries: NO
8. Exact blockers: NONE
9. Verification summary: grep confirmed zero active runtime imports of legacy controllers/DALs
10. Final authority summary:
    - inbox main read: chat.* (engine)
    - inbox folder reads: chat.* (engine via useInboxFolder → useInbox)
    - archive action: chat.* (engine useInboxActions)
    - spam action: chat.* (engine markConversationSpam via useConversationActionsMenu)
    - leave conversation: chat.* (engine useInboxActions.leave)
    - inbox flags: chat.* (engine useInboxActions)
    - unread reset: chat.* (engine markConversationRead)
    - realtime: chat.* (engine subscribeToInbox inside useInbox)
    - badge: chat.* (inboxUnreadCount.dal reads chat.inbox_entries)

NOTE OF COMPLETITION