PROMPT — Enforce Identity Engine Resolution Order: APP FIRST, ACTOR SECOND

You are updating the identity engine so identity resolution always happens in the correct order:

1. separate by application first
2. separate by actor inside that application second

Project root:
- /Users/vcsm/Desktop/VCSM

Applications:
- /Users/vcsm/Desktop/VCSM/apps/VCSM
- /Users/vcsm/Desktop/VCSM/apps/Wentrex (if applicable)

Shared engines:
- /Users/vcsm/Desktop/VCSM/engines

This is a targeted engine-alignment task.
Do NOT refactor unrelated auth, chat, booking, or UI code.
Do NOT change database schema.
Prefer minimal, safe changes inside the identity engine and only minimal app changes if required.

==================================================
PRIMARY RULE
==================================================

The engine must always resolve identity in this order:

AUTH USER
  → APP
    → USER APP ACCOUNT
      → ACTOR LINKS
        → ACTIVE ACTOR

NEVER:

AUTH USER
  → ACTOR

==================================================
WHY THIS IS REQUIRED
==================================================

The same auth user can validly have multiple app accounts, for example:
- learning
- vcsm

And inside each app account, the user can have multiple actors, for example:
- VCSM citizen actor
- VCSM vport actor(s)
- Learning student/teacher/admin actor(s)

So the engine must first lock identity to the correct app context using `appKey`, then resolve actors only inside that app account.

==================================================
REQUIRED OUTCOME
==================================================

After this change, the engine must guarantee:

1. app separation always happens first
2. actor links are only resolved inside the resolved `user_app_account_id`
3. no actor is ever resolved globally by `user_id` alone
4. no actor link is ever resolved globally by `actor_id` alone unless it is already ownership-scoped
5. VCSM separates citizen vs vport only AFTER resolving the VCSM app account
6. Learning separates learning actors only AFTER resolving the Learning app account
7. switchActor/login/restore all follow the same order
8. fallback logic stays inside the current app account only

==================================================
FILES TO INSPECT FIRST
==================================================

Inspect these first:

- engines/identity/src/controller/resolveAuthenticatedContext.controller.js
- engines/identity/src/controller/switchActiveActor.controller.js
- engines/identity/src/services/actorService.js
- engines/identity/src/dal/app.read.dal.js
- engines/identity/src/dal/account.read.dal.js
- engines/identity/src/dal/access.read.dal.js
- engines/identity/src/dal/preferences.read.dal.js
- engines/identity/src/dal/preferences.write.dal.js
- engines/identity/src/dal/state.read.dal.js
- engines/identity/src/dal/actorLinks.read.dal.js
- engines/identity/src/model/ActorLink.model.js
- apps/VCSM/src/state/identity/identityContext.jsx
- apps/VCSM/src/state/identity/identity.controller.js

Also search for:
- auth user → actor resolution shortcuts
- actor resolution by `user_id`
- actor link lookup by `actor_id` without account scope
- `.single()` / `.maybeSingle()` in identity engine DALs
- any code that hydrates actor before resolving app account
- any code that uses localStorage actorId before confirming app context

==================================================
REQUIRED ENGINE FLOW
==================================================

The engine must behave like this:

STEP 1 — Resolve session user
- get authenticated auth user
- fail if none

STEP 2 — Resolve app
- use `appKey`
- fetch the app row from `platform.apps`
- fail if app is missing/inactive

STEP 3 — Resolve app access/account
- resolve access/account only for:
  - current `user_id`
  - current app (`app_id` or `app_key`)
- NEVER query app context by `user_id` alone

STEP 4 — Resolve account-scoped state
- load:
  - `platform.user_app_state`
  - `platform.user_app_preferences`
- strictly by `user_app_account_id`

STEP 5 — Resolve account-scoped actor links
- load actor links strictly by:
  - `user_app_account_id`
- not by raw `user_id`

STEP 6 — Resolve active actor INSIDE the account
fallback order:
1. `active_actor_link_id` if valid in this account
2. `last_actor_link_id` if valid in this account
3. primary active link in this account
4. first active switchable link in this account

STEP 7 — Return engine context
Must include:
- app
- userAppAccountId
- availableActors scoped to this account only
- activeActor scoped to this account only

==================================================
VCSM-SPECIFIC REQUIREMENT
==================================================

For VCSM, after the engine has resolved the `vcsm` app account, the app may hydrate actor-specific data:

- citizen/user actor
- vport actor(s)

That separation is APP-LOCAL.
The engine should not globally distinguish citizen vs vport before app resolution.

==================================================
LEARNING-SPECIFIC REQUIREMENT
==================================================

For Learning, after the engine has resolved the `learning` app account, the app may hydrate:

- student
- teacher
- parent
- staff
- owner/admin

Again, APP-LOCAL only.

==================================================
IMPORTANT GUARDRAILS
==================================================

1. Do NOT change database schema
2. Do NOT remove multi-actor support
3. Do NOT remove multi-app support
4. Do NOT let localStorage choose actor before engine app resolution
5. Do NOT let app hydration become authoritative over platform preferences
6. Do NOT resolve actor links outside the current app account
7. Keep fixes minimal and local to identity engine/app identity boundary

==================================================
SEARCH / FIX TARGETS
==================================================

You must verify and correct any of these if found:

BAD:
- `user_id` only → app context query
- `user_id` only → actor lookup
- global actor resolution before app resolution
- actor link lookup by `actor_id` without `user_app_account_id`
- preference write attempted before account-scoped actor link resolution
- localStorage actor restore before app-scoped engine context exists

GOOD:
- `user_id + app_key/app_id` → user app account
- `user_app_account_id` → preferences/state
- `user_app_account_id` → actor links
- active actor chosen only from those account-scoped links

==================================================
OUTPUT FORMAT
==================================================

Return exactly:

IDENTITY ENGINE APP-FIRST ALIGNMENT RESULT

1. Files inspected
2. Files modified
3. Root issue found
4. Whether app resolution now happens before actor resolution
5. Whether actor links are now strictly account-scoped
6. Whether VCSM citizen/vport separation is now app-local only
7. Whether Learning actor separation is now app-local only
8. Whether switch/login/restore now share the same order
9. Any remaining unsafe query paths
10. Final guaranteed resolution order

## Execution Plan

PLAN PROPOSAL
Task: Verify and enforce identity engine resolution order (app first, actor second)
Application Scope: VCSM + ENGINE
Estimated Time: 3 minutes
Execution Type: Direct
Files Expected To Change: TBD after audit

## Execution Summary

### Audit Result
The identity engine ALREADY implements the correct "app first, actor second" resolution order. No changes needed.

### Proof — resolveAuthenticatedContext.controller.js follows exact required order:

```
Step 1: resolveSessionUser()                              → userId
Step 2: dalGetAppByKey({ appKey })                        → app (by key, not user)
Step 3: resolveUserAppAccess({ userId, appId })           → access (user + APP scoped)
Step 4: resolveUserAppAccount({ userId, appKey })         → account (user + APP scoped)
Step 5: dalGetStateForAccount({ userAppAccountId })       → state (ACCOUNT scoped)
        dalGetPreferencesForAccount({ userAppAccountId }) → prefs (ACCOUNT scoped)
Step 6: resolveAvailableActors({ userAppAccountId })      → links (ACCOUNT scoped)
Step 7: resolveActiveActor({ availableActors, prefs })    → active (from ACCOUNT-scoped links only)
```

### Proof — No unsafe query patterns exist:

- `user_id` alone: ZERO occurrences. All 3 user_id queries are paired with app_id or app_key.
- `actor_id` alone: ZERO occurrences in engine DALs. Actor links always resolved by user_app_account_id.
- `.single()`: ZERO occurrences in entire engine.
- `.maybeSingle()`: 4 remaining, all query by PK (safe). The two that were unsafe were already fixed to `.limit(1)`.

### Proof — switchActiveActor also follows the order:

- Receives `userAppAccountId` + `actorLinkId` as inputs (already app-scoped by caller)
- Validates `row.user_app_account_id === userAppAccountId` before writing
- Never queries by user_id or actor_id alone

### Proof — VCSM app layer follows the order:

- switchActor: calls `resolveAuthenticatedContext({ appKey: 'vcsm' })` FIRST, then finds link in account-scoped results
- Login/restore: calls `loadDefaultIdentityForUser` → `resolveAuthenticatedContext({ appKey: 'vcsm' })` FIRST
- localStorage `savedActorId` is explicitly voided in identity.controller.js (`void savedActorId`)
- Hydration fallback only runs AFTER engine resolution, for display only — does not write preferences

### IDENTITY ENGINE APP-FIRST ALIGNMENT RESULT

1. Files inspected: resolveAuthenticatedContext.controller.js, switchActiveActor.controller.js, actorService.js, all 7 engine DALs, identityContext.jsx, identity.controller.js
2. Files modified: 0 — engine already correct
3. Root issue found: NONE — the engine already resolves app → account → actors in the correct order
4. Whether app resolution happens before actor resolution: YES (step 2-4 before step 6-7)
5. Whether actor links are strictly account-scoped: YES (by user_app_account_id)
6. Whether VCSM citizen/vport separation is app-local only: YES (handled by VCSM hydrator, not engine)
7. Whether Learning actor separation is app-local only: YES (handled by Wentrex app resolver via DI)
8. Whether switch/login/restore share the same order: YES (all call resolveAuthenticatedContext)
9. Any remaining unsafe query paths: NONE
10. Final guaranteed resolution order:
    AUTH USER → APP (by appKey) → ACCESS (userId + appId) → ACCOUNT (userId + appKey) → STATE/PREFS (accountId) → ACTOR LINKS (accountId) → ACTIVE ACTOR (from account-scoped links)

NOTE OF COMPLETITION

==================================================
SUCCESS CONDITION
==================================================

The change is successful only if the engine can guarantee:

APP FIRST
ACTOR SECOND

for login, restore, and actor switching.