# Planning — May / 14 / Sequence 01

---

## Execution Plan

**Task:** Feed Feature — BLOCKING FA1 Fix (modal stacking context) + HIGH SA2 Fix (mentions DAL engine import)  
**Date:** 2026-05-14  
**Application Scope:** VCSM  
**Task Class:** IMPLEMENTATION  

### Slice 1 — FA1: Move overlays outside PullToRefresh (BLOCKING)

**File:** `apps/VCSM/src/features/feed/screens/CentralFeedScreen.jsx`

Move `PostActionsMenu`, `ReportModal`, `ShareModal`, `FeedConfirmModal`, and `Toast` from inside `<PullToRefresh>` children to fragment siblings after `</PullToRefresh>`.

**Before:**
```jsx
<PullToRefresh ...>
  {/* feed content */}
  <PostActionsMenu ... />
  <ReportModal ... />
  <ShareModal ... />
  <FeedConfirmModal ... />
  <Toast ... />
</PullToRefresh>
```

**After:**
```jsx
<PullToRefresh ...>
  {/* feed content only */}
</PullToRefresh>
<PostActionsMenu ... />
<ReportModal ... />
<ShareModal ... />
<FeedConfirmModal ... />
<Toast ... />
```

---

### Slice 2 — SA2: Split mentions DAL from @hydration engine import (HIGH)

**Files changed:**
1. `apps/VCSM/src/features/feed/dal/feed.mentions.dal.js` — strip to raw DAL only (no @hydration import)
2. `apps/VCSM/src/features/feed/model/enrichMentionRows.model.js` — new pure enrichment transform
3. `apps/VCSM/src/features/feed/pipeline/fetchFeedPage.pipeline.js` — wire raw DAL + hydration + enrichment

**Flow change:**
- Before: `pipeline → fetchPostMentionRows (DAL+engine) → buildMentionMaps`
- After: `pipeline → fetchRawPostMentionEdgesDAL (DAL) + hydrateAndReturnSummaries (engine, pipeline level) → enrichMentionRows (model) → buildMentionMaps`

---

## Execution Summary

**Completed:** 2026-05-14

### Slice 1 — FA1 BLOCKING resolved

**File changed:** `apps/VCSM/src/features/feed/screens/CentralFeedScreen.jsx`

Moved `PostActionsMenu`, `ReportModal`, `ShareModal`, `FeedConfirmModal`, and `Toast` from inside `<PullToRefresh>` to fragment siblings after `</PullToRefresh>`. Debug panels (`DebugPrivacyPanel`, `DebugFeedFilterPanel`) remain inside — inline DEV-only panels, not position:fixed overlays.

iOS stacking context violation eliminated. `position:fixed` modals are no longer children of the `transform: translateY()` wrapper.

### Slice 2 — SA2 HIGH resolved

**Files changed:**
- `apps/VCSM/src/features/feed/dal/feed.mentions.dal.js` — stripped to raw DAL only; removed `@hydration` import; renamed `fetchPostMentionRows` → `fetchRawPostMentionEdgesDAL`; `readPostMentionRows` alias preserved; `console.warn` gated behind `import.meta.env.DEV`
- `apps/VCSM/src/features/feed/model/enrichMentionRows.model.js` — new pure model; `enrichMentionRows(rawEdges, presentations)` → enriched rows; no I/O, no side effects
- `apps/VCSM/src/features/feed/pipeline/fetchFeedPage.pipeline.js` — imports `fetchRawPostMentionEdgesDAL`; imports `hydrateAndReturnSummaries` from `@hydration` at pipeline level; imports `enrichMentionRows`; post-Promise.all enrichment step; `buildMentionMaps` call unchanged

Architecture layer boundary restored:
- Before: DAL → engine (violation)
- After: DAL (raw) → Pipeline (orchestration) → Model (pure) → Model (pure)

### SENTRY Review (Post-Execution): ALIGNED

| File | Layer | Result |
|---|---|---|
| `CentralFeedScreen.jsx` | Screen | iOS stacking context fix confirmed. No new violations. |
| `feed.mentions.dal.js` | DAL | Engine import removed. Raw Supabase only. Explicit column select. |
| `enrichMentionRows.model.js` | Model | Pure function. No I/O. No side effects. |
| `fetchFeedPage.pipeline.js` | Pipeline | `@hydration` at pipeline level — correct for aggregator layer. |

---

NOTE OF COMPLETITION
