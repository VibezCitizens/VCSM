# Planning — May / 26 / Sequence 01

> Session: 2026-05-26
> Task: VENOM F-007 (realm IDs to env vars) + F-008 (dual Supabase client doc)

---

## Execution Plan

**Task:** F-007 — Move PUBLIC_REALM_ID and VOID_REALM_ID to env vars with hardcoded fallback; F-008 — Document dual Supabase client (schema selector) pattern in DAL and ARCHITECT module map  
**Application Scope:** VCSM  
**Task Class:** MIXED (IMPLEMENTATION + DOCUMENTATION)  
**Estimated Time:** ~3 minutes  
**Execution Type:** Direct  
**SENTRY Review:** Required — Post-Execution  
**SENTRY Scope:** `apps/VCSM/src/shared/utils/resolveRealm.js`  
**SENTRY Timing:** Post-Execution  

### Slices

**Slice 1 — F-007:** Update `resolveRealm.js` to read `VITE_PUBLIC_REALM_ID` and `VITE_VOID_REALM_ID` from `import.meta.env` with hardcoded fallback values; create `apps/VCSM/.env.example` documenting both vars  
**Slice 2 — F-008:** Add inline comment to `vportFuelPricePost.read.dal.js` explaining dual-schema client pattern; add rationale note to `vcsm.vport-gas-prices.architecture.md`

### Files Expected To Change

1. `apps/VCSM/src/shared/utils/resolveRealm.js` — env var with fallback
2. `apps/VCSM/.env.example` — CREATE
3. `apps/VCSM/src/features/profiles/kinds/vport/dal/gas/vportFuelPricePost.read.dal.js` — dual client comment
4. `zNOTFORPRODUCTION/_CANONICAL/logan/marvel/architect/modules/vcsm.vport-gas-prices.architecture.md` — dual client rationale note

### Assumptions
- `vportClient.js` is `supabase.schema('vport')` — same underlying connection, not a separate Supabase project; F-008 doc clarifies this
- Env vars embed at build time (VITE_*) — UUIDs remain visible in bundle, but source control no longer hard-codes them and deployments can differ per environment
- .env.example is safe to commit — documents required vars, contains no secrets

### Blocking Risks
- None — both are P3 hardening/documentation tasks

---

## Execution Summary

**Completed:** 2026-05-26

### What was done

| Slice | Finding | File | Change |
|---|---|---|---|
| 1 | F-007 | `shared/utils/resolveRealm.js` | `PUBLIC_REALM_ID` and `VOID_REALM_ID` now read from `import.meta.env.VITE_*` with hardcoded fallback |
| 1 | F-007 | `apps/VCSM/.env.example` | CREATED — documents `VITE_PUBLIC_REALM_ID`, `VITE_VOID_REALM_ID` alongside Supabase vars |
| 2 | F-008 | `dal/gas/vportFuelPricePost.read.dal.js` | Inline comment added explaining dual-schema selector pattern |
| 2 | F-008 | `architect/modules/vcsm.vport-gas-prices.architecture.md` | Dual client section expanded with full rationale; resolved F-009 flag updated |

### Additional note
F-005 migration (`20260526010000_fuel_price_submissions_rls.sql`) confirmed deployed by user — RLS is live on `vport.fuel_price_submissions`.

### Why this way
- `??` fallback pattern — env var takes priority per deployment; defaults keep dev working without setup; matches `supabaseClient.js` error-guard pattern
- `.env.example` committed to source — standard convention, no secrets, informs onboarding
- F-008 fix is a comment only — no logic change; correct layer (DAL import block) so the explanation is visible where the confusion would arise

### SENTRY Review
- **SENTRY Status: ALIGNED**
- **SENTRY Report:** `audits/compliance/2026-05-26_sentry_vport-gas-prices-f007-f008.md`
- **SENTRY Reviewed Files:** `shared/utils/resolveRealm.js`
- **SENTRY Follow-Up:** NONE

### ALL VENOM GAS PRICES FINDINGS CLOSED

| ID | Status |
|---|---|
| F-001 through F-010 | ✅ All resolved |
| S-001 | ✅ Resolved |

NOTE OF COMPLETITION
