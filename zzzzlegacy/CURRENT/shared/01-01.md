Here’s a phased plan that lets you work in parallel without mixing responsibilities.

North star

Freeze the architecture around these rules:
	•	platform.* = identity/access truth
	•	learning.* = Wentrex domain truth
	•	old learning.communication_* = legacy, no new investment
	•	new work must follow DAL -> Controller -> Hook -> Screen
	•	realm validation and auth checks are mandatory in all write/read controllers
	•	old direct-Supabase screens are being replaced, not improved unless needed as a temporary blocker fix

⸻

Phase 0 — Freeze boundaries and create workstreams

Goal

Prevent more drift while parallel work starts.

Deliverables
	•	Mark old routes as legacy
	•	Mark new /learning/:realmSlug/* routes as canonical target
	•	Freeze learning.communication_* except critical break/fix only
	•	Create a single migration board with 4 tracks:
	•	Admin hardening
	•	Teacher migration
	•	Student migration
	•	Parent migration

Parallel owners
	•	Engineer A: Admin + auth hardening
	•	Engineer B: Teacher new-pattern completion
	•	Engineer C: Student new-pattern completion
	•	Engineer D: Parent new-pattern completion

Rules for all tracks
	•	No direct Supabase in screens/components
	•	No .select('*')
	•	Every controller touching realm-owned data validates realm_id
	•	Every create/update controller checks actor authorization explicitly
	•	Reuse DAL/controller code across roles where possible

⸻

Phase 1 — Security and correctness first

Goal

Fix the highest-risk issues before feature migration.

Priority 1 fixes

1. Add auth to admin write controllers

Fix:
	•	createOrganizationMember
	•	createParentMember

Required checks
	•	caller is allowed in current realm/org
	•	caller has admin/owner/staff capability as appropriate
	•	target org/course belongs to current realm
	•	actor relationships are valid before insert

2. Add realm validation to teacher controllers

Fix missing realm validation in:
	•	getTeacherDashboard
	•	listTeacherCourses

3. Kill student .select('*') violations

Fix StudentAssignmentScreen.jsx immediately, even if temporary.

4. Fix parent old-pattern data leak risk

Any old parent screen querying public.profiles directly must be replaced or blocked.

Parallelization
	•	Engineer A: Admin auth hardening
	•	Engineer B: Teacher realm validation
	•	Engineer C: Student .select('*') cleanup
	•	Engineer D: Parent leak-risk cleanup

Exit criteria
	•	zero missing auth checks in admin creation flows
	•	zero missing realm validation in active teacher controllers
	•	zero .select('*') in active student screens
	•	zero parent screens reading unsafe profile data directly

⸻

Phase 2 — Build minimum new-pattern parity

Goal

Make the new pattern usable enough to retire old routes dashboard by dashboard.

Teacher track

Build missing new-pattern features:
	•	assignment creation
	•	quiz builder replacement or temporary feature flag fallback
	•	submission filtering
	•	rubric scoring UI
	•	gradebook view

Teacher minimum parity target

Teacher can:
	•	view dashboard
	•	open course
	•	create assignment
	•	review submissions
	•	grade with rubric
	•	filter submissions
	•	see gradebook

Student track

Build missing new-pattern features:
	•	assignment view
	•	assignment submission
	•	lesson content viewer
	•	course enrollment modal replacement if still needed

Student minimum parity target

Student can:
	•	view dashboard
	•	open course
	•	open lesson
	•	submit assignment
	•	track progress

Parent track

Build missing new-pattern features:
	•	settings/profile screen
	•	child password reset flow if still required
	•	parent enrollment replacement if still needed

Parent minimum parity target

Parent can:
	•	view dashboard
	•	open linked child progress
	•	open course detail
	•	manage settings
	•	perform required account actions

Admin track

Fix remaining alignment issues:
	•	courses list screen moved to proper pattern
	•	staff management screen moved to proper pattern
	•	sidebar nav includes Courses + Staff
	•	observer link query moved out of controller inline query into DAL

Parallelization
	•	Engineer A: Admin parity
	•	Engineer B: Teacher parity
	•	Engineer C: Student parity
	•	Engineer D: Parent parity

Exit criteria

Each dashboard has a new-pattern route set that covers all critical daily workflows.

⸻

Phase 3 — Shared consolidation layer

Goal

Stop duplicating DAL/model/controller logic across administration, teacher, student, and parent.

Work

Create or consolidate shared canonical domains for:
	•	courses
	•	assignments
	•	submissions
	•	grades
	•	lesson progress
	•	rubrics
	•	membership/authorization lookups

Specific cleanup
	•	deduplicate duplicate DAL files across administration/, student/, staff/
	•	deduplicate duplicate models
	•	normalize imports to canonical paths
	•	remove brittle import-path assumptions

Recommended target structure

Use canonical shared locations like:
	•	learning/shared/dal/...
	•	learning/shared/model/...
	•	learning/shared/controller/...

App-role-specific behavior stays in role controllers, but shared data access/mapping should be singular.

Parallelization

This can run alongside late Phase 2 once feature parity is stable:
	•	Engineer A: shared DAL consolidation
	•	Engineer B: shared models
	•	Engineer C: import normalization
	•	Engineer D: regression coverage

Exit criteria
	•	one canonical DAL per domain action
	•	one canonical model per entity
	•	no duplicated logic across role folders unless behavior truly differs

⸻

Phase 4 — Route cutover

Goal

Make new-pattern routes the only active routes.

Work
	•	identify old /teacher, /student, /parent routes still active
	•	map each to new /learning/:realmSlug/* equivalent
	•	feature-flag cutover if needed
	•	remove links to old routes
	•	redirect old routes to new routes where safe

Dashboard-by-dashboard cutover order
	1.	Admin
	2.	Student
	3.	Parent
	4.	Teacher

Admin first because it is already closest to aligned. Teacher last because it has the largest parity gap.

Exit criteria
	•	no user-facing navigation to legacy screens
	•	old routes redirected or removed
	•	new-pattern routes are canonical

⸻

Phase 5 — Legacy communication cleanup

Goal

Once dashboard migration is stable, stop carrying learning.communication_* as an active path.

Work
	•	confirm Wentrex chat uses only neutral chat engine path
	•	ensure no dashboard depends on learning.communication_*
	•	remove or quarantine old communication code
	•	keep migration notes for reference only

Exit criteria
	•	Wentrex communication uses neutral engine path only
	•	learning.communication_* is legacy-only and not used by active screens

⸻

Phase 6 — Final hardening

Goal

Prevent regression.

Add guardrails

Lint / grep / CI checks

Block:
	•	supabase queries inside screen files
	•	.select('*')
	•	direct writes in screens/components
	•	missing realm_id validation in realm-owned controllers
	•	imports from deprecated DAL locations
	•	use of old legacy route modules

Architectural checks
	•	every controller must declare auth assumptions
	•	every DAL must use explicit columns
	•	every screen must consume hooks, not raw data clients

Regression tests
	•	admin member creation auth
	•	teacher dashboard realm isolation
	•	student assignment view/submission
	•	parent linked-child authorization
	•	old-route redirect behavior

⸻

Suggested parallel execution by sprint

Sprint 1
	•	Phase 0
	•	Phase 1

Sprint 2
	•	Phase 2 for Admin + Student
	•	Teacher and Parent scaffolding

Sprint 3
	•	Phase 2 for Teacher + Parent
	•	start Phase 3 consolidation

Sprint 4
	•	finish Phase 3
	•	Phase 4 cutover

Sprint 5
	•	Phase 5 legacy cleanup
	•	Phase 6 hardening

⸻

Immediate next-week plan

Track A — Admin hardening
	•	add auth to createOrganizationMember
	•	add auth to createParentMember
	•	move cross-org courses/staff list screens to proper DAL/controller pattern
	•	add sidebar nav entries

Track B — Teacher foundation
	•	add realm validation to teacher controllers
	•	build assignment creation in new pattern
	•	add submission filtering
	•	add rubric scoring surface

Track C — Student foundation
	•	replace old assignment screen with new-pattern assignment view
	•	add assignment submit flow
	•	add lesson viewer
	•	remove .select('*') violations immediately

Track D — Parent foundation
	•	build new settings/profile screen
	•	normalize observer/parent relationship naming strategy
	•	migrate remaining parent old-pattern actions
	•	remove unsafe direct profile access

⸻

Recommended sequencing decision

If you want the fastest visible stabilization:
	1.	Admin security fixes
	2.	Student new assignment flow
	3.	Teacher realm validation + assignment creation
	4.	Parent settings migration
	5.	Shared consolidation
	6.	Legacy route cutover

That order reduces risk and unlocks actual deprecation of old routes.

---

## Execution Plan

**Why splitting:** This file is a multi-phase roadmap. Total estimated time exceeds 4 minutes. Executing Phase 0 + Phase 1 today as Sprint 1.

**Execution order for today:**

1. **Slice 1 — Phase 0: Mark legacy routes + freeze boundaries** (~2 min)
   - Add comments marking old `/teacher`, `/student`, `/parent` App.jsx routes as legacy
   - Confirm `/learning/:realmSlug/*` routes are canonical
   - No code logic changes, annotation only

2. **Slice 2 — Phase 1.1: Add auth to admin write controllers** (~4 min)
   - Fix `createOrganizationMemberController` — add realm/org/actor authorization
   - Fix `createParentMemberController` — add realm/course management checks

3. **Slice 3 — Phase 1.2: Add realm validation to teacher controllers** (~3 min)
   - Fix `getTeacherDashboard` — add realm_id validation on loaded courses
   - Fix `listTeacherCourses` — add realm_id validation on loaded courses

4. **Slice 4 — Phase 1.3: Fix student .select('*') violations** (~2 min)
   - Replace 4x `.select('*')` in `StudentAssignmentScreen.jsx` with explicit columns

5. **Slice 5 — Phase 1.4: Fix parent old-pattern data leak** (~2 min)
   - Remove `public.profiles` query from `ParentClassScreen.jsx`

**Starting with Slice 1.**

---

## Execution Summary

All 5 slices completed. Phase 0 + Phase 1 (Sprint 1) done.

**Slice 1 — Legacy route annotations:**
- Annotated all App.jsx routes with `[LEGACY]` comments grouping admin, teacher, parent, student routes
- Marked `/learning/:realmSlug/*` as canonical target
- File: `apps/wentrex/src/App.jsx`

**Slice 2 — Admin auth hardening:**
- `createOrganizationMemberController`: Added realm validation (org must exist in realm), authorization check (`canManageOrganization`), platform admin bypass
- `createParentMemberController`: Added realm validation (org + course must exist in realm), authorization check, platform admin bypass
- Files: `controller/admin/createOrganizationMember.controller.js`, `controller/admin/createParentMember.controller.js`

**Slice 3 — Teacher realm validation:**
- `getTeacherDashboardController`: Made `realmId` required (returns REALM_REQUIRED if missing)
- `listTeacherCoursesController`: Same fix
- DAL already filters by realmId; this prevents null-realm bypass
- Files: `staff/teacher/controller/getTeacherDashboard.controller.js`, `staff/teacher/controller/listTeacherCourses.controller.js`

**Slice 4 — Student .select('*') fix:**
- Replaced 4x `.select('*')` in `StudentAssignmentScreen.jsx` with explicit column lists for assignments, assignment_resources, submissions, grades
- File: `learning/student/screens/StudentAssignmentScreen.jsx`

**Slice 5 — Parent data leak fix:**
- Removed `public.profiles` fallback query from `ParentClassScreen.jsx`
- Teacher name now resolved only from `learning.actor_profiles`
- File: `learning/parent/screens/ParentClassScreen.jsx`

**Build:** Passes cleanly. Zero errors.

**Exit criteria met:**
- Zero missing auth checks in admin creation flows
- Zero missing realm validation in active teacher controllers
- Zero .select('*') in active student screens
- Zero parent screens reading unsafe profile data directly

NOTE OF COMPLETITION
