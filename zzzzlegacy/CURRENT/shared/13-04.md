# Planning — April / 13 / Sequence 04

> Awaiting tasks.

## Execution Plan

Task: Vport migration drift cluster — P0/P1/P2 fixes
Application Scope: VCSM
Task Class: MIXED (IMPLEMENTATION + DB_REVIEW)
Execution Type: Split (2 slices)

Slice 1 — Code (no DB approval needed):
  - Item 1: readVportIdentityDAL ← ALREADY DONE, skip
  - Item 4: vcsmActorHydrator.js — add owner_user_id fallback when vc.actor_owners row is missing

Slice 2 — DB (each item requires explicit SQL approval):
  - Item 2: vport.create_vport_for_actor — add INSERT into vport.profile_categories
  - Item 3: identity.refresh_actor_directory_row — remove vc.vports reference
  - Item 5: vport.profile_categories INSERT RLS policy
  - Item 6: vport_rates 404
  - Item 7: service_catalog 400
  - Items 8-10: restaurant/barber endpoints — P2, deferred

Files Expected To Change (Slice 1):
  - apps/VCSM/src/features/hydration/vcsmActorHydrator.js

SENTRY Review: Optional — Post-Execution
Blocking Risks: DB items need per-item SQL approval

## Execution Summary

Slice 1 complete.

Item 1 — readVportIdentityDAL: Already fixed earlier in session. Skipped.

Item 4 — vcsmActorHydrator.js:
- Added ownerUserId fallback: ownerRow?.user_id ?? vport?.owner_user_id ?? null
- vport.owner_user_id already returned by readVportIdentityDAL from vport.profiles
- Pre-migration vport actors missing vc.actor_owners rows now resolve ownerActorId correctly
- Zero breaking change — additive fallback only

Also fixed this run:
- All command paths updated NOTFORPRODUCTION → zNOTFORPRODUCTION after folder rename
- vite.config.js @debuggers alias updated to zNOTFORPRODUCTION

Files changed:
- apps/VCSM/src/features/hydration/vcsmActorHydrator.js
- apps/VCSM/vite.config.js
- .claude/commands/ — Wolverine, BUGSBUNNY, CAPTAIN, Logan, session-summary, BATMAN, Thor, review-contract, Sentry, listofcomand
- apps/wentrex/REVIEW.md
- apps/Traffic/REVIEW.md

Slice 2 — pending DB approval per item.

NOTE OF COMPLETITION
