# Planning — May / 03 / Sequence 12

> Execution in progress.

## Execution Plan

Task: Implement hashtag post search for Vibe posts in Explore
Application Scope: VCSM
Task Class: IMPLEMENTATION
Estimated Time: ~20 min
Execution Type: Split / 3 Slices
Execution Status: COMPLETE

Slices:
  Slice 1 — DAL: add searchPosts(), replace stub cases in searchDal()
  Slice 2 — New UI: PostSearchResultRow.jsx + wire into ResultList.jsx
  Slice 3 — FeaturedResultCard.jsx: add post type handling + final verify

Constraints: No DB schema changes. Read code before writing. No debug probes left unguarded.

Files Expected To Change:
  - apps/VCSM/src/features/explore/dal/search.dal.js
  - apps/VCSM/src/features/explore/ui/PostSearchResultRow.jsx (NEW)
  - apps/VCSM/src/features/explore/ui/ResultList.jsx
  - apps/VCSM/src/features/explore/ui/FeaturedResultCard.jsx

Specialist Commands Required: None
Relevant Logan Docs: None identified
Documentation Mode: None (no Logan docs exist for explore search)
SENTRY Review: Optional
SENTRY Scope: apps/VCSM/src/features/explore/
SENTRY Timing: Post-Execution

Assumptions:
  - vc.posts.tags is text[] confirmed from prior audit
  - PostgREST cs operator (array contains) works on text[] with {tag} literal format
  - /posts/:id route exists for navigation
  - explore-result-row / explore-result-pill / explore-result-avatar CSS classes exist (confirmed from ActorSearchResultRow)

Blocking Risks: None

Root Cause (confirmed from audit):
  case 'posts': return [Promise.resolve([])]  — search.dal.js:63-64
  The posts tab stub was never replaced with a real query.
  vc.posts.tags is text[], no DB schema change required.

---

## Execution Summary

### Slice 1 — DAL (search.dal.js)

Added `searchPosts(rawQuery, opts)`:
- Two parallel queries: `byText` (ilike on post.text) + `byTag` (contains on tags[])
- Uses `makeBase()` factory to avoid mutating shared builder state
- Dedupes by post id via Map
- Returns `result_type: 'post'` shape
- DEV logs: `[explore:search:posts:start]`, `[explore:search:posts:result]`, `[explore:search:posts:empty]`, error variants
- `is_hidden` handled via `.or('is_hidden.is.null,is_hidden.eq.false')` — NULL-safe

Replaced stubs:
- `case 'posts'`: now calls `searchPosts(query, opts)`
- `case 'all'`: second slot now calls `searchPosts(query, opts)` (was `Promise.resolve([])`)

### Slice 2 — PostSearchResultRow.jsx (NEW) + ResultList.jsx

PostSearchResultRow:
- Navigates to `/posts/${post.id}`
- Hash icon + preview (title || text.slice(80)) + first tag as subtitle
- Uses existing `explore-result-row`, `explore-result-pill`, `explore-result-avatar` CSS classes
- "Vibe" pill label

ResultList:
- Imported PostSearchResultRow
- Added `result_type === 'post'` branch rendering PostSearchResultRow

### Slice 3 — FeaturedResultCard.jsx

Added post type block (between actor and feature blocks):
- Purple gradient background (matches feature card style)
- Shows "Featured Vibe" label + preview + first tag
- Navigates to `/posts/${item.id}`
- Prevents null gap when a post is the first result

### Files Changed

- apps/VCSM/src/features/explore/dal/search.dal.js
- apps/VCSM/src/features/explore/ui/PostSearchResultRow.jsx (NEW)
- apps/VCSM/src/features/explore/ui/ResultList.jsx
- apps/VCSM/src/features/explore/ui/FeaturedResultCard.jsx

### Before / After Query Behavior

Before:
  Search "California" on Posts tab → Promise.resolve([]) → EmptyState always
  Search "California" on All tab → actors only, posts always empty

After:
  Search "California" on Posts tab → `vc.posts` where text ilike "%california%" OR tags ∋ "california"
  Search "#California" → normalized to "california", same query
  Search on All tab → actors + posts merged, deduped, ordered by created_at

### Test Checklist

  a. Posts tab: type "california" → posts with tag "california" appear → should now work
  b. Posts tab: type "#california" → same results (# stripped in normalization) → should work
  c. Posts tab: type "California" → text ilike match fires (case-insensitive) → should work
  d. Posts tab: type query with no matches → EmptyState shown → unchanged behavior
  e. All tab: mixed actors + posts return together → should now show both
  f. Post row navigates to /posts/:id → should work (route assumed present)
  g. Post as first result in All → FeaturedResultCard renders "Featured Vibe" not null → fixed
  h. Vibes/Districts tabs (videos/groups) → still EmptyState (stubs remain) → unchanged
  i. Citizens/VPORTs tabs → unaffected, actors-only flow unchanged → should still work
  j. DEV logs visible in browser console when searching posts → only in DEV mode

SENTRY Review: Optional / Post-Execution / scoped to apps/VCSM/src/features/explore/
Documentation Drift: No Logan docs exist for explore search — no update required.

NOTE OF COMPLETITION

