# Planning — May / 27 / Sequence 05

> Task: Remaining VENOM + ARCHITECT fixes — V-006, V-007, V-008, useDesktopBreakpoint dedup; then SPIDER-MAN + LOKI

## Execution Plan

Task Class: MIXED (IMPLEMENTATION + DOCUMENTATION)
Scope: VCSM
Files changed: 7 (1 new)
SENTRY: Required — Post-Execution

---

## Execution Summary

### What was completed

**V-006 (P2 — LOW) — UUID guard at lib layer — RESOLVED**
`lib/qrUrlBuilders.js` — Added exported `isQrSafeSlug(slug)` utility (single UUID_RE declaration,
shared across the codebase). Applied UUID guard to `buildMenuQrUrl`, `buildReviewsQrUrl`, and
`buildBusinessCardQrUrl` — each returns `""` if slug is absent or a raw UUID. Defense-in-depth:
UUID protection is now enforced at both the lib layer and view layer simultaneously.

**V-008 (P2 — LOW) — Flyer viewer ownership gate — RESOLVED**
`VportActorMenuFlyerScreen.jsx` — Added `useIdentity` + `useVportOwnership` gate mirroring the
exact pattern from the sibling `VportActorMenuFlyerEditorScreen`. Auth states: loading skeleton,
"Sign in required", "You can only view flyers for your own vport." No route changes needed.

**V-007 (P3 — LOW) — Oracle disclosure — ACCEPTED, DOCUMENTED**
No code change. Decision annotation added to VENOM audit finding V-007:
`resolveMenuSlugDAL` vs `resolveVportSlugDAL` differentiation is intentional product behavior.
The disclosed information (whether a vport has active menu items) is observable by direct
inspection and low-sensitivity. Accepted by design.

**useDesktopBreakpoint dedup — RESOLVED**
NEW `shared/hooks/useDesktopBreakpoint.js` — canonical implementation.
Three prior feature copies converted to thin re-exports (one line each). All 14+ existing
dashboard screen consumers keep their import paths unchanged — zero import refactor needed.

### Build result
✓ 7260 modules transformed — zero errors.

### SENTRY Status: ALIGNED
SENTRY Reviewed Files:
  - apps/VCSM/src/lib/qrUrlBuilders.js — ALIGNED
  - apps/VCSM/src/features/dashboard/flyerBuilder/screens/VportActorMenuFlyerScreen.jsx — ALIGNED
  - apps/VCSM/src/shared/hooks/useDesktopBreakpoint.js — ALIGNED
  - apps/VCSM/src/features/public/vportMenu/hooks/useDesktopBreakpoint.js — ALIGNED
  - apps/VCSM/src/features/dashboard/vport/screens/useDesktopBreakpoint.js — ALIGNED
  - apps/VCSM/src/features/ads/hooks/useDesktopBreakpoint.js — ALIGNED
SENTRY Follow-Up: None

---

### SPIDER-MAN — Coverage Report (post-session)

Overall: HIGH RISK — TESTS REQUIRED

Module has zero regression coverage. Every VENOM remediation fix is live with no test lock.

Top 3 priorities:
1. `qrUrlBuilders.js` — isQrSafeSlug + UUID builder guards (pure functions, 10 min)
2. `listQrLinks.controller.js` — requestActorId auth gate regression (VENOM V-002)
3. `VportActorMenuFlyerScreen.jsx` — ownership gate behavioral test (VENOM V-008)

Full report: zNOTFORPRODUCTION/CURRENT/features/dashboard/evidence/2026-05-27_00-00_spiderman_vport-booking-security-remediation.md

---

### LOKI — Runtime Trace Report

Overall posture: WATCH

No N+1 risks. No runaway loops.

Key findings:
- LF-001 (MEDIUM): getVportProfileIdByActorIdDAL silent null — DB failure indistinguishable from "no profile" → useQrLinks silently returns [] with no error state
- LF-002 (LOW): useVportOwnership fires fresh DB reads on every window focus with no debounce (flyer viewer)
- LF-003 (MEDIUM): checkVportOwnership swallows all errors — DB outage looks like "not owner"
- LF-004 (MEDIUM): useVportPublicDetails has no monitoring on public route
- LF-006 (MEDIUM): Delegated QR list = 5 serial DB reads (~750ms at 150ms/query)

Full report: zNOTFORPRODUCTION/CURRENT/features/dashboard/evidence/2026-05-27_00-00_loki_vport-menu-qr-security-remediation.md

Handoff: LF-001, LF-003 → DEADPOOL; LF-002, LF-006 → KRAVEN; LF-004, LF-007 → SENTRY

---

### VENOM Module Security Posture — Final State

All 8 VENOM findings resolved or accepted:

| ID | Severity | Status |
|---|---|---|
| V-001 | HIGH | ✅ RESOLVED — 27-03 |
| V-002 | MEDIUM | ✅ RESOLVED — 27-04 |
| V-003 | MEDIUM | ✅ RESOLVED — 27-03/27-04 |
| V-004 | MEDIUM | ✅ RESOLVED — 27-03 |
| V-005 | MEDIUM | ✅ RESOLVED — 27-03 |
| V-006 | LOW | ✅ RESOLVED — 27-05 |
| V-007 | LOW | ✅ ACCEPTED — 27-05 |
| V-008 | LOW | ✅ RESOLVED — 27-05 |

Module security posture: CLEAN (0 open findings)

### Change Intent Registry
- Entry written: NO
- Files logged: N/A — no deletions, moves, or renames occurred

---

NOTE OF COMPLETITION
