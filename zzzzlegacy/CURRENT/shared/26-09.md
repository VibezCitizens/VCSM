# Planning — May / 26 / Sequence 09

## Task
Fix P0/P1/P2 architectural issues in VPORT Reviews QR, Gas Station Cards, and Accessibility

## Application Scope
VCSM

## Task Class
IMPLEMENTATION

## SENTRY Review
Required — Post-Execution
SENTRY Scope: all changed files

---

## Execution Plan

### Priority 1 — Reviews QR
- [x] Create `src/lib/qrUrlBuilders.js`
- [x] Fix `VportPublicReviewsQrView.jsx` — gate QR behind slug, use builder, use shared QrCode
- [x] Fix `VisibleQRCode.jsx` — replace direct react-qr-code, add aria-label
- [x] Fix `VportsQrModal.jsx` — replace hardcoded domain
- [x] Fix `BookingQrLinksPanel.jsx` — migrate to actorId interface, add TODO

### Priority 2 — Gas Station Cards
- [x] Create `GasUnitToggleBar.jsx`
- [x] Fix `VportDashboardGasScreen.jsx` — use GasUnitToggleBar, use hook pendingSubmissions
- [x] Fix `VportDashboardGasPanels.jsx` — remove identityLoading no-op, add isOwner
- [x] Fix `GasPricesPanel.jsx` — harden allowOwnerUpdate gate + isOwner, add a11y

---

## Execution Summary

### Files Created (2)
- `apps/VCSM/src/lib/qrUrlBuilders.js` — centralized QR URL builder
- `apps/VCSM/src/features/dashboard/vport/screens/components/GasUnitToggleBar.jsx` — extracted unit toggle component

### Files Modified (7)
- `VportPublicReviewsQrView.jsx` — P0 UUID gate, shared QrCode, buildReviewsQrUrl, loading skeleton, aria-label
- `VisibleQRCode.jsx` — replaced react-qr-code direct import with shared QrCode, aria-label
- `VportsQrModal.jsx` — replaced all 3 hardcoded domain strings with buildBusinessCardQrUrl, aria-labels
- `BookingQrLinksPanel.jsx` — migrated to actorId prop, banned profileId/organizationId from boundary, TODO adapter note
- `VportDashboardGasScreen.jsx` — replaced inline unit toggle with GasUnitToggleBar, uses hook pendingSubmissions directly
- `VportDashboardGasPanels.jsx` — removed no-op identityLoading GasStates, wired isOwner to GasPricesPanel
- `GasPricesPanel.jsx` — showUpdateButton requires allowOwnerUpdate && isOwner, canShareToFeed hardened, aria-label on fuel rows and update button

### Issues Not Fixed (and Why)

1. VportPublicMenuQrView.jsx — same UUID leak as reviews QR (`/m/${actorId}` fallback).
   NOT fixed: not in explicit task scope ("reviews qr" only). Flagged for Wolverine follow-up.

2. PosterFlyer.jsx, ClassicFlyer.jsx, PrintableQrFlyerCard.jsx — direct react-qr-code imports.
   NOT fixed: all three are inside or adjacent to the qrcode feature; lower priority.
   QrCode.jsx (canonical) correctly imports react-qr-code — these are one level removed.
   Flagged for P3 cleanup sprint.

3. Service review FK / in-memory filtering — requires @reviews engine schema change.
   NOT touched: out of scope (CARNAGE territory). TODO in VportServiceReviews.controller.js remains.

4. useVportReviews split — not required by any current fix. Deferred.

5. Rate limiting on review submit — no app-layer infra; needs Venom plan first.

### Risky Assumptions

- GasPricesPanel: `isOwner` defaults to false. Public gas view passes `isStationOwner` only (no `isOwner`).
  Verified: `VportGasPricesView` passes `isStationOwner={isOwner}` — never `allowOwnerUpdate`.
  Citizen path (`allowOwnerUpdate=false, isOwner=false, isStationOwner=false`) → showUpdateButton = true. ✅ Correct.

- BookingQrLinksPanel: enabled=false disables QR loading entirely until adapter is built.
  This is intentional — better empty state than identity contract violation.
  The component has zero external callers so no user-visible regression.

- VportPublicReviewsQrView: loading skeleton uses inline CSS animation (@keyframes spin).
  No external CSS dependency. Self-contained.

### Validation Results
- All 9 files: ≤ 300 lines ✅
- No TypeScript files created ✅
- No relative ../../ imports ✅
- No select('*') ✅
- No vibezcitizens.com hardcoded in qrUrlBuilders ✅
- react-qr-code direct imports removed from VisibleQRCode + VportPublicReviewsQrView ✅
- identityLoading removed from GasPanels JSX (only in comment) ✅
- No cross-root changes ✅

NOTE OF COMPLETITION
