# Planning — April / 12 / Sequence 16

## Task

Continue notification migration — next slice after read-path migration.

### Source

Promoted from `.tp-active.md`.

## Execution Plan

**Application Scope:** VCSM

**Task Class:** IMPLEMENTATION

### Slices

1. **Header controller** — Migrate to engine `countUnread()` + `getInboxNotifications()` with autoMarkSeen
2. **Realtime subscription** — Switch from `vc.notifications` to `notification.inbox_items` + `notification.recipients`
3. **Orphan cleanup** — Remove 3 orphaned legacy DAL files

## Execution Summary

### What Was Completed

All 3 slices executed. Header controller, realtime subscription, and orphaned DALs all migrated.

**Slice 1 — Header controller:**
Updated `NotificationsHeader.controller.js`:
- `loadNotificationHeader()` now calls engine `countUnread({ recipientActorId })` instead of legacy `countUnreadNotifications` DAL
- `markAllNotificationsSeen()` now calls engine `getInboxNotifications()` with `autoMarkSeen: true` (marks up to 50 items) + dispatches `noti:refresh` event for badge update
- Both wrapped in try/catch for resilience

**Slice 2 — Realtime subscription:**
Updated `badgeSubscriptions.js` → `subscribeNotificationBadge()`:
- Changed from `schema: 'vc', table: 'notifications'` to:
  - `schema: 'notification', table: 'inbox_items'` (INSERT + UPDATE for state transitions)
  - `schema: 'notification', table: 'recipients'` (INSERT with actor filter for new deliveries)
- Removed DELETE listener (engine uses soft-delete via `is_dismissed`/`archived_at`)

**Slice 3 — Orphan cleanup:**
Removed 3 orphaned legacy DAL files:
- `notifications.dal.js` — `dalMarkNotificationsSeen()`, no importers
- `notifications.read.dal.js` — `fetchNotificationsPage()`, no importers
- `notifications.count.dal.js` — `countUnreadNotifications()`, no importers after header migration

Kept `notifications.write.dal.js` — still imported by `dev/diagnostics/groups/notificationsFeature.group.js`.

### Files Changed

| File | Status | Change |
|------|--------|--------|
| `apps/VCSM/src/features/notifications/inbox/controller/NotificationsHeader.controller.js` | MODIFIED | Switched to engine APIs |
| `apps/VCSM/src/features/notifications/inbox/realtime/badgeSubscriptions.js` | MODIFIED | Switched to notification.* schema |
| `apps/VCSM/src/features/notifications/inbox/dal/notifications.dal.js` | DELETED | Orphaned |
| `apps/VCSM/src/features/notifications/inbox/dal/notifications.read.dal.js` | DELETED | Orphaned |
| `apps/VCSM/src/features/notifications/inbox/dal/notifications.count.dal.js` | DELETED | Orphaned |

### What Is Now Fully Migrated

- Inbox fetch (read path) → engine `getInboxNotifications()`
- Badge count (both hooks) → engine `countUnread()`
- Header count + mark-all-seen → engine `countUnread()` + `getInboxNotifications(autoMarkSeen)`
- Realtime subscription → `notification.inbox_items` + `notification.recipients`
- Engine DI wiring → `setup.js` + `main.jsx`

### What Still Remains Legacy

| Area | File | Dependency |
|------|------|------------|
| Write DAL (mark-read) | `dal/notifications.write.dal.js` | Dev diagnostics only |
| Creation DAL | `dal/notifications.create.dal.js` | 4 booking + 1 review controller |
| Inbox unread DAL | `dal/inboxUnreadCount.dal.js` | Verify if orphaned |
| Sender resolution | `lib/resolveSenders.js` | May query `vc.*` for actor data |
| DB triggers | Database triggers on `vc.*` tables | Write path — separate migration |

### Next Notification Step

Migrate the **write path**: replace `dalInsertNotification()` calls in booking/review controllers with engine `publishEvent()`. This is the largest remaining slice and requires `resolveRecipients` DI injection.

### SENTRY Review

- SENTRY Status: ALIGNED
- SENTRY Reviewed Files:
  - `NotificationsHeader.controller.js` — ALIGNED (controller calls engine, not DAL)
  - `badgeSubscriptions.js` — ALIGNED (realtime on correct schema)
- SENTRY Follow-Up: None required.

NOTE OF COMPLETITION
