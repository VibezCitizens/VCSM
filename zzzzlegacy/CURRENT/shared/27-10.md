# Planning — May / 27 / Sequence 10

> Task: Close remaining ELEKTRA INFO + BW-001 sibling-controller follow-ups

## Execution Plan

Task Class: IMPLEMENTATION
Scope: ENGINE
Files changed: 3 production files
SENTRY: Required — Post-Execution

---

## Execution Summary

### What was completed

**ELEK-007 — Engine config.js one-call guard + Object.freeze**
- File: `engines/booking/src/config.js`
- Added module-level `_frozen = false` flag
- `configureBookingEngine()` now throws if called a second time:
  `'[BookingEngine] already configured — configureBookingEngine may only be called once per process.'`
- Config object frozen with `Object.freeze()` after first successful call
- Prevents any rogue module loaded after app startup from swapping the active Supabase clients
- App-layer `_configured` guard in `setup.js` was already present; this adds the equivalent protection at the engine layer

**BW-001 follow-up — `confirmBooking`: terminal status guard + UUID linkPath fix**
- File: `engines/booking/src/controller/confirmBooking.controller.js`
- Added `TERMINAL_STATUSES = new Set(['cancelled', 'completed', 'no_show'])` guard after booking fetch
- Throws `'This booking has already been finalized and cannot be confirmed.'` before resource lookup or write
- Added `dalGetVportProfileSlugByActorId` import
- Replaced `linkPath: /profile/${resource.owner_actor_id}?tab=book` (raw UUID) with slug resolution:
  `linkPath: ownerSlug ? /profile/${ownerSlug}?tab=book : undefined`
- Pattern matches `cancelBooking` post-fix implementation

**BW-001 follow-up — `completeBooking`: missing `no_show` guard + UUID linkPath fix**
- File: `engines/booking/src/controller/completeBooking.controller.js`
- Existing guards covered `completed` and `cancelled`; `no_show` was missing
- Added: `if (booking.status === 'no_show') throw new Error('Cannot complete a no-show booking.')`
- Added `dalGetVportProfileSlugByActorId` import
- Replaced `linkPath: /profile/${resource.owner_actor_id}?tab=book` (raw UUID) with slug resolution
- All three terminal statuses now fully covered in `completeBooking`

---

### Deferred (requires non-code action)

**ELEK-004 (LOW) — QR scan count non-atomic increment**
- Requires a DB-side atomic RPC: `UPDATE vport.qr_links SET scan_count = scan_count + 1 WHERE id = p_id`
- Cannot be addressed with application code alone
- Assigned to: Carnage (migration planning) + DB (confirm atomic pattern)

**BW-008-INFO — vportLeads delegation policy**
- Whether delegated org managers should access vportLeads is a product decision
- Assigned to: IRONMAN (feature ownership confirmation)

---

### Build result
✓ 7261 modules transformed — zero errors

### Test result
✓ 35/35 tests passing across 5 test files — no regressions

---

### Files changed

| File | Change |
|---|---|
| `engines/booking/src/config.js` | ELEK-007: one-call guard (`_frozen`) + `Object.freeze()` after first configuration |
| `engines/booking/src/controller/confirmBooking.controller.js` | BW-001 follow-up: terminal status guard; UUID linkPath → slug resolution |
| `engines/booking/src/controller/completeBooking.controller.js` | BW-001 follow-up: no_show guard added; UUID linkPath → slug resolution |

---

### SENTRY Status: ALIGNED

SENTRY Reviewed Files:
  - `engines/booking/src/config.js` — ALIGNED (module-level flag, not business logic; freeze applied after spread merge so all keys captured before freezing)
  - `engines/booking/src/controller/confirmBooking.controller.js` — ALIGNED (guard placed before resource lookup and write; DAL import from correct actor DAL layer; slug pattern consistent with cancelBooking)
  - `engines/booking/src/controller/completeBooking.controller.js` — ALIGNED (guard placed before resource lookup and write; no_show added consistently with existing individual-status check style; slug pattern consistent)
SENTRY Follow-Up: None

---

### Carnage Handoff — ELEK-004

CARNAGE TASK (pending scheduling):
- Finding: ELEK-2026-05-27-004
- File: `engines/booking/src/dal/qrLink.write.dal.js`
- Current: `dalIncrementQrScanCountRaw` — read-modify-write in application code
- Required: DB-side atomic increment RPC:
  `UPDATE vport.qr_links SET scan_count = scan_count + 1 WHERE id = p_id`
- After Carnage creates the RPC, replace `dalIncrementQrScanCountRaw` with:
  `await getVportClient().rpc('increment_qr_scan_count', { p_id: qrLinkId })`

---

### IRONMAN Handoff — BW-008

IRONMAN TASK (pending scheduling):
- Finding: BW-2026-05-27-008
- File: `apps/VCSM/src/features/dashboard/vport/controller/vportLeads.controller.js`
- Question: Should vportLeads be accessible to delegated org/location managers?
  - Current: uses `assertActorOwnsVportActorController` — owner-only, no delegation
  - If yes: route through `assertActorCanManageResource` instead
  - If no: add code comment documenting owner-only as intentional policy

---

### Change Intent Registry
- Entry written: NO
- Files logged: N/A — no deletions, moves, or renames occurred

---

NOTE OF COMPLETITION
