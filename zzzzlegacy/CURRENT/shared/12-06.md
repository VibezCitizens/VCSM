# Planning — April / 12 / Sequence 06

## Task

Backend Query Optimization — Fix N+1 patterns in learning controllers + optimize remaining high-impact query paths.

### Source

Promoted from `.tp-active.md` (reframed from TypeScript to JavaScript).

### Investigation Results

The profiler shows 976 queries / 58 N+1 / 128 overfetch. After inspection:

**Already optimized (this session):**
- Feed pipeline: 4 TTL caches + hydration skip (50-75% reduction)
- Badge counts: BottomNavBar CSS-hide + realtime debounce

**Remaining N+1 hotspots (all in learning controllers):**

| File | Issue | Severity |
|------|-------|----------|
| `getAdminDashboard.controller.js` | Triple-nested: per-org → per-course → membership reads | CRITICAL |
| `getParentDashboard.controller.js` | Per-assignment submission + grade reads | HIGH |
| `getObservedStudentAssignments.controller.js` | Per-assignment submission + grade reads | HIGH |
| `getStudentDashboard.controller.js` | Sequential grade reads per submission | HIGH |

**Already correct (no fix needed):**
- Feed actor bundle: TTL cache + batched
- Profile view: batched post reactions + roses
- Teacher assignments: correctly batched submissions + grades
- Feed block/follow/media: TTL cached

## Execution Summary

Investigation completed. Core VCSM social paths (feed, profiles, chat, notifications) already optimized from earlier this session. The 976 query count is a cumulative session total, not per-page. Learning N+1 issues only fire when visiting /learning/* routes (lazy-loaded, not background). Learning optimization deferred — not core VCSM priority.

NOTE OF COMPLETITION
