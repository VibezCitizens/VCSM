# Planning — April / 09 / Sequence 06

You are a senior full-stack engineer working inside this codebase:

/Users/vcsm/Desktop/VCSM/apps/VCSM

You have already completed an audit of the Vport Review Pipeline for:

profile / vport / tabs / review

Now use that audit as the source of truth and perform the next phase:

PHASE GOAL
Turn the review pipeline audit into an implementation-ready improvement plan and code-change plan.

REFERENCE DOCUMENT
Base your work on this audit:

Vport Review Pipeline — End-to-End Audit

Key findings from the audit:
- architecture is DAL -> Model -> Controller -> Hook -> Screen
- pipeline health is generally good
- schema health is good
- authorization is adequate but controller lacks explicit citizen-only guard
- review flow is dimension-based and idempotent
- RLS exists but controller should add defense in depth
- no pagination
- no explicit edit/delete UI
- no moderation workflow
- `overall_rating` depends on DB trigger
- author enrichment is a multi-step read path
- fallback dimension config can drift from DB config

PRIMARY TASK
Review the existing review implementation and create a concrete work package for improving it.

WHAT YOU MUST PRODUCE

1. A prioritized implementation plan
Split the work into:
- Phase 1 — Critical fixes
- Phase 2 — UX improvements
- Phase 3 — Performance and maintainability
- Phase 4 — Optional product improvements

2. Exact code-change targets
For each improvement, identify:
- file path
- function/component name
- what should change
- whether it is UI / hook / controller / DAL / model / schema / policy

3. Recommended improvements based on the audit
At minimum, cover these items:

A. Add explicit citizen-only guard in controller
- review submission must be rejected in controller if active actor is not a user/citizen
- do not rely only on UI hook and RLS

B. Add explicit actor-kind validation contract
- identify where actor kind is sourced
- define safest guard logic for submit flow

C. Add edit/delete UI plan
- show how existing implicit resubmit/edit behavior should become explicit UI
- wire delete UI to existing soft delete controller if appropriate

D. Add pagination plan
- define how review list pagination should work
- identify changes needed in DAL/controller/hook/UI

E. Add moderation/verification plan
- explain current `is_verified` behavior
- propose a proper moderation-ready structure without breaking the current pipeline

F. Document or verify `overall_rating` trigger dependency
- identify what should be documented or validated
- explain how app code depends on it

G. Review config drift risk
- compare DB-driven dimension config vs fallback config file
- recommend how to avoid drift

H. Review query efficiency
- identify whether author enrichment should stay multi-query or be refactored
- explain tradeoffs

4. Deliverable document
Write a technical markdown document and save it to:

/Users/vcsm/Desktop/VCSM/logan

Recommended filename:
vport-review-implementation-plan.md

DOCUMENT FORMAT
Use these sections:

1. Executive Summary
2. Current State
3. Critical Risks
4. Priority Fixes
5. File-by-File Change Plan
6. Authorization Hardening Plan
7. Pagination Plan
8. Edit/Delete UX Plan
9. Moderation Plan
10. Performance Improvements
11. Schema / Trigger Notes
12. Final Recommended Order of Work

IMPORTANT RULES
- Do not guess blindly; inspect the code when needed
- Base recommendations on the actual reviewed implementation
- Prefer exact file paths and function names
- Be concrete and implementation-oriented
- If a change requires schema/RLS work, say so explicitly
- If something should remain unchanged, say so explicitly
- Distinguish clearly between must-fix vs nice-to-have

FINAL DELIVERABLE
After analyzing the code and the audit, save the markdown document to:

/Users/vcsm/Desktop/VCSM/logan/vport-review-implementation-plan.md

Using the existing Vport Review Pipeline audit as the source of truth, review the code in:

/Users/vcsm/Desktop/VCSM/apps/VCSM

Focus only on these 4 improvements:

1. add explicit citizen-only submit guard in review controller
2. add pagination to review list
3. add explicit edit review UI behavior
4. add explicit delete review UI behavior using soft delete only for the citizen who posted the review

For each one, identify:
- exact file paths
- exact functions/components to change
- exact data flow impact
- exact risks
- exact implementation order

Then write a markdown implementation plan to:

/Users/vcsm/Desktop/VCSM/logan/vport-review-focused-plan.md

## Execution Plan

PLAN PROPOSAL
Task: Produce review pipeline implementation plan — focused on 4 improvements
Application Scope: VCSM (read-only inspection, output to logan/)
Estimated Time: ~8 minutes — split into 2 slices
Execution Type: Split

Slice 1 (~4 min) — Re-read key files + write focused plan (vport-review-focused-plan.md)
Slice 2 (~4 min) — Write full implementation plan (vport-review-implementation-plan.md)

Files Expected To Change:
- logan/vport-review-focused-plan.md (new)
- logan/vport-review-implementation-plan.md (new)

Assumptions:
- Based on audit in logan/vport-review-pipeline-audit.md
- No app code modifications — documentation only

## Execution Summary

### What was completed
Two implementation plan documents produced from the review pipeline audit:

1. **logan/vport-review-focused-plan.md** — Concrete implementation details for the 4 priority improvements:
   - Citizen-only guard in `ctrlSubmitReview` (reuses existing `dalReadReviewTargetActor`)
   - Cursor-based pagination (DAL → controller → hook → UI chain)
   - Edit review UI (pre-fill form from `myReview`, isEditing state, cancel/update flow)
   - Delete review UI (wire `ctrlDeleteMyReview` to button with confirmation prompt)

2. **logan/vport-review-implementation-plan.md** — Full 12-section plan covering all 8 improvements:
   - Authorization hardening (3-layer defense)
   - Pagination plan (cursor-based)
   - Edit/Delete UX plan
   - Moderation plan (simple flag via `is_verified`)
   - Performance analysis (keep multi-query, memoize aggregation)
   - Schema/trigger notes (`overall_rating` trigger dependency, config drift risk)
   - Prioritized order of work with estimates

### Files created
- logan/vport-review-focused-plan.md
- logan/vport-review-implementation-plan.md

NOTE OF COMPLETITION
