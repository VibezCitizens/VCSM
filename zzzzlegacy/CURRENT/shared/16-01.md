# Planning — April / 16 / Sequence 01

> Promoted from backlog: portfolio schema migration fix.

## Execution Plan

Task: Portfolio engine schema migration (vc.vport_portfolio_* → vport.portfolio_*)
Scope: ENGINE (engines/portfolio/ only)
Slices: 3

---

## Execution Summary

### Completed

**Slice 1 — DAL schema + table name fixes (9 files)**
Every `.schema('vc').from('vport_XYZ')` call replaced with `.schema('vport').from('XYZ')`.

| File | Old table | New table |
|------|-----------|-----------|
| portfolioItems.read.dal.js | vc.vport_portfolio_items | vport.portfolio_items |
| portfolioItems.write.dal.js | vc.vport_portfolio_items | vport.portfolio_items |
| portfolioMedia.read.dal.js | vc.vport_portfolio_media | vport.portfolio_media |
| portfolioMedia.write.dal.js | vc.vport_portfolio_media | vport.portfolio_media |
| portfolioTags.read.dal.js | vc.vport_portfolio_tags | vport.portfolio_tags |
| portfolioTags.write.dal.js | vc.vport_portfolio_tags | vport.portfolio_tags |
| barberDetails.read.dal.js | vc.vport_barber_portfolio_details | vport.barber_portfolio_details |
| locksmithDetails.read.dal.js | vc.vport_locksmith_portfolio_details | vport.locksmith_portfolio_details |
| portfolioItems.rpc.dal.js | (tombstoned — see Slice 3) | — |

**Slice 2 — Column fixes + model updates**
- `actor_id` → `profile_id` in ITEM_COLUMNS, ITEM_RETURN_COLUMNS, MEDIA_COLUMNS, MEDIA_RETURN_COLUMNS
- `dalInsertPortfolioItem`: parameter `actorId` → `profileId`, insert field `actor_id` → `profile_id`
- `dalInsertPortfolioMedia`: parameter `actorId` → `profileId`, insert field `actor_id` → `profile_id`
- `dalListPortfolioItemsByActor` renamed to `dalListPortfolioItemsByProfileId`, queries by `profile_id`
- NEW: `dalGetProfileIdByActorId` added to `portfolioItems.read.dal.js` — resolves `vport.profiles.id` from `actor_id`
- `PortfolioItem.model.js`: `actorId: raw.actor_id` → `profileId: raw.profile_id`
- `PortfolioMedia.model.js`: `actorId: raw.actor_id` → `profileId: raw.profile_id`

**Slice 3 — Controller fixes**
- `listPortfolio.controller.js`: removed broken RPC (`get_vport_portfolio` targeted old schema); replaced with `dalGetProfileIdByActorId` + `dalListPortfolioItemsByProfileId`; `coverUrl` now derived from mediaMap (cover role or first media)
- `createItem.controller.js`: resolves `profileId` via `dalGetProfileIdByActorId` before insert; throws if no profile found
- `addMedia.controller.js`: fetches item + callerProfileId in parallel; ownership check `item.actor_id !== actorId` → `item.profile_id !== callerProfileId`; passes `profileId` to `dalInsertPortfolioMedia`
- `updateItem.controller.js`: same parallel fetch + ownership fix
- `deleteItem.controller.js`: same parallel fetch + ownership fix
- `portfolioItems.rpc.dal.js`: tombstoned with deprecation comment; safe to delete once confirmed unused

**CLAUDE.md**: updated stale DB table list to reflect full `vport.*` schema

### Files Changed
- engines/portfolio/src/dal/portfolioItems.read.dal.js
- engines/portfolio/src/dal/portfolioItems.write.dal.js
- engines/portfolio/src/dal/portfolioItems.rpc.dal.js
- engines/portfolio/src/dal/portfolioMedia.read.dal.js
- engines/portfolio/src/dal/portfolioMedia.write.dal.js
- engines/portfolio/src/dal/portfolioTags.read.dal.js
- engines/portfolio/src/dal/portfolioTags.write.dal.js
- engines/portfolio/src/dal/barberDetails.read.dal.js
- engines/portfolio/src/dal/locksmithDetails.read.dal.js
- engines/portfolio/src/model/PortfolioItem.model.js
- engines/portfolio/src/model/PortfolioMedia.model.js
- engines/portfolio/src/controller/listPortfolio.controller.js
- engines/portfolio/src/controller/createItem.controller.js
- engines/portfolio/src/controller/addMedia.controller.js
- engines/portfolio/src/controller/updateItem.controller.js
- engines/portfolio/src/controller/deleteItem.controller.js
- engines/portfolio/CLAUDE.md

### Blockers / Follow-up
- `dalGetProfileIdByActorId` is a new round trip per write/ownership operation. If `vport.profiles` RLS blocks reads for unauthenticated calls, these will throw. No action needed now — will surface in runtime testing.
- `portfolioItems.rpc.dal.js` is tombstoned, not deleted. Delete it once the team confirms no other callers exist outside this engine.
- No DB migrations required — all changes are in application code only.

NOTE OF COMPLETITION

