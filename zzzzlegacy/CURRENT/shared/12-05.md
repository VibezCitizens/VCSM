# Planning — April / 12 / Sequence 05

## Task

Enhanced Performance Dashboard — upgrade `/dev/performance` with per-screen traces, multi-view tabs, caller attribution, and threshold alerts.

### Source

Promoted from `.tp-backlog.md`.

### What Already Exists

- `debuggers/performance/` — 17 files: Supabase proxy, fetch proxy, client metrics, store, analysis, dashboard, overlay
- `debuggers/feed/` — feed-specific DAL profiler with wrapDAL
- Dashboard at `/dev/performance` — query table, waterfall, route breakdown, recommendations
- Overlay — floating PERF pill with live stats

### What's Missing (gap analysis)

1. **Per-screen trace correlation** — group all queries/fetches/images by route load ID
2. **Multi-view tabbed dashboard** — currently one long page, needs: Overview, By Screen, Screen Detail, By DAL, By Table, Duplicates, N+1
3. **Component/hook attribution** — know which component/hook triggered each DAL call
4. **Threshold alert system** — auto-highlight when queries > 25, duplicates > 5, hydration > 300ms

## Execution Plan

PLAN PROPOSAL
Task: Enhanced Performance Dashboard
Application Scope: VCSM
Estimated Time: ~12 minutes
Execution Type: Split (3 slices)
Slices:
  Slice 1: Screen trace system (per-route trace correlation)
  Slice 2: Tabbed dashboard (7 views)
  Slice 3: Threshold alerts + build verify

## Execution Summary

### Completed

- Screen trace system: per-route grouping of all queries/API/images
- 7-tab dashboard: Overview, By Screen, Screen Detail, By DAL, By Table, Duplicates, N+1
- Threshold alerts: auto-detect queries>25, duplicates>5, N+1, slow loads
- 6 new files, 4 modified files, build passes

### Files Created
- debuggers/performance/screenTrace.js
- debuggers/performance/components/DashboardTabs.jsx
- debuggers/performance/components/ScreenListView.jsx
- debuggers/performance/components/ScreenDetailView.jsx
- debuggers/performance/components/GroupView.jsx
- debuggers/performance/components/AlertBanner.jsx

### Files Modified
- debuggers/performance/PerfDashboardScreen.jsx
- debuggers/performance/store.js
- debuggers/performance/instrumentation/clientMetrics.js
- debuggers/performance/index.js

NOTE OF COMPLETITION
