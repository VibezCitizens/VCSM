# Planning — April / 19 / Sequence 10

**Task:** RLS Hardening — Top 10 Fixes
**Scope:** DATABASE only
**Task Class:** MIXED (DB_REVIEW + IMPLEMENTATION)

## Execution Plan

5 migration files, grouped by risk tier.

Slice 1 — Safe policy cleanup (no data impact)
Slice 2 — Missing policies on locked tables
Slice 3 — Learning schema (submission_files, modules, lesson_progress)
Slice 4 — platform.* hardening (6 tables)
Slice 5 — Sensitive/data-touching (zubappnexrn, tripoint_reviews)

## Execution Summary

### What was completed

5 migration files written and verified via dry-run. Push blocked by expired CLI temp-role password (`e=Wrong password` in SASL debug). Push ready once user runs `supabase login && supabase db push --workdir apps/VCSM`.

**Migrations ready:**
- `20260419100000_rls_fix_slice1_policy_cleanup.sql` — FIX-2 (courses bypass drop), FIX-8 (follows explicit policies), FIX-9 (pin_comments legacy drop)
- `20260419110000_rls_fix_slice2_missing_policies.sql` — FIX-3 (conversation_keys), FIX-10 (reference tables + service-only tables)
- `20260419120000_rls_fix_slice3_learning.sql` — FIX-4 (submission_files), FIX-7 (modules + lesson_progress ENABLE RLS)
- `20260419130000_rls_fix_slice4_platform.sql` — FIX-1 (platform.* 6 tables ENABLE RLS + policies)
- `20260419140000_rls_fix_slice5_sensitive.sql` — FIX-5 (zubappnexrn drop allow_all), FIX-6 (tripoint_reviews PII + INSERT fix + default change)

### Blocker
`supabase db push` failing: `e=Wrong password` from SCRAM-SHA-256. CLI temp role expired. Fix: `supabase login` then re-run push.
