# Planning — May / 26 / Sequence 04

> Session: 2026-05-26
> Task: P2 — normalize submissionId resolution in useSubmitBulkFuelPrices (remove dead fallbacks)

---

## Execution Plan

**Task:** Remove dead fallback chain `res?.submissionId ?? res?.id` from submissionId resolution in `useSubmitBulkFuelPrices.js`; controller contract is `{ ok: true, submission: { id } }` for citizen path — `res?.submission?.id` is the only valid path  
**Application Scope:** VCSM  
**Task Class:** IMPLEMENTATION  
**Estimated Time:** ~3 minutes  
**Execution Type:** Direct  
**SENTRY Review:** Optional  

### Change

- `useSubmitBulkFuelPrices.js:84` — `res?.submissionId ?? res?.id ?? res?.submission?.id ?? null` → `res?.submission?.id ?? null`
- JSDoc param comment for `afterSubmitSuggestion` (line 11) — clarify: cascade runs when citizen submission returns a `submission.id` (not on owner-direct path)

### Files Expected To Change

1. `features/profiles/kinds/vport/hooks/gas/useSubmitBulkFuelPrices.js`

### Assumptions

- Controller returns either `{ ok: true, official }` (owner) or `{ ok: true, submission: { id, ... } }` (citizen) — confirmed by reading `submitFuelPriceSuggestion.controller.js` and `vportFuelPriceSubmission.model.js`
- `res?.submissionId` — never returned by any controller in this module
- `res?.id` — never returned by this controller (top-level `id` only appears in DAL return shapes, not controller responses)
- Behavior is identical: owner path already produces `null` with current code

### Blocking Risks

- None

---

## Execution Summary

**Completed:** 2026-05-26

### What was completed

- `useSubmitBulkFuelPrices.js:83` — `res?.submissionId ?? res?.id ?? res?.submission?.id ?? null` simplified to `res?.submission?.id ?? null`
- JSDoc for `afterSubmitSuggestion` param updated to accurately describe when it fires vs. is skipped

### Why it was done this way

Controller `submitFuelPriceSuggestion` has exactly two response shapes:
- Owner path: `{ ok: true, official: mapVportFuelPriceRow(row) }` — no `submission` key; `submissionId` → null → afterSubmitSuggestion skipped (correct)
- Citizen path: `{ ok: true, submission: mapFuelPriceSubmissionRow(row) }` — `submission.id` is the canonical ID field

`res?.submissionId` and `res?.id` were defensive fallbacks against shapes that never existed in this controller. Removing them makes the code self-documenting and eliminates false ambiguity about what the response contract is.

### Files changed

1. `features/profiles/kinds/vport/hooks/gas/useSubmitBulkFuelPrices.js` — line 83 + JSDoc line 11

NOTE OF COMPLETITION
