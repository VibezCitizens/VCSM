# Batman Planning — May / 03 / Sequence 03

Generated by: Batman
Scope: VCSM
Task Class: MIXED (DEBUGGING + IMPLEMENTATION)

## Task

Fix VPORT Business Card "View Full Menu" routing to login instead of public menu

## Root Cause

File: `apps/VCSM/src/features/public/vportBusinessCard/view/businessCardPrimarySection.jsx:76`

```js
const href = `https://vibezcitizens.com/vport/${slug}/menu`;
```

**Root cause**: `/vport/${slug}/menu` is not a registered public route.

Public vport routes registered in React Router:
- `/vport/:slug/card` → `VportBusinessCardPublicScreen` ✓ (public)
- `/vport/:type` → `VportCategoryLandingScreen` ✓ (public how-to)
- `/vport/${slug}/menu` → NOT REGISTERED

The SPA catch-all: `{ path: "*", element: <Navigate to="/login" replace /> }` captures this
path and redirects to login.

Correct public menu route (already registered in vportMenu.routes.jsx):
- `/profile/:slug/menu` → `VportMenuBySlugScreen` ✓ (public)

Fix: Change `href` from `/vport/${slug}/menu` → `/profile/${slug}/menu`

The `slug` prop passed to `MenuSection` is `card.slug` from the business card model — this
matches the `:slug` param expected by `VportMenuBySlugScreen`.

## Execution Plan

BATMAN PLAN
Task: Fix "View Full Menu" routing to login on VPORT Business Card
Scope: VCSM
Task Class: MIXED (DEBUGGING + IMPLEMENTATION)
Execution Type: Direct
Slices: N/A
Files Expected To Change:
  - apps/VCSM/src/features/public/vportBusinessCard/view/businessCardPrimarySection.jsx
    (line 76: href template — change /vport/${slug}/menu → /profile/${slug}/menu)
Specialist Commands Required: None
Batman Docs Expected: None
Assumptions:
  - VportMenuBySlugScreen accepts the vport slug (not actorId) — confirmed by route param :slug
  - The slug passed to MenuSection (card.slug) matches the route expectation
  - No Cloudflare Function conflict — _routes.json includes /profile/* so it routes to SPA correctly
Blocking Risks: None — one-line fix in a presentational component

## Execution Summary

What was completed:
  - Fixed `href` in `MenuSection` inside `businessCardPrimarySection.jsx`
  - Changed from `/vport/${slug}/menu` (unregistered route → login redirect) to `/profile/${slug}/menu` (registered public route → VportMenuBySlugScreen)

Why this way:
  - `/profile/:slug/menu` is the canonical registered public menu route in vportMenu.routes.jsx
  - The `slug` prop passed to `MenuSection` is `card.slug`, which matches the `:slug` param expected by VportMenuBySlugScreen
  - No auth guard on this route — works fully unauthenticated including private Safari

Files Changed:
  - apps/VCSM/src/features/public/vportBusinessCard/view/businessCardPrimarySection.jsx

Generated by: Batman

BATMAN TASK COMPLETE
