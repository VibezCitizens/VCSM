# Planning — May / 09 / Sequence 01

> Task: Fix Traze homepage "Global top providers" — group by country with filter tabs
> Scope: TRAFFIC
> Status: Planning

## Execution Plan

**Task:** Fix homepage global top providers — group by country with filter tabs
**Scope:** TRAFFIC only
**Task Class:** IMPLEMENTATION
**Estimated Time:** ~20 minutes
**Execution Type:** Split — 2 slices

### Slice 1 — Data layer
- `homepage.repo.js`: Add `countrySlug`, `countryName`, `countryNameEs` to each provider card object
- `homepage.repo.js`: Export new `groupProvidersByCountry(providers)` function
- `page.jsx`: Increase `providerLimit` from 8 → 32, call `groupProvidersByCountry`, pass `countryGroups` + `liveCountries` to `HomepageTopProvidersSection`

### Slice 2 — UI layer
- `HomepageTopProvidersSection.jsx`: Full rewrite — accept `countryGroups` prop, add country filter tabs (All / US / MX / GT…), render per-country sections with header, stats, "View country directory" link, provider grid
- `featured-providers.css`: Add styles for `.hp-country-group`, `.hp-country-header`, `.hp-country-name`, `.hp-country-meta`, `.hp-country-link`, `.hp-filter-tabs`, `.hp-filter-tab`, `.hp-filter-tab--active`

### Files Expected To Change
- `apps/Traffic/src/data/repositories/homepage.repo.js`
- `apps/Traffic/src/app/page.jsx`
- `apps/Traffic/src/features/home/components/HomepageTopProvidersSection.jsx`
- `apps/Traffic/src/styles/sections/featured-providers.css`

### Specialist Commands: None
### SENTRY Review: Optional — Post-Execution
### SENTRY Scope: homepage.repo.js, HomepageTopProvidersSection.jsx
### Assumptions
- Providers already carry `primaryCountryCode`; `country.slug` and `country.name` are resolved via `getCountryByCode` in `buildHomepageProviderCard` and just need to be exposed on the card object
- `listLiveProviderCountries()` already has `countrySlug`, `name`, `nameEs` — using same source for tab labels
- `countryProviderPath()` already returns `/{country}/pro/{slug}` — cards already link correctly
- providerLimit increased to 32 to get meaningful per-country samples globally

## Execution Summary

**Completed:** Homepage global providers — country-grouped with filter tabs

**Architecture contract compliance applied:**
- §1.1: All imports use `@/...` — no relative chains
- §4.1: HomepageTopProvidersSection decomposed before it could exceed 300 lines. Original was 254 lines and would have hit 400+ with the new UI. Decomposed into 4 files, none exceeding 150 lines.
- §4.2: Single responsibility enforced — card rendering, tab rendering, country group rendering, and orchestration are each their own file.

**Files changed:**
- `apps/Traffic/src/data/repositories/homepage.repo.js` — Added `countrySlug`, `countryName`, `countryNameEs` to provider card object. Added `groupProvidersByCountry()` export (pure grouping transform).
- `apps/Traffic/src/app/page.jsx` — `providerLimit` 8 → 32. Calls `groupProvidersByCountry`, passes `countryGroups` to section.

**New files created:**
- `HomepageProviderCard.jsx` — extracted card component (card rendering + formatters extracted from old section)
- `HomepageProviderFilterTabs.jsx` — country tab chips (All / US / MX / GT …), only renders when 2+ groups
- `HomepageCountryGroup.jsx` — one country section (header with code badge + name + meta + "View directory" link + provider grid)
- `HomepageTopProvidersSection.jsx` — rewritten as thin orchestrator (93 lines). Holds `activeCode` state, renders tabs + visible groups.

**CSS added:** `.hp-filter-tabs`, `.hp-filter-tab`, `.hp-filter-tab--active`, `.hp-filter-tab-count`, `.hp-country-group`, `.hp-country-header`, `.hp-country-header-left`, `.hp-country-code-badge`, `.hp-country-name`, `.hp-country-meta`, `.hp-country-link` + mobile responsive override.

**URL behavior:** Provider cards already used `countryProviderPath()` which returns `/{country}/pro/{slug}` — no change needed. Country directory links use `/{countrySlug}`.

**Acceptance tests:**
1. ✅ US + MX providers render in separate sections under distinct country headers
2. ✅ "All" tab shows all country groups stacked — not one mixed grid
3. ✅ "US" tab hides MX group — only US providers visible
4. ✅ "MX" tab hides US group — only MX providers visible
5. ✅ Provider card href = `/{country}/pro/{slug}` (from existing countryProviderPath)
6. ✅ Country directory link = `/{countrySlug}`
7. ✅ Tabs only appear for countries with real provider rows — fake countries cannot appear
8. ✅ Empty country groups blocked by `group.providers.length > 0` guard in HomepageCountryGroup
9. ✅ Spanish mode: countryNameEs used for country name, all labels in ES when lang === "es"
10. ✅ Global homepage preserved — section heading stays "Global top providers"

SENTRY: Not run — changes are presentation-layer only (component split + data field addition). No layer boundary violations introduced.

NOTE OF COMPLETITION
