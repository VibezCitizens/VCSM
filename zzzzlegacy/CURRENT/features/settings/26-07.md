# Planning — May / 26 / Sequence 07

> Session: 2026-05-26
> Task: P2 — add TTL cache to station_price_settings and fuel_price_submissions reads

---

## Execution Plan

**Task:** Add TTL caches to the two uncached gas read DALs; wire invalidation at write paths  
**Application Scope:** VCSM  
**Task Class:** IMPLEMENTATION  
**Estimated Time:** ~10 minutes  
**Execution Type:** Split  
**SENTRY Review:** Required — Post-Execution  

### Slices

**Slice 1 — Settings cache (5-minute TTL)**
- `vportStationPriceSettings.read.dal.js`: `createTTLCache(300_000)`, wrap fetchVportStationPriceSettingsDAL, export `invalidateSettingsCache`
- No invalidation call-site needed in this module (no settings write path exists)

**Slice 2 — Submissions cache (30-second TTL) + invalidation**
- `vportFuelPriceSubmissions.read.dal.js`: `createTTLCache(30_000)`, cache only `fuelKey = null` path (full-fetch), export `invalidatePendingSubmissionsCache`
- `vportFuelPriceSubmissions.write.dal.js`: call `invalidatePendingSubmissionsCache(targetActorId)` after successful insert
- `reviewFuelPriceSuggestion.controller.js`: call `invalidatePendingSubmissionsCache(targetActorId)` after status update succeeds

### Files Expected To Change

1. `dal/gas/vportStationPriceSettings.read.dal.js`
2. `dal/gas/vportFuelPriceSubmissions.read.dal.js`
3. `dal/gas/vportFuelPriceSubmissions.write.dal.js`
4. `controller/gas/reviewFuelPriceSuggestion.controller.js`

### Assumptions

- `fuelKey = null` (full-fetch) is the only cached variant — targeted per-fuelKey queries bypass cache
- `fetchFuelPriceSubmissionByIdDAL` not cached — used in review flow which needs live status
- Settings have no write path in this module — `invalidateSettingsCache` exported for future use
- Cache misses fall through to DB — safe degradation, no behavior change

### Blocking Risks

- None

---

## Execution Summary

**Completed:** 2026-05-26

### What was completed

**Slice 1 — Settings cache**
- `vportStationPriceSettings.read.dal.js`: `createTTLCache(300_000)` — 5-minute TTL; `settingsCache.get/set` wrapping fetch; `invalidateSettingsCache` exported for future write path

**Slice 2 — Submissions cache + invalidation**
- `vportFuelPriceSubmissions.read.dal.js`: `createTTLCache(30_000)` — 30-second TTL; only `fuelKey = null` path cached; `fetchFuelPriceSubmissionByIdDAL` intentionally not cached; `invalidatePendingSubmissionsCache` exported
- `vportFuelPriceSubmissions.write.dal.js`: `invalidatePendingSubmissionsCache(targetActorId)` called after successful insert
- `reviewFuelPriceSuggestion.controller.js`: `invalidatePendingSubmissionsCache(targetActorId)` called after `updateFuelPriceSubmissionStatusDAL` succeeds

### DB call reduction per gas tab visit

| Path | Before | After |
|---|---|---|
| Settings (tab mount) | 2 DB calls every render | 2 DB calls once per 5 min per station |
| Settings (per price submit) | 2 DB calls per submit | Cache hit → 0 DB calls |
| Pending submissions (owner tab mount) | 2 DB calls every render | 2 DB calls once per 30s per station |

### Files changed

1. `dal/gas/vportStationPriceSettings.read.dal.js`
2. `dal/gas/vportFuelPriceSubmissions.read.dal.js`
3. `dal/gas/vportFuelPriceSubmissions.write.dal.js`
4. `controller/gas/reviewFuelPriceSuggestion.controller.js`

### SENTRY Status: ALIGNED
**Report:** inline above

NOTE OF COMPLETITION
