# Flyer Builder — Performance Report

**KRAVEN status:** NOT_STARTED

Canvas rendering in designStudio may have frontend performance concerns (large component tree: canvasStage, sidebarRight, topBar). KRAVEN should assess DAL read/write patterns and whether canvas state causes unnecessary re-renders or network calls.