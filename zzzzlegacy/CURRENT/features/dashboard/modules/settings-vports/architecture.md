# Settings VPORT — Architecture

**ARCHITECT status:** NOT_STARTED

Architecture report will be generated after the ARCHITECT command runs.

---

## Known Source Paths

- `apps/VCSM/src/features/settings/vports/controller/`
- `apps/VCSM/src/features/settings/vports/dal/`
- `apps/VCSM/src/features/settings/vports/hooks/`
- `apps/VCSM/src/features/settings/vports/model/`
- `apps/VCSM/src/features/settings/vports/ui/`

## Relationship to DASHBOARD/modules/settings/

ARCHITECT must confirm whether this feature is consumed by the dashboard settings card (which is covered under `DASHBOARD/modules/settings/`) or whether it is an independent settings management layer with its own entry points.
