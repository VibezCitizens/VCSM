# Planning — May / 26 / Sequence 10

**Scope:** VCSM
**Task Class:** IMPLEMENTATION (P3-A continuation from 26-09)
**Task:** Fix UUID leak and direct react-qr-code import in VportPublicMenuQrView.jsx

---

## Execution Plan

**Files to change:**
- `apps/VCSM/src/features/public/vportMenu/view/VportPublicMenuQrView.jsx`

**Changes:**
1. Remove `useMemo` import (no longer needed)
2. Replace `import QRCode from "react-qr-code"` → `import { QrCode } from "@/features/dashboard/qrcode/adapters/qrcode.adapter"`
3. Add `import { buildMenuQrUrl } from "@/lib/qrUrlBuilders"`
4. Destructure `loading` from `useActorCanonicalSlug`
5. Remove `useMemo` URL builder — replace with direct `const menuUrl = canonicalSlug ? buildMenuQrUrl(canonicalSlug) : ""`
6. Gate QR render: show spinner skeleton when `loading || !canonicalSlug`
7. Gate URL display and action buttons behind `!loading && canonicalSlug`
8. Replace `<QRCode>` with `<QrCode bgColor fgColor level>` + `role="img"` + `aria-label`
9. Add `aria-label="Close menu QR"` to close button; replace `X` text with `✕`
10. Add `aria-label` on copy button

**SENTRY Review:** Not required (same pattern as VportPublicReviewsQrView — already verified)

---

## Execution Summary

**What was completed:**
- P3-A: `VportPublicMenuQrView.jsx` — UUID leak fixed, direct `react-qr-code` import replaced, adapter path used
- `useMemo` removed (no longer needed — `menuUrl` computed directly from resolved slug)
- QR gated behind `loading || !canonicalSlug` — spinner skeleton shown during resolution
- URL display and action buttons gated behind `!loading && canonicalSlug`
- Close button: added `aria-label="Close menu QR"`, replaced `X` with `✕`
- Copy button: added `aria-label={copied ? "Link copied" : "Copy menu link"}`
- QR wrapper: `role="img"` + `aria-label="QR code linking to menu page"`

**Why this way:**
- Identical fix pattern to `VportPublicReviewsQrView.jsx` — consistent across all public QR views
- `buildMenuQrUrl` uses `window.location.origin` (no hardcoded domain, env-parity compliant)
- UUID (`actorId`) now can never appear in a menu QR code
- Adapter import path satisfies S1 architecture finding from SENTRY

**Files changed:**
- `apps/VCSM/src/features/public/vportMenu/view/VportPublicMenuQrView.jsx`

---

---

## P3-B Execution Summary

**What was completed:**
- `QrCode.jsx` — added `title` and `className` pass-through props (non-breaking additive; forwarded to inner SVG for accessibility and print CSS)
- `PosterFlyer.jsx` — replaced `import QRCode from "react-qr-code"` with `import { QrCode } from "@/features/dashboard/qrcode/components/QrCode"` (internal intra-feature path, not adapter). Added `bgColor`, `fgColor`, `level` explicit props.
- `ClassicFlyer.jsx` — same pattern as PosterFlyer. Internal path, explicit color/level props.
- `PrintableQrFlyerCard.jsx` — cross-feature consumer (`flyerBuilder` → `qrcode`). Import updated to adapter path: `import { QrCode } from "@/features/dashboard/qrcode/adapters/qrcode.adapter"`. Removed redundant outer sizing `div` (shared `QrCode` provides its own sizing wrapper). `title` and `className` props preserved via new pass-through.

**Verification:**
- `grep react-qr-code` across all VCSM source: only `QrCode.jsx` remains (the canonical wrapper). Zero consumer files have direct library imports.

**Files changed:**
- `apps/VCSM/src/features/dashboard/qrcode/components/QrCode.jsx`
- `apps/VCSM/src/features/dashboard/qrcode/components/flyer/PosterFlyer.jsx`
- `apps/VCSM/src/features/dashboard/qrcode/components/flyer/ClassicFlyer.jsx`
- `apps/VCSM/src/features/dashboard/flyerBuilder/components/printableQr/PrintableQrFlyerCard.jsx`

---

---

## LOGAN Execution Summary

**What was completed:**
- `vcsm.vport-gas-prices.architecture.md` — added cross-link to `vcsm.vport-gas-station-cards-individual.architecture.md` in MODULE GOVERNANCE LINKS
- `vcsm.vport-reviews-dashboard.architecture.md` — replaced MISSING Logan doc row with 3 concrete rows (dashboard module, QR system, public reviews); updated LOGAN handoff note from "zero docs exist" to reflect current state
- `vcsm.vport.gas-station-profile-spec.md` — added Section 13 (Architecture Documentation) with a 4-row table linking all module docs for gas prices, gas cards UI, reviews QR, and reviews dashboard

**Files changed:**
- `zNOTFORPRODUCTION/_CANONICAL/logan/marvel/architect/modules/vcsm.vport-gas-prices.architecture.md`
- `zNOTFORPRODUCTION/_CANONICAL/logan/marvel/architect/modules/vcsm.vport-reviews-dashboard.architecture.md`
- `zNOTFORPRODUCTION/_CANONICAL/logan/vports/vcsm.vport.gas-station-profile-spec.md`

---

## Pending (remaining open items — deferred)

- P3-C: `BookingQrLinksPanel` booking adapter — resolve `actorId → booking profileId` internally (enabled: false until adapter exists)
- S2: `VportDashboardGasScreen` Final/View screen split — deferred to dashboard structural sprint
- Service FK in `@reviews` engine schema — CARNAGE territory

NOTE OF COMPLETITION
