# MAGNETO REPORT

**Date:** 2026-06-05
**Scope:** VCSM — `vport-booking-feed-security-updates` branch
**Run Type:** FEATURE — dashboard (bookings + gasprices modules)
**Session Isolation:** MAGNETO_BLIND_MODE ACTIVE
**Branch:** `vport-booking-feed-security-updates`

---

## MAGNETO SESSION ISOLATION CHECK

```
Session received prior findings: NO
Session received patch reports: NO
Session received THOR output: NO
Session received severity rankings: NO
MAGNETO_BLIND_MODE: ACTIVE

Violation status: CLEAN
```

Note: Compaction summary was present in session. MAGNETO conducted all analysis from source-level dependency traces and ARCHITECT artifacts only. No finding narratives, exploit descriptions, or severity rankings from prior commands were used as analysis inputs.

---

## ARCHITECT DEPENDENCY GATE

**ARCHITECT Report:** `ZZnotforproduction/GOVERNANCE/outputs/2026/06/05/ARCHITECT/` (present, 2026-06-05, SUCCESS, VCSM scope)
**Formal graph artifacts required:**

| Artifact | Required | Present |
|---|---|---|
| ARCHITECT report (date-current, SUCCESS, VCSM scope) | YES | YES |
| `dependencies.graph.json` | YES | NO — absent from this ARCHITECT run |
| `system.graph.json` | YES | NO |
| `routes.graph.json` | YES | NO |
| `database-reads.graph.json` | YES | NO |
| `governance-overlays.graph.json` | YES | NO |
| `feature-map.md` | YES | NO (system-map.md present as functional equivalent) |

**Gate Status: PARTIAL_PASS**

The ARCHITECT ran same-day in `SECURITY_WARFARE_SIMULATION / BLUE_TEAM` mode and produced `system-map.md` (cross-feature dependency graph, trust boundary map, schema client map) and `security-surface.json`. Formal `.graph.json` files were not generated by this ARCHITECT mode.

Per stale artifact policy: "An ARCHITECT report from the same release cycle that predates minor file changes is acceptable — MAGNETO should note the coverage gap in its report rather than blocking."

Coverage gap noted. All MAGNETO findings from this run are classified INFERRED (not CONFIRMED from formal JSON) unless also verified by direct source reading. Confidence ceiling for graph-based findings: MEDIUM.

---

## MAGNETO DEPENDENCY CHECK

| Check                                         | Status              |
|-----------------------------------------------|---------------------|
| ARCHITECT dependency gate passed              | PARTIAL_PASS (noted) |
| Session isolation confirmed (MAGNETO_BLIND_MODE) | PASS             |
| Dependency concentration analyzed             | PASS                |
| Cascade failure paths analyzed                | PASS                |
| Security dependency analysis completed        | PASS                |
| Single points of failure identified           | PASS                |
| Architectural gravity wells analyzed          | PASS                |
| Cross-system dependencies analyzed            | PASS                |
| Boundary collapse propagation analyzed        | PASS                |
| Release-risk concentration analyzed           | PASS                |

---

## DEPENDENCY CONCENTRATION MAP

| Component | Consumers | Type | Cross-Layer | Alternative | Risk |
|---|---|---|---|---|---|
| `assertActorOwnsVportActorController` | 12+ (6 feature domains) | authorization | YES | NO | CRITICAL |
| `checkVportOwnershipController` | 4 direct + via assert | authorization | NO | NO | HIGH |
| `resolveVportProfileId` DAL (profiles) | 2 DAL files (gasprices) | DAL/identity | YES (DAL-to-DAL) | PARTIAL | HIGH |
| `getActorByIdDAL` | 2+ controllers + assert | identity | NO | NO | HIGH |
| Notification engine (`publishVcsmNotification*`) | 2+ booking controllers | notification | NO | PARTIAL | MEDIUM |
| `FuelPriceCacheService` | 2 controllers (gasprices) | cache/service | NO | YES (TTL expiry) | LOW |

**CONCENTRATION_RISK nodes (consumer count ≥ 6):** `assertActorOwnsVportActorController` — 12+ consumers

---

## CASCADE FAILURE PATHS

### CASCADE_FAILURE_PATH_FOUND — MAG-003

```
CASCADE_FAILURE_PATH_FOUND

Origin: assertActorOwnsVportActorController bug or silent-failure
Chain:
  Depth 1: All direct callers lose correct ownership verification
           settings/* (6 controllers), join/* (2 controllers),
           bookings/* (3 controllers), vportOwnerStats (1 controller)
  Depth 2: checkVportOwnershipController adopts wrong trust level
           (it wraps assertActorOwnsVportActorController)
           → All gasprices controllers using checkVportOwnership also affected
  Depth 3: Trust boundary crossed — VPORT ownership boundary collapses
           → All VPORT settings mutations, booking mutations, and fuel price
             mutations proceed without verified ownership
           → If failure is FAIL_SILENT (wrong trust granted rather than error):
             cross-actor VPORT data mutation becomes possible
  Depth 4: Feed write path affected (publishFuelPriceUpdateAsPost calls
           checkVportOwnershipController — already failing silently for
           specific call patterns as confirmed from source)

Cascade Depth: 4
Boundaries Crossed: 2 — [VPORT ownership / Feed write access]
Systemic: NO (contained within VCSM — does not cross app boundaries)
Circuit Breaker Present: PARTIAL — FAIL_CLOSED behavior for explicit errors
  (checkVportOwnershipController catch block returns false);
  NO circuit breaker for FAIL_SILENT patterns (self-shortcut misfires silently)
User-Visible Impact: YES — unauthorized mutations across VPORT settings, bookings,
  fuel prices, and feed posts
Write Path Affected: YES
Finding Type: CASCADE_FAILURE_PATH_FOUND
Handoff: VENOM (trust boundary design) + THOR (release CAUTION — 2 boundary crossings)
```

### CASCADE_FAILURE_PATH_FOUND — MAG-004 (Source-Confirmed)

```
CASCADE_FAILURE_PATH_FOUND

Origin: publishFuelPriceUpdateAsPostController — degenerate self-reference call
  [Source-confirmed from direct source read — not inferred]
Chain:
  Depth 1: VPORT ownership boundary bypassed (checkVportOwnershipController
           returns true for any non-void actor when callerActorId === targetActorId)
  Depth 2: Feed write access boundary bypassed
           → createSystemPost called with caller's actorId without legitimate
             ownership verification
  Depth 3: PUBLIC_REALM feed written with unauthorized fuel_price_update system posts

Cascade Depth: 3
Boundaries Crossed: 2 — [VPORT ownership / feed write access]
Systemic: NO (PUBLIC_REALM scope only — not cross-realm, not platform-wide)
Circuit Breaker Present: NO — no secondary ownership verification in createSystemPost
User-Visible Impact: YES — fake fuel price system posts visible in public feed
Write Path Affected: YES (feed.posts write)
Finding Type: CASCADE_FAILURE_PATH_FOUND + TRUST_BOUNDARY_PROPAGATION_FOUND
Handoff: VENOM (trust boundary design) + THOR (MANDATORY BLOCK — TRUST_BOUNDARY_PROPAGATION)
```

---

## SECURITY CONTROL CONCENTRATION

### SECURITY_CONTROL_CONCENTRATION_FOUND — MAG-001

```
SECURITY_CONTROL_CONCENTRATION_FOUND

Control: assertActorOwnsVportActorController
Type: authorization (ownership assertion)
Dependent Features: 6 domains — settings (account, privacy, vports), join, bookings, vport/dashboard
  Full call-site list:
    settings/privacy/actorPrivacy.controller.js
    settings/account/account.controller.js
    settings/vports/vportDirectoryVisibility.controller.js (2 calls)
    settings/vports/vportBusinessCardSettings.controller.js (2 calls)
    settings/vports/vportBusinessCard.controller.js
    settings/vports/vportSocialSettings.controller.js (2 calls)
    join/joinBarbershopQr.controller.js (2 calls)
    join/joinBarbershopAccount.controller.js
    dashboard/vport/controller/checkVportOwnership.controller.js (wrapper)
    dashboard/bookings/updateVportBooking.controller.js
    dashboard/bookings/rescheduleBooking.controller.js (inferred)
    dashboard/bookings/createOwnerBooking.controller.js (new in this branch)
    dashboard/vport/vportOwnerStats.controller.js

Consumer Count: 13+ call sites across 6 feature domains

Failure Mode Analysis:
  Normal failure: FAIL_CLOSED (throws — callers receive error, access denied) ✓
  FAIL_SILENT case: when requestActorId === targetActorId AND kind === "user"
    → user self-shortcut fires → actor_owners NOT queried → returns {ok:true, mode:"self"}
    → callers receive success WITHOUT ownership verification
    → This is NOT a bug in this case UNLESS the caller should supply different IDs
    → publishFuelPriceUpdateAsPostController supplies SAME actorId to checkVportOwnership
      which internally calls assertActorOwns with same IDs → FAIL_SILENT confirmed
  Overall Failure Mode: FAIL_SILENT (for degenerate call patterns — returns wrong
    success result without error, without checking actor_owners)

Secondary Control Present: NO — no alternative ownership verification path; all VPORT 
  mutation gates lead to this single function
Modified In Current Release: YES — ELEK-004 fix (kind check moved before self-shortcut)
  All 13+ call sites were implicitly re-evaluated by this change.

Impact If Failed (FAIL_SILENT):
  Settings mutations execute without VPORT ownership verification → any user can modify
  any VPORT's settings if they supply their own actorId as both requestActorId and targetActorId
  (currently only possible via degenerate call patterns, but the pattern exists in production
  code for publishFuelPriceUpdateAsPost and would be possible for any controller that passes
  the same ID as both parameters)

Finding Type: SECURITY_CONTROL_CONCENTRATION_FOUND
Handoff: VENOM (trust boundary design — ownership assertion concentrations)
THOR Blocker: YES — FAIL_SILENT failure mode on a modified, high-concentration control
```

---

## SINGLE POINTS OF FAILURE

### SINGLE_POINT_OF_FAILURE_FOUND — MAG-002

```
SINGLE_POINT_OF_FAILURE_FOUND

Component: assertActorOwnsVportActorController
Category: authorization (VPORT ownership assertion)
Consumers Affected If Failed: 13+ controllers across 6 feature domains (all VPORT mutations)
Surviving Paths If Removed: 0 — no alternative ownership assertion mechanism exists
Classification: SPOF
What Survives If Removed: NOTHING (all VPORT ownership-gated mutations become unprotected)
What Becomes Reachable If Compromised:
  Any actor can mutate any VPORT's settings, bookings, fuel prices, and stats
  without ownership verification. Cross-actor VPORT data mutation becomes possible.
Monitored: UNKNOWN — no monitoring references found in codebase
Finding Type: SINGLE_POINT_OF_FAILURE_FOUND
Handoff: VENOM (security SPOF — ownership boundary), THOR (release critical — modified in release)
```

### SINGLE_POINT_OF_FAILURE_FOUND — MAG-005

```
SINGLE_POINT_OF_FAILURE_FOUND

Component: checkVportOwnershipController
Category: authorization (VPORT dashboard visibility + mutation gate)
Consumers Affected If Failed: 4+ gasprices controllers (all VPORT fuel price mutations)
Surviving Paths If Removed: 0 for gasprices mutation protection — no secondary gate in gasprices
Classification: SPOF (for gasprices scope)
What Survives If Removed: NOTHING (gasprices mutations become unprotected)
What Becomes Reachable If Compromised:
  Any actor can submit owner fuel price updates, update station fuel units,
  and review fuel price submissions without ownership verification.
Monitored: UNKNOWN
Finding Type: SINGLE_POINT_OF_FAILURE_FOUND
Handoff: VENOM (security SPOF), THOR (release caution)
```

---

## ARCHITECTURAL GRAVITY WELLS

### ARCHITECTURAL_GRAVITY_WELL_FOUND — MAG-006

```
ARCHITECTURAL_GRAVITY_WELL_FOUND

Module: assertActorOwnsVportActorController (booking.adapter.js export)
Gravity Level: HEAVY
Coupling Signals:
  - Convention-forcing: all VPORT mutation controllers adopt the 
    {requestActorId, targetActorId} naming pattern to interface with this function
  - Anti-inversion: consumers adapted their parameter naming to match this function's API
  - Schema gravity: actor_owners DB table's access pattern radiates from this function
  - Cross-layer pull: consumed by feature controllers (app layer) AND
    by checkVportOwnershipController which is in the VPORT dashboard sub-feature (cross-feature)
  - Engine-level centrality: available via booking.adapter.js — the booking engine
    became the de facto platform ownership authority by exporting this function

Modules Shaped By It: 13+ controllers across settings, join, bookings, gasprices, stats
  All adopted {requestActorId, targetActorId} vocabulary
  All adopted the "throws on failure" contract pattern

Would Interface Change Cascade: YES — changing parameter names, return shape, or failure
  mode would require updates to 13+ call sites across 6 feature domains

Implicit Coupling Detected: YES
  Description: checkVportOwnershipController wraps assertActorOwnsVportActorController
  with a TRY/CATCH that converts ALL errors to boolean false. This implicit coupling means
  that any change to assertActorOwnsVportActor's error types or messages is swallowed
  invisibly — callers cannot distinguish between "ownership denied" and "DB connection error."

Handoff: VENOM (security coupling — single ownership authority for platform-wide mutations)
Finding Type: ARCHITECTURAL_GRAVITY_WELL_FOUND
```

---

## CROSS-SYSTEM COUPLING RISKS

### COUPLING_RISK_FOUND — MAG-007 (CONFIRMED — source read)

```
COUPLING_RISK_FOUND

Dependency Path: [gasprices/dal] → [profiles/kinds/vport/dal/services]
Dependency Type: DAL-to-DAL (cross-feature)
  vportFuelPrices.read.dal.js:3 imports resolveVportProfileId from profiles DAL
  vportFuelPrices.write.dal.js:4 imports resolveVportProfileId from profiles DAL

Poisoning Risk: YES
  Description: If profiles feature changes resolveVportProfileId's behavior
  (e.g., returns null for inactive profiles, changes actorId resolution logic),
  gasprices DAL silently reads/writes fuel prices for wrong VPORT or fails to
  resolve target VPORT at all. The write path (upsertVportFuelPriceDAL) accepts
  the profileId from resolveVportProfileId and writes fuel_prices directly to
  that profile — a wrong resolution means wrong VPORT's fuel prices are overwritten.

Governed By Contract: NO — DAL Import Boundary Rule explicitly forbids this
  (CR-002 in CONTRACT REVIEW report documents this violation)
Direction Violation: NO (app imports app, no engine-to-app inversion)
Race Condition Risk: NO (sequential resolution, no concurrent mutation)
Blast Radius If Coupling Breaks:
  All official fuel price writes (upsertVportFuelPriceDAL) silently fail or
  write to wrong VPORT's profile; all official fuel price reads
  (fetchVportFuelPricesDAL) silently return empty or wrong VPORT's prices;
  downstream controllers receive wrong/empty data without error signals

Finding Type: COUPLING_RISK_FOUND
Handoff: VENOM (trust boundary — wrong VPORT mutation via silent coupling),
  Wolverine (refactor — move profileId resolution to controller per CR-002)
```

### COUPLING_RISK_FOUND — MAG-008 (INFERRED)

```
COUPLING_RISK_FOUND

Dependency Path: [publishFuelPriceUpdateAsPost] → [checkVportOwnershipController]
  ↓ [checkVportOwnershipController] → [assertActorOwnsVportActorController]

Dependency Type: feature-to-feature (mutation gate depends on navigation gate)
  checkVportOwnershipController inline comment: "navigation/visibility gate only — mutations
  require a user-kind actor." The function was designed for navigation; it is used for mutation.

Poisoning Risk: YES
  Description: If checkVportOwnershipController is "fixed" to be navigation-only
  (e.g., adding a mutation-guard early return), callers that use it for mutation
  gating will silently lose their protection. There is no version contract between
  the navigation semantics and the mutation usage.

  Additionally: the navigation gate uses the VPORT self-shortcut (kind === "vport"
  → return true without actor_owners query). This is valid for navigation. When the
  same gate is used for mutations by passing self-referential IDs, the shortcut
  creates an unauthorized write path.

Governed By Contract: NO — the inline comment documents the gate as navigation-only,
  but no adapter contract prevents mutation-use consumers from importing it
Direction Violation: NO
Race Condition Risk: NO
Blast Radius If Coupling Breaks:
  If checkVportOwnershipController is corrected to navigation-only behavior,
  all 4 gasprices mutation controllers lose their ownership gate simultaneously —
  they would need to be rerouted to assertActorOwnsVportActorController directly

Finding Type: COUPLING_RISK_FOUND
Handoff: VENOM (trust boundary — navigate/mutation gate confusion), Wolverine (refactor)
```

---

## BOUNDARY COLLAPSE PROPAGATION

### TRUST_BOUNDARY_PROPAGATION_FOUND — MAG-009 (Source-Confirmed)

```
TRUST_BOUNDARY_PROPAGATION_FOUND

Origin Boundary: VPORT ownership boundary (actor_owners table verification)

Collapse Chain:
  Step 1: publishFuelPriceUpdateAsPostController calls checkVportOwnershipController
    with callerActorId === targetActorId (degenerate self-reference)
    → VPORT self-shortcut fires for VPORT-kind actors (no actor_owners query)
    → user self-shortcut fires via assertActorOwnsVportActorController for user-kind actors
    RESULT: VPORT ownership boundary COLLAPSED — any non-void actor passes

  Step 2: createSystemPost is called with caller's actorId (unverified as VPORT owner)
    → feed.posts table receives fuel_price_update system post from unauthorized actor
    → PUBLIC_REALM feed write access boundary COLLAPSED
    RESULT: Feed write access boundary bypassed for fuel_price_update system post type

  Step 3: Post persists in PUBLIC_REALM feed for all users
    → platform public content integrity boundary affected for fuel price data

Chain Depth: 3 (ownership → feed write → public content)
Secondary Enforcement Present: NO
  No secondary ownership check in createSystemPost adapter
  No actor kind check on post_type = "fuel_price_update" in post creation path
Systemic: NO (chain contained within VCSM / PUBLIC_REALM — does not escape to platform level)
User-Visible Impact: YES — fake system posts visible in public feed
Escalating Finding Types: CASCADE_FAILURE_PATH_FOUND (MAG-004)
Finding Type: TRUST_BOUNDARY_PROPAGATION_FOUND
Handoff: VENOM (trust boundary design authority) + THOR (MANDATORY BLOCK per area 8 §7)
```

---

## RELEASE RISK CONCENTRATION TABLE

```
RELEASE RISK CONCENTRATION TABLE

| Component | RRS | Consumer Count | Cascade Depth | Security Critical | Review Coverage |
|---|---|---|---|---|---|
| assertActorOwnsVportActorController | RRS-4 | 13+ | 4 | YES | PARTIAL (modified in release — downstream settings/join controllers not re-verified for self-shortcut pattern) |
| publishFuelPriceUpdateAsPostController | RRS-4 | PUBLIC_REALM feed | 3 | YES | PARTIAL (degenerate self-reference undetected by Blue Team ownership tests — no test for same-ID call pattern) |
| checkVportOwnershipController | RRS-3 | 4+ | 3 | YES | PARTIAL (used as mutation gate despite navigation-only contract) |
| vportFuelPrices.read.dal.js | RRS-3 | 1 feature | 2 | MEDIUM | PARTIAL (CR-002/003 violations unresolved; TTL cache compounds TOCTOU) |
| createOwnerBookingController | RRS-3 | booking calendar | 2 | YES | MISSING (no test file for this controller) |
```

**Highest-risk component in this release:** `assertActorOwnsVportActorController` (RRS-4)
**Largest blast radius if regression introduced:** `assertActorOwnsVportActorController` — 13+ controllers across 6 feature domains would lose correct ownership verification simultaneously.

**Hidden review invalidation risk:**

```
REVIEW_INVALIDATION_RISK

Component Changed: assertActorOwnsVportActorController (ELEK-004 fix — kind check order)
Changed After: Prior to VENOM/BLACKWIDOW/ELEKTRA runs in this warfare simulation
Risk: ELEK-004 fix moved the kind check before the self-shortcut. All 13+ callers
  were implicitly affected. No downstream re-verification was performed for
  settings/* and join/* controllers after this change. Those controllers were
  outside the declared scope of this branch's review.
Required Action: Verify that all 13+ call sites do NOT pass matching IDs as both
  requestActorId and targetActorId. Only publishFuelPriceUpdateAsPost is confirmed
  to do so in production code — but the pattern risk applies to any controller where
  both parameters are derived from the same source value.
```

---

## FINDING SUMMARY

| ID | Finding Type | Component | Coupling Risk | Confidence | THOR Blocker |
|---|---|---|---|---|---|
| MAG-001 | SECURITY_CONTROL_CONCENTRATION_FOUND | assertActorOwnsVportActorController | CRITICAL | HIGH | YES |
| MAG-002 | SINGLE_POINT_OF_FAILURE_FOUND | assertActorOwnsVportActorController | HIGH | HIGH | YES (security SPOF, modified in release) |
| MAG-003 | CASCADE_FAILURE_PATH_FOUND | assertActorOwnsVportActorController cascade | CRITICAL | MEDIUM | YES (2 boundary crossings + trust boundary) |
| MAG-004 | CASCADE_FAILURE_PATH_FOUND + TRUST_BOUNDARY_PROPAGATION_FOUND | publishFuelPriceUpdateAsPost → checkVportOwnership | CRITICAL | HIGH | YES (MANDATORY — TRUST_BOUNDARY_PROPAGATION) |
| MAG-005 | SINGLE_POINT_OF_FAILURE_FOUND | checkVportOwnershipController | HIGH | HIGH | NO (route to VENOM) |
| MAG-006 | ARCHITECTURAL_GRAVITY_WELL_FOUND | assertActorOwnsVportActorController | HIGH | HIGH | NO (governance acceptance path) |
| MAG-007 | COUPLING_RISK_FOUND | gasprices DAL → profiles DAL (cross-feature import) | HIGH | HIGH (source-confirmed) | NO (route to Wolverine + VENOM) |
| MAG-008 | COUPLING_RISK_FOUND | publishFuelPriceUpdate → checkVportOwnership (nav/mutation gate confusion) | MEDIUM | HIGH (source-confirmed) | NO (route to VENOM) |

```
CRITICAL: 3 (MAG-001, MAG-003, MAG-004)
HIGH:     4 (MAG-002, MAG-005, MAG-006, MAG-007)
MEDIUM:   1 (MAG-008)
LOW:      0
```

**THOR Blockers: 4** (MAG-001, MAG-002, MAG-003, MAG-004)

---

## HANDOFF ROUTING

| Finding | Handoff Command | Reason |
|---|---|---|
| MAG-001 (SECURITY_CONTROL_CONCENTRATION) | VENOM + THOR | Trust boundary design authority; release block |
| MAG-002 (SPOF — assert controller) | VENOM + THOR | Security SPOF, modified in release |
| MAG-003 (CASCADE — assert cascade) | VENOM + THOR | Trust boundary crossed; 2 boundaries |
| MAG-004 (CASCADE + TRUST_BOUNDARY_PROPAGATION) | VENOM + THOR | MANDATORY BLOCK — trust boundary propagation confirmed from source |
| MAG-005 (SPOF — checkVportOwnership) | VENOM | Security SPOF — route to trust boundary redesign |
| MAG-006 (GRAVITY_WELL — assert) | SENTRY | Architecture boundary enforcement — gravity well documentation |
| MAG-007 (COUPLING — DAL cross-feature) | VENOM + Wolverine | Trust boundary (wrong VPORT mutation path) + architecture refactor |
| MAG-008 (COUPLING — nav/mutation gate) | VENOM + Wolverine | Trust boundary semantics confusion + refactor |
