# CAPTAIN Log — 2026-06-08 — Run 001

---

### [2026-06-08 13:10]

**Idea:**
IDENTITY-BOUNDARY consolidation program — structurally COMPLETE (001–007 incl. 006A). Full file ledger for provenance.

**Context:**
Multi-ticket program that flipped ownership authority, extracted hydration, migrated the identity runtime out of `state/identity`, fixed a runtime white-screen, and zeroed adapter-boundary violations. End state: `authorization` owns ownership decisions, `features/hydration` owns display mapping, `features/identity` owns the live runtime, the `state/identity ⇄ features/identity` folder cycle is gone, identity adapter-boundary violations = 0. Regression net green throughout (booking gate 13/13, vportLeads 10/10).

**Next Action:**
None for identity itself — program is done. Promote the actor-row consolidation program (separate entry below) when ready.

**Scope:**
VCSM

**Architecture Impact:**
Yes (feature boundaries + ownership relocated; ARCHITECT maps will need regeneration)

**Engine Impact:**
No (engines/identity + engines/hydration untouched and verified app-agnostic)

**Notes — complete file ledger by ticket:**

002 (contract + dead-code):
- CREATED `ZZnotforproduction/APPS/VCSM/features/identity/IDENTITY_BOUNDARY_CONTRACT.md`
- CREATED `apps/VCSM/src/features/identity/README.md`
- EDITED `apps/VCSM/src/features/authorization/README.md` (status note)
- DELETED `apps/VCSM/src/state/identity/IdentityDebugger.jsx`, `apps/VCSM/src/state/identity/identitySwitcher.jsx`

004 (authority flip → authorization):
- EDITED `apps/VCSM/src/features/authorization/controllers/assertActorOwnsActor.controller.js` (ownershipMode shape parity)
- EDITED `apps/VCSM/src/features/booking/controllers/assertActorOwnsVportActor.controller.js` (now compatibility wrapper)
- EDITED `apps/VCSM/src/features/booking/controllers/assertSessionOwnsVportActor.controller.js` (now compatibility wrapper)
- EDITED `apps/VCSM/src/features/booking/controllers/__tests__/assertActorOwnsVportActor.controller.test.js` (mocks repointed to authorization DALs)

004A (cleanup + stale test repair):
- EDITED `apps/VCSM/src/features/vportDashboard/dashboard/cards/leads/__tests__/vportLeads.controller.test.js` (session-gate mock + stale signatures fixed)

004B (orphaned DAL retirement):
- DELETED `apps/VCSM/src/features/booking/dal/readActorOwnerLinkByActorAndUserProfile.dal.js`
- DELETED `apps/VCSM/src/features/booking/dal/readOwnerLinkByActorAndSession.dal.js`

005 (hydration extraction):
- CREATED `apps/VCSM/src/features/hydration/model/vcsmActorMappers.model.js` (mapProfileActor, mapVportActor)
- CREATED `apps/VCSM/src/features/hydration/dal/vcsmActorHydration.read.dal.js` (readProfileIdentityDAL, readActorPrivacyDAL, readVportIdentityDAL, readActorOwnerUserDAL)
- EDITED `apps/VCSM/src/features/hydration/vcsmActorHydrator.js` (import sources)
- EDITED `apps/VCSM/src/features/identity/identity.model.js` (removed mappers — file later moved in 006)
- EDITED `apps/VCSM/src/features/identity/identity.read.dal.js` (removed 4 hydration DALs + unused imports — file later moved in 006)

006 (runtime migration — 13 files moved state/identity → features/identity):
- MOVED `identityContext.jsx`, `identity.controller.js`, `identity.controller.inflight.js`, `identity.model.js`, `identity.read.dal.js`, `identityStorage.js`, `identitySelection.store.js`, `identitySelectors.js`, `identitySelfHeal.controller.js`, `identityResolutionSelfHeal.helper.js`, `useIdentityResolutionEffect.hook.js`, `controller/switchActor.controller.js`, `queries/identityEngineQuery.js` → under `apps/VCSM/src/features/identity/`
- DELETED directory `apps/VCSM/src/state/identity/`
- REWROTE `@/state/identity` → `@/features/identity` across 15 files. External consumers: `apps/VCSM/src/app/providers/AuthProvider.jsx`, `apps/VCSM/src/app/routes/protected/appRoutes.redirects.jsx`, `apps/VCSM/src/features/chat/setup.js`, `apps/VCSM/src/features/hydration/vcsmActorHydrator.js`, `apps/VCSM/src/features/identity/adapters/identity.adapter.js`, `apps/VCSM/src/features/identity/hooks/useActiveActorState.js`, `apps/VCSM/src/features/profiles/kinds/vport/screens/booking/hooks/useVportBookingView.js`, `apps/VCSM/src/features/vportDashboard/dashboard/cards/team/controller/vportTeamAccess.controller.js`, `apps/VCSM/src/learning/layout/LearningLayout.jsx`
- EDITED `apps/VCSM/src/features/identity/adapters/identity.adapter.js` (+useIdentitySelectionStore hook export)
- EDITED `apps/VCSM/src/features/chat/setup.js`, `apps/VCSM/src/features/vportDashboard/dashboard/cards/team/controller/vportTeamAccess.controller.js` (2 hook violations repointed to adapter)

006A (runtime white-screen fix — missed imports outside the rewrite scope):
- EDITED `ZZnotforproduction/_ACTIVE/debuggers/global/GlobalDebugPanel.jsx`
- EDITED `ZZnotforproduction/_ACTIVE/debuggers/identity/useActorConsistencyCheck.js`
- (Investigation also cleared `apps/VCSM/node_modules/.vite` — harmless, not the cause)

007 (consumer remediation — adapter-boundary → 0):
- CREATED `apps/VCSM/src/features/identity/hooks/useCanCitizenBook.js`
- EDITED `apps/VCSM/src/features/identity/adapters/identity.adapter.js` (+useCanCitizenBook hook export)
- EDITED `apps/VCSM/src/features/profiles/kinds/vport/screens/booking/hooks/useVportBookingView.js` (selector → hook)
- EDITED `apps/VCSM/src/features/hydration/vcsmActorHydrator.js` (C5 sanctioned eslint-disable on 2 identity-runtime reads)

Memory (persistent index):
- CREATED `~/.claude/projects/-Users-vcsm-Desktop-VCSM/memory/project_identity_boundary_program.md`
- EDITED `~/.claude/projects/-Users-vcsm-Desktop-VCSM/memory/MEMORY.md`

001 + 003 were READ-ONLY reviews (no files changed).

---

### [2026-06-08 13:10]

**Idea:**
NEXT PROGRAM — Actor-Row Consolidation (the one routed identity-side debt).

**Context:**
Two known debts were intentionally deferred to a dedicated actor-row program rather than hacked into the identity tickets:
1. **Hydration C5 exception** — `apps/VCSM/src/features/hydration/vcsmActorHydrator.js` carries 2 sanctioned `eslint-disable vcsm-architecture/adapter-boundary` directives (lines ~17, ~22) for identity-runtime reads (`readIdentityActorByIdDAL`, `readUserActorByProfileIdDAL` from `identity.read.dal`; `resolveRealmId` from `identity.controller`). The adapter contract forbids exposing DALs/controllers, so the proper fix is to source actor-row reads from a `features/actors` public surface.
2. **getActorById triplication** — same actor-by-id row read is reimplemented in 3 places: `apps/VCSM/src/features/booking/dal/getActorById.dal.js` (live, 2 importers: createBooking + checkVportOwnership via adapter), `apps/VCSM/src/features/authorization/dal/actors.read.dal.js` (`readActorByIdDAL`), `apps/VCSM/src/features/identity/identity.read.dal.js` (`readIdentityActorByIdDAL`, +`is_deleted` column). `features/actors` is currently search-only and underbuilt.

**Next Action:**
Scope ACTORS-CONSOLIDATION-001: make `features/actors` the canonical actor-row reader (single `readActorById` + a public surface via `actors.adapter`), migrate booking/authorization/identity/hydration consumers, preserve `getActorByIdDAL` projection contract (`id,kind,profile_id,vport_id,is_void`; identity variant adds `is_deleted`). This removes the hydration C5 exception and the triplication. Keep it scoped — do NOT reopen identity tickets.

**Scope:**
VCSM

**Architecture Impact:**
Yes (consolidates a DAL across 4 features; ARCHITECT remap likely)

**Engine Impact:**
No

**Notes:**
Also unrelated, separate future track (NOT this program): ownership consumer-migration — 48 actor-owns + 3 session consumers still import via `booking.adapter` compatibility wrappers; migrate them to `authorization.adapter`, then retire the booking wrappers.

---

### [2026-06-08 13:10]

**Idea:**
Minor identity-side cleanups (low priority, not blocking).

**Context:**
Small cosmetic/debt items observed during the program, left untouched to keep tickets surgical.

**Next Action:**
Fold into a future low-priority cleanup pass (or the relevant feature's next touch):
- `apps/VCSM/src/features/identity/` now has BOTH `controller/` (singular, moved `switchActor.controller.js`) and `controllers/` (plural, original). Consolidate folder naming.
- `apps/VCSM/src/features/profiles/kinds/vport/screens/booking/hooks/useVportBookingView.js:46` — PRE-EXISTING `vcsm-architecture/single-source-actor` violation (local `ownerCustomerActorId` useState). Different rule, not adapter-boundary; needs its own remediation.
- Stale provenance comments in `apps/VCSM/src/features/authorization/dal/actorOwners.read.dal.js:9-10` reference the two booking DALs deleted in 004B.
- Authorization README Layer-Map lists controllers that do not exist (`assertActorCanManageActor`, `resolveActorOwnerActor`).
- `apps/VCSM/src/features/hydration/setup.js:7` stale comment ("vcsmActorHydrator… merge into identity/ or delete") — resolved differently (hydration owns it).
- Stale path-header comment in `apps/VCSM/src/features/identity/identityStorage.js:1` (`// src/state/identity/identityStorage.js`).

**Scope:**
VCSM

**Architecture Impact:**
No

**Engine Impact:**
No

**Notes:**
All idea-capture only. No code, DB, or engine changes performed by CAPTAIN.

**Prompt Origin:**
run this with captain america link all files of everything we did

---
