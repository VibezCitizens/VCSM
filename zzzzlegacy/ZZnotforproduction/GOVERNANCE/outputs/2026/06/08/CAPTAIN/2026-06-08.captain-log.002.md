# CAPTAIN Log — 2026-06-08 — Run 002

> Session capture: Profiles consolidation + boundary program and follow-ups completed today.
> Records what was done and what remains pending for next session. Ideas only — not executable instructions.

---

### [2026-06-08 06:35]

**Idea:**
SESSION RECAP — Profiles consolidation + boundary program (what was DONE today)

**Context:**
Full pass over `apps/VCSM/src/features/profiles/` to separate Citizen vs VPORT domains, enforce boundaries, and clear extraction blockers. All artifacts live in `ZZnotforproduction/APPS/VCSM/features/profiles/`.

**Next Action:**
No action — reference record. Canonical evidence base = `PROFILES-CONSOLIDATION-MASTER-001.md`.

**Scope:**
VCSM

**Architecture Impact:**
Yes (boundary enforcement, ESLint rule, file relocations)

**Engine Impact:**
No

**Notes:**
DONE today (in order):
- SPLIT-001 — relocated 12 VPORT passthrough adapters → `kinds/vport/adapters/`; deleted dead umbrella.
- SPLIT-002 — identified 2 shared leaks (analysis).
- SPLIT-003 — fixed both leaks: `ProfileModel` → `model/userProfile.model.js`; split `useProfilesOps` → `useProfileOps` (shared) + `kinds/vport/hooks/useVportProfileOps.js`; relocated friends subtree → `kinds/citizen/tabs/friends/`; relocated vport DAL test.
- SPLIT-004 — boundary-enforcement readiness review.
- SPLIT-005 — authored `profiles-domain-boundary` ESLint rule (eslint-plugin-vcsm-architecture/index.js) + registered at ERROR. Dispatcher whitelist = 3 files (profileKindRegistry, ActorProfileScreen, ActorProfileViewScreen).
- SPLIT-006 — fixed `useProfileGate.js` block import → block adapter.
- SPLIT-007 — documented remaining block violations (block-side root cause).
- BLOCK-ADAPTER-NAMING-001 — classified block surface (decision OPTION C).
- BLOCK-ADAPTER-NAMING-002 — fixed `adapter-boundary` ESLint rule to recognize index barrels + `/adapters/` folders. Repo errors 167→158 (−9 adapter-boundary; 16→7 remaining, all genuine deep internals).
- OWN-001 — DECISION: canonical VPORT ownership owner = `features/authorization`; booking + vportDashboard are thin delegating wrappers; `deriveVportIsOwner` is UI-only. Ownership does NOT block VPORT extraction.
- SUBDOMAIN-DECOUPLE-001 — DECISION: services↔locksmith is domain-level coupling, NOT a module cycle; cut = relocate locksmith default-seeding out of `upsertVportServices.controller`. Cleanup, not an extraction blocker.
- BRIDGE-AUDIT-001 — audited vportDashboard bridges; found 6 dead gas adapters + ownership.adapter round-trip; no hard blockers.
- BRIDGE-CLEANUP-001 — IMPLEMENTED: deleted 7 files (6 dead gas adapters + ownership.adapter round-trip); repointed 6 vportDashboard consumers → `vportDashboard/controller/checkVportOwnership.controller`; lint clean; behavior unchanged.

Final boundary state: Citizen⇄VPORT = 0; shared→kind only via 3 dispatchers; profiles-domain-boundary rule at ERROR.

---

### [2026-06-08 06:35]

**Idea:**
PENDING #1 — PROFILES-LINT-DEBT-001 (clear ~44 pre-existing ESLint errors in profiles)

**Context:**
Profiles carries ~44 pre-existing ESLint errors unrelated to the boundary work: mostly `single-source-actor` (e.g. `TopFriendsRankEditor.jsx:23` `localActorIds`), `no-direct-layer-skip`, and `no-ttl-cache`. Surfaced during SPLIT-003/004 verification. None are boundary violations.

**Next Action:**
Open PROFILES-LINT-DEBT-001. Sweep per-rule, surgically. Independent of other tickets — can run first/parallel. Lowest risk.

**Scope:**
VCSM

**Architecture Impact:**
Possible (single-source-actor fixes may touch hook/state shape)

**Engine Impact:**
No

**Notes:**
Lowest-risk pending item. Recommended next pickup.

---

### [2026-06-08 06:35]

**Idea:**
PENDING #2 — PROFILES-SUBDOMAIN-DECOUPLE-IMPL (break services↔locksmith)

**Context:**
DECOUPLE-001 decided the plan; implementation not yet done. Backwards edge = locksmith default-seeding hardcoded at `controller/services/upsertVportServices.controller.js:98-109` (`if vportType === 'locksmith'`).

**Next Action:**
Implement Option B-via-D: make `upsertVportServices.controller` pure (return newly-enabled keys), relocate seeding to a locksmith-owned controller, invert orchestration so all edges are locksmith→services. Then edge-3 UI cleanup (`VportServicesView` → locksmith hook). Authority doc: `PROFILES-SUBDOMAIN-DECOUPLE-001.md`.

**Scope:**
VCSM

**Architecture Impact:**
Yes (subdomain dependency direction)

**Engine Impact:**
No

**Notes:**
~3–5 files. Cleanup, not an extraction blocker. Parallelizable with LINT-DEBT.

---

### [2026-06-08 06:35]

**Idea:**
PENDING #3 — Booking-assert repoint → authorization (OWN-001 follow-through)

**Context:**
~13 profiles vport controllers import `assertActorOwnsVportActorController` from `@/features/booking/adapters/booking.adapter`, which is a thin delegate to `authorization`. OWN-001 named authorization canonical.

**Next Action:**
Repoint the 13 sites to `@/features/authorization/adapters/authorization.adapter` (`assertActorOwnsActorController`) — verify arg/return shape parity first (booking wrapper may adapt args). Mechanical import swap. Optional/non-blocking.

**Scope:**
VCSM

**Architecture Impact:**
Possible (ownership consumption path)

**Engine Impact:**
No

**Notes:**
Authority doc: `PROFILES-OWN-001.md` + `PROFILES-BRIDGE-AUDIT-001.md`. Confirm booking wrapper isn't reshaping args before swapping.

---

### [2026-06-08 06:35]

**Idea:**
PENDING #4 — PROFILES-CACHE-MODERNIZE-001 (TTL cache → React Query)

**Context:**
~13 `@/shared/lib/ttlCache` imports in profiles DAL; `no-ttl-cache` governance prefers React Query. `useProfileOps` exposes the invalidation contract consumed externally by settings/profile.

**Next Action:**
Migrate profile TTL caches to React Query; PRESERVE the `useProfileOps` invalidation contract for the settings consumer. Gated by / overlaps LINT-DEBT-001 (no-ttl-cache).

**Scope:**
VCSM

**Architecture Impact:**
Yes (caching behavior)

**Engine Impact:**
No

**Notes:**
Cross-feature cache contract — don't break settings/profile.

---

### [2026-06-08 06:35]

**Idea:**
PENDING #5 — BLOCK-ADAPTER-NAMING-003 (optional block hygiene)

**Context:**
`features/block/adapters/ui/ActorActionsMenu.jsx` lacks the `.adapter` suffix its siblings have. No longer needed for lint (NAMING-002 made the rule recognize `/adapters/` folders) — pure hygiene.

**Next Action:**
OPTIONAL: rename → `ActorActionsMenu.adapter.jsx` + update 1 importer (`profiles/screens/views/ActorProfileHeader.jsx`). Block-owned, not profiles.

**Scope:**
VCSM

**Architecture Impact:**
No

**Engine Impact:**
No

**Notes:**
Low priority. Authority doc: `features/block/BLOCK-ADAPTER-NAMING-001.md`.

---

### [2026-06-08 06:35]

**Idea:**
PENDING #6 — Identity boundary ticket (IN PROGRESS — interrupted)

**Context:**
A scoped identity adapter-boundary ticket was mid-analysis when paused. 3 cross-feature identity-internal imports remain repo-wide: `hydration/vcsmActorHydrator.js` → `identity.read.dal` + `identity.controller`; `profiles/kinds/vport/screens/booking/hooks/useVportBookingView.js` → `identitySelectors`. These are deep-internal imports correctly still flagged by adapter-boundary. Owned by the Identity program, NOT the Profiles map.

**Next Action:**
Resume: classify each (FIX NOW if existing identity adapter surface provides the capability; DEFER if it would require exposing an internal — no new internal exports). Locked rules: adapters expose hooks/runtime APIs only, never DAL/controller/model/selector/store. If unresolved → IDENTITY-BOUNDARY-008.

**Scope:**
VCSM

**Architecture Impact:**
Yes (identity boundary)

**Engine Impact:**
Possible (hydration consumes identity internals)

**Notes:**
Memory: identity program has its own MASTER + 001-004A done; authorization is canonical ownership; booking = wrappers. Don't restart the full identity review.

---

### [2026-06-08 06:35]

**Idea:**
PENDING #7 — PROFILES-PHYSICAL-FOLDERS-001 (optional) + PROFILES-EXTRACT-VPORT-001 (future)

**Context:**
Zones are logical path-globs today (no physical `shared/`/`dispatch/` folders). Extraction is the end goal. After OWN-001 + DECOUPLE-001 + BRIDGE-AUDIT/CLEANUP, **VPORT extraction has NO remaining hard blockers** — only the cleanup items above.

**Next Action:**
PHYSICAL-FOLDERS-001 (optional hygiene): materialize physical zone folders + update rule globs. EXTRACT-VPORT-001 (future): run cleanup steps (decouple impl, booking-assert repoint, dead-bridge already done) then extract. Authority docs: MASTER + BRIDGE-AUDIT-001.

**Scope:**
VCSM

**Architecture Impact:**
Yes (major — extraction)

**Engine Impact:**
No

**Notes:**
Sequence: LINT-DEBT + DECOUPLE-IMPL (parallel) → booking repoint → (cache) → (physical folders) → EXTRACT-VPORT. Extraction is the largest, do last.

---

### [2026-06-08 06:35]

**Idea:**
DOC-HYGIENE NOTE — MASTER doc corrections made this session

**Context:**
During the session, MASTER (`PROFILES-CONSOLIDATION-MASTER-001.md`) had two inaccuracies caught and fixed: TD-01 undercounted block violations (1→5); extraction-readiness rows for VPORT booking/gas/team were stale ("BLOCKED") and were corrected to NOT BLOCKED. A missing `PROFILES-BRIDGE-CLEANUP-001.md` was also created after the fact.

**Next Action:**
When resuming, trust MASTER as canonical but spot-check that TD rows + extraction-readiness table stay in sync with the per-ticket docs (they drifted once this session).

**Scope:**
VCSM

**Architecture Impact:**
No (documentation)

**Engine Impact:**
No

**Notes:**
Lesson: update MASTER TD rows AND the extraction-readiness table together when a ticket closes.

---

**Prompt Origin:**

run captain america, everything we did and pending for tomorrow what ever is pending
