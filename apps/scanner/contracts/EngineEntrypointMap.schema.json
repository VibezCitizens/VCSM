{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "EngineEntrypointMap",
  "type": "object",
  "required": ["version", "scannerVersion", "generatedAt", "root", "scanDurationMs", "confidence", "engineEntrypointCount", "data"],
  "properties": {
    "version": { "const": 1 },
    "scannerVersion": { "type": "string" },
    "generatedAt": { "type": "string", "format": "date-time" },
    "root": { "type": "string" },
    "scanDurationMs": { "type": "number" },
    "confidence": { "enum": ["HIGH", "MEDIUM", "LOW", "BLOCKED"] },
    "engineEntrypointCount": { "type": "number" },
    "data": {
      "type": "object",
      "required": ["engines"],
      "properties": {
        "engines": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["engine", "path", "entrypoints", "confidence"],
            "properties": {
              "engine": { "type": "string" },
              "path": { "type": "string" },
              "entrypoints": { "type": "array", "items": { "type": "string" } },
              "confidence": { "enum": ["HIGH", "MEDIUM", "LOW", "BLOCKED"] }
            }
          }
        }
      }
    }
  }
}
