# SPA fallback — single safe catch-all for Cloudflare Pages.
# Cloudflare Functions take priority over this rule automatically,
# so /how-to/*, /vport/*/card, /p/* and any other function routes are unaffected.
/* /index.html 200
