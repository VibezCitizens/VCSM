VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# OneSignal Web Push — App ID is safe for frontend. REST key stays server-side.
VITE_ONESIGNAL_APP_ID=
# Sentry error reporting (VCSM only) — optional. App runs without this.
# Get your DSN from: https://sentry.io → Project Settings → Client Keys (DSN)
VITE_SENTRY_DSN=
