// src/features/chat/vchat/VChatScreen.jsx
// If your ChatScreen already respects the active identity (profile/vport),
// we can just re-export it and use a different route namespace.
import Chat from '@/features/chat/ChatScreen';

export default Chat;
