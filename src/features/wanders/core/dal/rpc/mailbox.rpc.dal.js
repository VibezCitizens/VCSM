// C:\Users\trest\OneDrive\Desktop\VCSM\src\features\wanders\core\dal\rpc\mailbox.rpc.dal.js
// ============================================================================
// WANDERS DAL — MAILBOX (RPC)
// Contract: raw rpc output only, explicit args, no derived meaning.
// ============================================================================

import { getWandersSupabase } from "@/features/wanders/services/wandersSupabaseClient";

// NOTE: Only add functions here when you actually create SQL rpc() in Postgres.
// Keeping file ready so your DAL categories stay consistent.

/**
 * Example stub:
 * export async function rpcWandersMailboxSomething(input) {
 *   const supabase = getWandersSupabase();
 *   const { data, error } = await supabase.rpc("wanders_some_fn", { ... });
 *   if (error) throw error;
 *   return data;
 * }
 */
